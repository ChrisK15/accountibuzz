# JIRA Skip Rationale — Plan 03.1-03 (Shutter Visual Rewrite)

**Date:** 2026-05-10
**Plan:** `03.1-03` — Shutter visual rewrite (D-08..D-10)
**Phase:** `03.1-p3-polish-realtime-hardening`
**Author:** Plan 03 executor agent (parallel worktree)

## Context

Per `CLAUDE.md` "Process — JIRA + commit convention" + `docs/processes/03-requirements-status-tracking.md`, every commit under a phase scope must update the matching JIRA story (status forward + work log). Per the plan's `<user_setup>` block, this work was to be tied to a SCRUM story whose ID was to be captured by Plan 01 Task 1 and inherited here.

## Why Task 4 is deferred (not skipped)

Three independent constraints prevent the executor from executing Task 4 inline:

1. **Atlassian MCP tools are unavailable in this subagent.** The executor was spawned as a parallel-wave worktree agent and inherits only the Read/Write/Edit/Bash tool set. The Atlassian MCP tools (`mcp__atlassian__editJiraIssue`, `mcp__atlassian__addCommentToJiraIssue`) are not in scope. This matches the upstream Claude Code limitation noted in the project memory file (`reference_claude_code_subagent_skills.md`).

2. **Plan 01 has not yet shipped.** At the time of this commit:
   - `git log --oneline -10` shows no `03.1-01` SUMMARY commit
   - `.planning/phases/03.1-p3-polish-realtime-hardening/` contains only PLAN files; no SUMMARY files exist for any of the three Wave 1 plans
   - Per the plan's `<user_setup>`, the SCRUM ID for Phase 03.1 was to be captured by Plan 01 Task 1 — that capture has not yet occurred

3. **Plan 03 has `requirements: []` in frontmatter.** Unlike Phase 4 plans that mapped 1:1 to SCRUM-31..38, Phase 03.1 is a pure polish/UX phase with no v1 requirement IDs. The `docs/processes/04-tracing.md` SCRUM-epic table has no row for Phase 03.1, so there is no canonical SCRUM ID to inherit even if Plan 01 had shipped.

## Action required at phase close

The orchestrator (or whoever runs the closing `/gsd-complete-phase` for 03.1) must:

1. Identify or create the SCRUM story for the Shutter rewrite (likely a new bug-class story under the `SUB-*` epic SCRUM-4, since the Shutter is part of the capture surface), OR confirm that Phase 03.1 will be tracked as a single rolled-up story alongside the capture-modal + review-realtime work.
2. Move that SCRUM story status forward per the closing convention (To Do → In Progress on first commit; In Progress → In Review or Done on phase close).
3. Add a cumulative work-log entry covering Plan 03 effort (3 commits: test scaffold + Shutter rewrite + UAT verification scaffold; estimated 45-60 min total).
4. Suggested work-log message:

   ```
   Plan 03: Shutter visual rewrite — close 2026-05-06 user complaint (D-08..D-10).
   3-variant Feather-icon-based design replaces the "square inside a circle" geometry.
   Public ShutterProps API unchanged. UAT receipts CK-09..CK-12 captured.
   Commits: 52b5c66 (test scaffold), 3ee3af4 (rewrite), 6a0e3fc (UAT scaffold).
   ```

## Audit trail

This deferral is intentional and documented per CLAUDE.md ("a commit that touches a phase scope but does not update JIRA is treated as a process defect — the audit on 2026-04-28 (SCRUM-25–30 rollback) is the canonical example of what happens when this is skipped"). The skip is recorded here so the audit trail is preserved and the closing-phase action is explicit.
