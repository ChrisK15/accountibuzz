---
phase: 04-social-surfaces
plan: 07
type: execute
wave: 4
depends_on: ["04-05", "04-06"]
files_modified:
  - .planning/phases/04-social-surfaces/04-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [PTS-01, PTS-02, PTS-03, LB-01, LB-02, FEED-01, FEED-02, FEED-03]
must_haves:
  truths:
    - "Full automated suite green: pnpm test --ci (Jest) + supabase test db (pgTAP) + pnpm typecheck + pnpm expo-doctor"
    - "All 8 P4 requirements (PTS-01..03, LB-01..02, FEED-01..03) verified per the validation map in 04-VALIDATION.md"
    - "HIGH #13 (REVIEWS.md, RESOLVED via REVIEWS replan 2026-05-08): BEFORE the 2-device UAT begins, verify against the REMOTE/dev Supabase project that group_members AND submissions are in supabase_realtime publication. Local pgTAP cannot prove the remote project is migrated correctly."
    - "LB-02 hard gate (cross-device leaderboard Realtime) cleared via 2-device manual UAT — mirror P3 CK-5"
    - "Hard-gate count is consistent across summary + checklist + acceptance criteria: 6 hard gates (CK-02 LB-02 cross-device, CK-03 D-19 client-counter-write rejection, CK-04 FEED-01 visual, CK-05 FEED-01 Realtime prepend, CK-07 FEED-03 tombstone tone, CK-13 D-11 fullscreen viewer) + 1 prerequisite hard gate (CK-00 remote publication check). MEDIUM concern from REVIEWS.md RESOLVED via REVIEWS replan 2026-05-08."
    - "REQUIREMENTS.md and ROADMAP.md updated to reflect Phase 4 completion (8 requirements flipped to Complete)"
    - "04-VERIFICATION.md captures the full UAT walkthrough with PASS/FAIL/DEFERRAL for each of 14 checkpoints (CK-00 through CK-14, except CK-00 is the remote-publication prerequisite added in this replan)"
  artifacts:
    - path: .planning/phases/04-social-surfaces/04-VERIFICATION.md
      provides: "Authoritative phase-close report — automated suite results + remote-publication check + UAT walkthrough + any inline-fix audit trail"
    - path: .planning/REQUIREMENTS.md
      provides: "Updated traceability — 8 P4 requirements marked Complete"
    - path: .planning/ROADMAP.md
      provides: "Phase 4 status flipped to Complete with completion date"
  key_links:
    - from: 04-VERIFICATION.md
      to: P4 requirements (PTS-01..03, LB-01..02, FEED-01..03)
      via: "1:1 PASS/FAIL gate per requirement"
      pattern: "PTS-01.*PASS"
    - from: 04-VERIFICATION.md CK-00 (NEW)
      to: remote Supabase project supabase_realtime publication
      via: "psql query against the dev project ref"
      pattern: "pg_publication_tables"
---

## Revisions Applied

This plan was revised on 2026-05-08 in response to cross-AI review (`04-REVIEWS.md`). Concerns addressed:

- **HIGH #13** (Remote publication state not verified during UAT): Added a NEW prerequisite hard-gate task **CK-00** that runs BEFORE the 2-device UAT begins. The check is `psql -c "select tablename from pg_publication_tables where pubname='supabase_realtime' and schemaname='public' and tablename in ('group_members','submissions')" $REMOTE_URL`. The output must contain BOTH `group_members` AND `submissions`. Without this verification, LB-02 + FEED-01 Realtime can pass locally (pgTAP) and fail on devices because the remote project's publication wasn't updated. The `$REMOTE_URL` is the dev Supabase project ref the devices use — captured in init context. See Task 1 (modified to include CK-00) and Task 2 (precedes the 2-device UAT).
- **MEDIUM** (Hard gate count inconsistent): Reconciled to **6 hard gates** consistently across summary, checklist, and acceptance criteria — CK-02, CK-03, CK-04, CK-05, CK-07, CK-13 — plus the new CK-00 prerequisite (which is itself a hard gate). Total: 6 in-UAT hard gates + 1 pre-UAT prerequisite = 7. The `<truths>` field, summary, and checklist all use this count.
- **LOW** ("12 CK" reference): Replaced with the consistent count of 14 checkpoints (CK-01..CK-14) — CK-00 is the new prerequisite (separate from the 14-CK checklist). Total visible CK rows in 04-VERIFICATION.md: 15 (1 prerequisite + 14 UAT).
- **LOW** (truth says `pnpm test:all` but task runs `pnpm test --ci`): Locked the truth to `pnpm test --ci` (Jest only — pgTAP is `supabase test db`). The phrase `pnpm test:all` is removed from must_haves.truths.

Iteration 1: WARNING-4 resolved — added the D-06 known-limit note under CK-07: late-approved 2-day-old submissions do NOT update today's 'Missed yesterday' row; UAT operators are explicitly told this is documented MVP behavior, not a bug.

Iteration 1: WARNING-5a resolved — CK-14 now ships a concrete burst sub-checkpoint (3 submissions approved within 1s with Reduce Motion OFF) to surface LayoutAnimation self-conflict; soft-gate disposition is preserved (jitter, if observed, becomes a P5 polish ticket — does not block phase close).

Iteration 2 (added 2026-05-08, mid-Wave-1): **PRE-TASK — repair migration-history filename mismatch before Task 1's pgTAP CLI run.** During plan 04-02's checkpoint, `supabase db push` failed because the remote migration history records two Phase 3 migrations under their MCP-applied timestamp version IDs (`20260429173246_phase3_capture_review`, `20260506165538_phase3_realtime_publication`) while the local files are named `0006_phase3_capture_review.sql` and `0007_phase3_realtime_publication.sql` (same SQL, different version strings). User chose to apply migration 0008 via Supabase MCP for that plan (registered remotely as `20260508233129_phase4_points_streaks_feed`). Before Task 1 can run `supabase test db` (which the CLI gates on a clean migration history), one of these paths must be executed and committed in 04-07:

  Option A (recommended) — `git mv supabase/migrations/0006_phase3_capture_review.sql → supabase/migrations/20260429173246_phase3_capture_review.sql`; `git mv 0007_phase3_realtime_publication.sql → 20260506165538_phase3_realtime_publication.sql`; `git mv 0008_phase4_points_streaks_feed.sql → 20260508233129_phase4_points_streaks_feed.sql`. Update any plan/summary doc references via `grep -rl "0006_phase3\|0007_phase3\|0008_phase4" .planning/ supabase/` (mostly path-only mentions; do not change SQL semantics). Verify with `supabase migration list` post-rename: local + remote show identical version IDs across all 8 entries.

  Option B — Run `supabase db pull` to capture remote-applied SQL as new local files; reconcile any duplicate content; commit. Less predictable since local already has equivalent content under different IDs.

After repair, `supabase test db` runs cleanly; the 4 new pgTAP files from 04-01 (handle_submission_approval_streak, handle_submission_approval_idempotency, phase4_rpc_permissions, phase4_rpc_correctness) flip RED→GREEN against the already-applied schema. This unblocks Task 1's automated-suite acceptance criteria (≥130 ok lines).

<objective>
Phase 4 closeout. Run the full automated suite (Jest + pgTAP + typecheck + expo-doctor), verify the **REMOTE** Supabase project's `supabase_realtime` publication includes `group_members` AND `submissions` (HIGH #13 — RESOLVED via REVIEWS replan 2026-05-08), execute a 2-device manual UAT covering the cross-device LB-02 hard gate plus visual checks. Document every checkpoint outcome in `04-VERIFICATION.md`. Update `REQUIREMENTS.md` + `ROADMAP.md` to reflect 8 P4 requirements complete.

Purpose: This is the gate that proves Phase 4 ships. Without the remote-publication check (CK-00), local pgTAP green is a false positive — the dev/remote project might be missing the publication entries shipped in 04-02.

Output: 1 new VERIFICATION report + 2 doc updates. After this, STATE.md gets updated by the orchestrator and Phase 5 unblocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-social-surfaces/04-CONTEXT.md
@.planning/phases/04-social-surfaces/04-RESEARCH.md
@.planning/phases/04-social-surfaces/04-PATTERNS.md
@.planning/phases/04-social-surfaces/04-UI-SPEC.md
@.planning/phases/04-social-surfaces/04-VALIDATION.md
@.planning/phases/04-social-surfaces/04-REVIEWS.md
@.planning/phases/04-social-surfaces/04-01-SUMMARY.md
@.planning/phases/04-social-surfaces/04-02-SUMMARY.md
@.planning/phases/04-social-surfaces/04-03-SUMMARY.md
@.planning/phases/04-social-surfaces/04-04-SUMMARY.md
@.planning/phases/04-social-surfaces/04-05-SUMMARY.md
@.planning/phases/04-social-surfaces/04-06-SUMMARY.md
@.planning/phases/03-capture-admin-review/03-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated suite + remote publication prerequisite check (CK-00) + record results</name>
  <files>.planning/phases/04-social-surfaces/04-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/03-capture-admin-review/03-VERIFICATION.md (the report structure to mirror)
    - 04-VALIDATION.md (the requirement-to-test map)
    - .planning/phases/04-social-surfaces/04-REVIEWS.md HIGH #13 (the remote-publication gate)
  </read_first>
  <action>
Run, in order:

1. `pnpm typecheck` — record exit code and any error count.
2. `pnpm test --ci` (full Jest suite — note: NOT `pnpm test:all`; LOW concern from REVIEWS.md normalized — RESOLVED via REVIEWS replan 2026-05-08) — record total test count + pass count + fail count.
3. `supabase test db` (full pgTAP) — record total assertion count + ok count + not-ok count + file count.
4. `pnpm expo-doctor` — record exit code + any warnings.

5. **NEW (HIGH #13 — RESOLVED via REVIEWS replan 2026-05-08): CK-00 remote-publication prerequisite check.**

Resolve the dev Supabase project's psql connection string (`$REMOTE_URL` — typically `postgres://postgres:<password>@db.<ref>.supabase.co:5432/postgres`, taken from the gsd-init context or the `.env` `EXPO_PUBLIC_SUPABASE_*` values + the project's `SUPABASE_DB_PASSWORD`). The exact env var name depends on gsd-init; if not set, escalate as a checkpoint and ask the user to run the psql command and paste output.

Run:
```bash
psql "$REMOTE_URL" -c "select tablename from pg_publication_tables where pubname='supabase_realtime' and schemaname='public' and tablename in ('group_members','submissions') order by tablename"
```

Expected output: 2 rows containing `group_members` and `submissions`.

If only `submissions` is returned (the P3 row from 0007), the dev project is missing 04-02's publication-add — surface as a checkpoint and instruct the user to run `supabase db push` against the remote project to apply migration 0008. If neither is returned, the dev project is missing both 0007 AND 0008 — escalate. If both are returned, CK-00 PASS.

Capture the output of each command. Flag any failure as a hard gate that requires a fix before continuing.

Create `.planning/phases/04-social-surfaces/04-VERIFICATION.md` with this structure (markdown body):

- Title `# Phase 4 — Verification Report`
- Header lines: Phase, Generated date, Status (Pending until Task 2 closes)
- §Summary: 8 requirements, **6 in-UAT hard gates** (CK-02, CK-03, CK-04, CK-05, CK-07, CK-13) + **1 pre-UAT hard gate** (CK-00 remote publication) = 7 hard gates total, N soft gates, inline fixes count. **MEDIUM hard-gate-count inconsistency RESOLVED via REVIEWS replan 2026-05-08** — the count is 6 in-UAT + 1 prereq = 7, used consistently across this section, the checklist, and acceptance criteria.
- §Automated Suite — table with rows: typecheck, pnpm test --ci (pass/total), supabase test db (ok/total across N files), expo-doctor (N/M)
- §**CK-00 Remote publication check (NEW, HIGH #13 — RESOLVED via REVIEWS replan 2026-05-08)**: PASS/FAIL with the verbatim psql output snippet showing `group_members` AND `submissions` in the result
- §Requirement Verification — table with one row per requirement (PTS-01, PTS-02, PTS-03, LB-01, LB-02, FEED-01, FEED-02, FEED-03), columns: Requirement / Hard or Soft / Test Type / Status (PASS/FAIL/DEFER) / Evidence
- §UAT Walkthrough — placeholder for Task 2 (CK-01..CK-14)
- §Inline Fixes — placeholder
- §Outstanding Items — placeholder

Fill in the Automated Suite table + the CK-00 row + the test-based requirement verifications now (PTS-01 / PTS-02 / PTS-03 / LB-01 / FEED-01 / FEED-02 / FEED-03 from the 4 pgTAP files + 5+ Jest hook tests). LB-02 + visual checks are filled by Task 2.

If `pnpm typecheck` or `pnpm test --ci` or `supabase test db` reports a failure, OR CK-00 fails, surface as a checkpoint and stop. Downstream UAT cannot proceed.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test --ci 2>&1 | tail -5 && supabase test db 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04-social-surfaces/04-VERIFICATION.md` exists
    - `pnpm typecheck` exits 0
    - `pnpm test --ci` exits 0; total test count >= 270 (P3 baseline 240 + at least 30 new P4 tests across 04-01 RED scaffolds + 04-03 implementation + 04-04 components + 04-05 MediaViewer + 04-06 today-social-signal)
    - `supabase test db` exits 0; total assertion count >= 130 (P3 baseline 107 + at least 25 new P4 assertions across 4 files)
    - `pnpm expo-doctor` runs; any failures documented (the existing 17/18 P1 deferral acceptable)
    - **HIGH #13 CK-00 gate (RESOLVED via REVIEWS replan 2026-05-08)**: 04-VERIFICATION.md contains a CK-00 row with PASS status AND the psql output shows BOTH `group_members` AND `submissions` in the publication. Verify via: `grep -c "CK-00" .planning/phases/04-social-surfaces/04-VERIFICATION.md` is at least 1 AND `grep -c "group_members" .planning/phases/04-social-surfaces/04-VERIFICATION.md` is at least 1 AND `grep -c "supabase_realtime" .planning/phases/04-social-surfaces/04-VERIFICATION.md` is at least 1
    - `grep -c "Automated Suite" .planning/phases/04-social-surfaces/04-VERIFICATION.md` is at least 1
    - `grep -c "PTS-01\|LB-01\|FEED-01" .planning/phases/04-social-surfaces/04-VERIFICATION.md` is at least 3
    - **MEDIUM hard-gate-count gate (RESOLVED via REVIEWS replan 2026-05-08)**: The summary §Summary section text is INTERNALLY CONSISTENT — `grep -E "6 in-UAT hard gates|7 hard gates total|6 hard gates" .planning/phases/04-social-surfaces/04-VERIFICATION.md` is at least 1
  </acceptance_criteria>
  <done>Automated suite green; CK-00 remote-publication prerequisite green; results recorded in 04-VERIFICATION.md; ready for manual UAT.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 2-device manual UAT — LB-02 hard gate + visual + tone checks (14 CK rows after CK-00 prereq)</name>
  <files>.planning/phases/04-social-surfaces/04-VERIFICATION.md</files>
  <read_first>
    - 04-VALIDATION.md "Manual-Only Verifications" (the 4 manual checkpoints)
    - 04-UI-SPEC.md (the visual contract)
    - 04-CONTEXT.md D-08 (quiet streak break — visual tone)
    - .planning/phases/03-capture-admin-review/03-VERIFICATION.md "CK-5 Cross-Device Realtime"
  </read_first>
  <what-built>
Phase 4 ships:
- Migration 0008 with race-safe handle_submission_approval (HIGH #1), replica identity full on submissions (HIGH #2), 4 RPCs with deterministic leaderboard tiebreaker (MEDIUM), group_members in supabase_realtime publication (CGF-1), group_members_counter_immutable trigger
- 7 data hooks (4 reads + 3 Realtime); useGroupTombstones returns the locked shape (MEDIUM #3); useGroupFeedRealtime uses MVP invalidation (HIGH #8); useGroupLeaderboardRealtime gates LayoutAnimation on reduceMotion (HIGH #3 cascade)
- src/theme/applyAlpha.ts (HIGH #5) + src/hooks/useSignedMediaUrl.ts (HIGH #11) shared infrastructure
- 5 UI components (LeaderboardRow with Animated.View cross-fade, FeedItem consuming shared useSignedMediaUrl with 'text' a11y role + conditional FeedVideoThumb, StillToPostAvatarRow, MissedYesterdayRow with applyAlpha, MediaViewer fullscreen surface) + GroupCard.social prop
- Group-detail screen with 4 new sections in CORRECT D-09 order (HIGH #4 — PendingReviewRow + InvitePanel MOVED below Members) + MediaViewer Modal + tz-correct cutoffLabelFor (HIGH #9) + reduceMotion declared TDZ-safe before consuming hooks (HIGH #3)
- Today screen with social-signal line on every GroupCard, STRICT social-prop derivation (HIGH #10), userId lifted to parent (MEDIUM)

Full automated suite is green (Jest + pgTAP + typecheck + expo-doctor). CK-00 remote-publication prerequisite is PASS (HIGH #13).
  </what-built>
  <how-to-verify>
**Setup:**
1. Build dev client on iPhone (or 2 iOS devices if available — prefer 2 different physical devices for the LB-02 hard gate).
2. Install on Device A and Device B. Sign in to two different accounts both members of the same test group with admin set on Device A.
3. Pre-seed test data.

**UAT Checklist (14 in-UAT checkpoints CK-01..CK-14, after the CK-00 prereq from Task 1; record PASS/FAIL/DEFER for each in 04-VERIFICATION.md UAT Walkthrough section):**

CK-01 (LB-01 leaderboard renders) — On Device B, open group-detail. Confirm Leaderboard section renders top-5 sorted by points DESC, current_streak DESC, joined_at ASC.

CK-02 (LB-02 cross-device Realtime — HARD GATE, mirror of P3 CK-5) — On Device B, navigate to group-detail. On Device A (admin), approve a pending submission for one of Device B's group members. Within ~1 second, Device B's leaderboard should reorder. PASS = visible reorder within ~2s.

CK-03 (D-19 column-allowlist trigger — HARD GATE) — From psql admin JWT, attempt `update public.group_members set points = 9999 where ...`. Expect raise: 'group_members counter columns are server-managed'.

CK-04 (FEED-01 today's feed renders — HARD GATE) — On Device A, open the test group's group-detail. Confirm Today's posts section shows the approved submissions.

CK-05 (FEED-01 Realtime feed prepend — HARD GATE) — On Device B, on group-detail. On Device A, approve a fresh pending submission. The feed on Device B should refetch and update within ~200ms (HIGH #8 MVP invalidation strategy — the new FeedItem appears via the invalidation+refetch path, NOT optimistic prepend). PASS = updated feed within ~2s.

CK-06 (FEED-02 still-to-post visible) — On Device A or B, open group-detail. Confirm Still to post section shows overlapping avatars + first-name comma list + cutoff inline.

CK-07 (FEED-03 missed yesterday tombstones — HARD GATE TONE) — Open group-detail. If at least one member missed yesterday, confirm MISSED YESTERDAY section renders with surface-muted/40 background (via applyAlpha — HIGH #5), 28pt avatars at opacity 0.7, comma-separated names, and trailing tz-correct copy. PASS = quiet, factual.

> **D-06 design limit (Iteration 1 plan-checker fix 2026-05-08, WARNING-4 — late-approved 2-day-old submissions)**: A 2-day-old submission that an admin approves today does NOT remove its author from today's "Missed yesterday" row, because `get_missed_yesterday`'s query only checks `local_date = (now() AT TIME ZONE g.timezone)::date - 1`. UAT operators MUST NOT treat this as a bug — it is the documented MVP behavior (D-06 yesterday-only tombstone semantics). If product later wants stale-approval cleanup to update tombstones, promote D-06 to a multi-day window in a future phase.

CK-08 (D-13 Today GroupCard social-signal) — On Today screen, confirm each GroupCard renders the social-signal line below the existing CTA. **HIGH #10 verification (RESOLVED via REVIEWS replan 2026-05-08): when leaderboard is loading the line is HIDDEN — observe the brief moment after navigation where the card shows only the existing P3 layout, then the social line appears once data lands.**

CK-09 (D-15 per-card Realtime invalidation) — On Today screen, open the test group's card. On Device A, approve a submission for this group. Within ~1 second, Device B's GroupCard social-signal line should update.

CK-10 (D-16 GroupCard tap unchanged) — Tap a GroupCard on Today screen. Routes to group-detail.

CK-11 (D-10 leaderboard tap-to-expand) — On group-detail, if total members > 5, tap "See all". Leaderboard expands. Tap "Show top 5" — collapses.

CK-12 (Pitfall #11 Realtime cleanup on tab navigation) — Navigate to group-detail. Tab to Profile. Tab back → group-detail. Approve a submission on Device A. Confirm Device B receives the patch (channel re-subscribed).

CK-13 (D-11 fullscreen viewer — HARD GATE) — On group-detail, tap the 80×80 thumbnail of a Today's posts FeedItem. The shared `<MediaViewer>` Modal opens at full bleed. Tap close button. Modal dismisses. Hardware back button on Android also dismisses.

CK-14 (LayoutAnimation row-reorder + 250ms cross-fade + burst self-conflict — visual confirmation, soft gate) — On Device B, on group-detail. On Device A, approve a submission for a member who is mid-pack. The leaderboard should animate position swap; affected row's points + streak briefly cross-fade. Toggle Reduce Motion ON, repeat: animations should be SKIPPED.

> **Burst sub-checkpoint (Iteration 1 plan-checker fix 2026-05-08, WARNING-5a — concrete LayoutAnimation burst test)**: With Reduce Motion OFF, on Device B keep group-detail visible. From Device A, approve 3 submissions for the same group within 1 second (rapid-fire taps in the admin review queue). On Device B, observe the leaderboard rows reorder smoothly — no flicker, no row-swap jitter, no LayoutAnimation self-conflict. If jitter IS observed (rows snap or visibly tear), file a P5 polish ticket — this is a documented MVP risk per Gemini MEDIUM in 04-REVIEWS.md and is acceptable for ship; the soft-gate disposition does NOT block phase close.

For each checkpoint, record in 04-VERIFICATION.md: CK-NN: PASS|FAIL|DEFER and 1-2 lines on observed behavior.

**HIGH #4 VERIFICATION (RESOLVED via REVIEWS replan 2026-05-08)**: While running CK-01..CK-13 manually, screenshot the group-detail screen and confirm the visual section order: Header → Leaderboard → Today's posts → Still to post → Missed yesterday → Members → Pending review (admin-only) → Invite panel (admin) → Leave/Transfer/Delete. If observed order does NOT match D-09, surface as a checkpoint failure even if individual sections pass.

If any HARD GATE (CK-02, CK-03, CK-04, CK-05, CK-07, CK-13) fails, surface as a hard checkpoint — fix before phase close.
If any soft check fails, document the deferral with rationale.
  </how-to-verify>
  <action>
After the manual UAT walkthrough completes, fill in the UAT Walkthrough + Inline Fixes + Outstanding Items sections of 04-VERIFICATION.md.

For LB-02 (CK-02), record:
- PASS/FAIL
- Approximate latency observed
- Confirmation that CK-00 prereq remote-publication check showed both `group_members` AND `submissions` (so Realtime path is structurally correct)
- Any inline fixes applied during the test

For FEED-03 / D-08 tone (CK-07), record a screenshot reference confirming the tombstone visual reads as factual / not punitive.

For D-09 stack order (CK-01..CK-13 cross-cutting), record a screenshot reference confirming Members appears BEFORE Pending review (HIGH #4 RESOLVED via REVIEWS replan 2026-05-08).
  </action>
  <verify>
    <automated>grep -c "CK-02.*PASS" .planning/phases/04-social-surfaces/04-VERIFICATION.md</automated>
  </verify>
  <resume-signal>"phase-complete" (after all 14 CK rows recorded with hard gates green) or describe failures + planned fix</resume-signal>
  <acceptance_criteria>
    - All 14 CK rows recorded in 04-VERIFICATION.md with PASS/FAIL/DEFER (CK-01 through CK-14)
    - Plus CK-00 remote-publication prereq from Task 1 — separate row, also PASS
    - **LOW count consistency gate (RESOLVED via REVIEWS replan 2026-05-08)**: `grep -cE "CK-1?[0-9]{1,2}\\b" .planning/phases/04-social-surfaces/04-VERIFICATION.md` is at least 14 (the 14 in-UAT checkpoints) — NO references to "12 CK"; verify via `grep -c "12 CK\|12 checkpoints" .planning/phases/04-social-surfaces/04-VERIFICATION.md` returns 0
    - All HARD GATES (CK-00 prereq + CK-02, CK-03, CK-04, CK-05, CK-07, CK-13) marked PASS — OR documented inline fixes that flipped them to PASS
    - `grep -c "CK-" .planning/phases/04-social-surfaces/04-VERIFICATION.md` is at least 14
    - `grep -E "CK-(02|03|04|05|07|13)[^|]*FAIL" .planning/phases/04-social-surfaces/04-VERIFICATION.md` returns no matches (no hard-gate FAILs)
    - The Inline Fixes section captures any commits made during UAT
    - **HIGH #4 verification reference**: 04-VERIFICATION.md mentions either a screenshot path OR a textual confirmation that the D-09 stack order was visually verified (Members BEFORE Pending review)
  </acceptance_criteria>
  <done>Manual UAT complete; 04-VERIFICATION.md captures all 14 CK outcomes + CK-00 prereq; hard gates green; D-09 stack order visually verified.</done>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md + ROADMAP.md to reflect Phase 4 completion</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (existing v1 list + traceability table)
    - .planning/ROADMAP.md (existing Phase 4 entry + Progress table)
    - .planning/phases/04-social-surfaces/04-VERIFICATION.md (created in Tasks 1-2)
  </read_first>
  <action>
**Update REQUIREMENTS.md:**

In the "Points, Streaks, Leaderboard" section, flip the 5 requirement bullets from `[ ]` to `[x]`:
- PTS-01, PTS-02, PTS-03, LB-01, LB-02

In the "Group Feed" section, flip the 3 requirement bullets from `[ ]` to `[x]`:
- FEED-01, FEED-02, FEED-03

In the "Traceability" table, flip the Status column for those 8 rows from "Pending" to "Complete".

If any requirement was DEFERRED via the UAT, mark it as PARTIAL with a footnote linking to 04-VERIFICATION.md.

**Update ROADMAP.md:**

In the Phase 4 entry, change the leading checkbox `[ ]` to `[x]`.

In the Progress table at the bottom, update the Phase 4 row:
- Plans Complete: 7/7
- Status: Complete (iOS UAT PASS — all hard gates including CK-00 remote-publication prereq)
- Completed: today's date in YYYY-MM-DD

Update the v1 progress count if other tables track it.
  </action>
  <verify>
    <automated>grep -cE "\[x\] \*\*(PTS-01|PTS-02|PTS-03|LB-01|LB-02|FEED-01|FEED-02|FEED-03)" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "\[x\] \*\*(PTS-01|PTS-02|PTS-03|LB-01|LB-02|FEED-01|FEED-02|FEED-03)" .planning/REQUIREMENTS.md` returns at least 8
    - In the Traceability table: `grep -cE "(PTS-0[1-3]|LB-0[1-2]|FEED-0[1-3]).*Complete" .planning/REQUIREMENTS.md` returns at least 8
    - `grep -c "\[x\] \*\*Phase 4" .planning/ROADMAP.md` returns at least 1
    - `grep -c "Phase 4.*Complete\|4. Social Surfaces.*Complete" .planning/ROADMAP.md` returns at least 1
  </acceptance_criteria>
  <done>REQUIREMENTS.md + ROADMAP.md reflect 8 P4 requirements complete; ready for STATE.md update by orchestrator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 2-device test data | Test JWT claims on dev devices |
| Local pgTAP vs remote Supabase project | Local automated suite proves migration code; CK-00 prereq proves the REMOTE project has the migration applied (HIGH #13) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-20 | Information Disclosure | Test data leakage between devices | accept | Test devices use dev Supabase project; no production data. |
| T-04-21 | Tampering | UAT walkthrough records inaccurate PASS | mitigate (process) | Each PASS requires 1-2 line evidence (latency, screenshot reference). |
| T-04-22 | Denial of Service | LB-02 hard gate passes locally but fails on devices due to missing remote publication | mitigate (HIGH #13 RESOLVED via REVIEWS replan 2026-05-08) | CK-00 prerequisite check verifies the remote project's `pg_publication_tables` BEFORE the 2-device UAT begins. If the remote project is missing entries, the prereq fails and surfaces as a checkpoint — the UAT does not proceed until the remote project is migrated. |
</threat_model>

<verification>
- Automated suite (pnpm typecheck, pnpm test --ci, supabase test db, pnpm expo-doctor) all green or with documented deferrals
- CK-00 remote-publication prerequisite PASS (HIGH #13 — both `group_members` and `submissions` in remote `supabase_realtime` publication)
- All 14 in-UAT CK rows recorded with PASS/FAIL/DEFER
- All 6 in-UAT hard gates (CK-02, CK-03, CK-04, CK-05, CK-07, CK-13) + CK-00 prereq marked PASS
- 8 P4 requirements flipped to Complete in REQUIREMENTS.md and ROADMAP.md
- Phase 4 row in ROADMAP Progress table updated
- Hard-gate count consistent (6 in-UAT + 1 prereq = 7) across summary, checklist, acceptance criteria
</verification>

<success_criteria>
- 04-VERIFICATION.md is the authoritative phase-close report
- 8 P4 requirements (PTS-01..03, LB-01..02, FEED-01..03) marked Complete
- ROADMAP.md Phase 4 status flipped to Complete with date
- STATE.md ready for orchestrator update (Phase 4 → Complete; Phase 5 unblocks)
- HIGH #13 remote-publication check is now part of the closeout protocol
</success_criteria>

<output>
After completion, create `.planning/phases/04-social-surfaces/04-07-SUMMARY.md` capturing:
- Final automated suite counts (Jest test count, pgTAP assertion count, typecheck status, expo-doctor result)
- CK-00 remote-publication prereq output snippet (HIGH #13 — proof the remote project is migrated)
- 14-CK UAT outcomes (PASS / FAIL / DEFER counts; 6 in-UAT hard gates + 1 prereq named)
- Inline-fix audit trail (commits made during UAT, if any)
- Deferrals to P5 / P6
- Pointer to 04-VERIFICATION.md as the authoritative report
- HIGH #4 D-09 stack order visual verification reference (screenshot path or note)
</output>
