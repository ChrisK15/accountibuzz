---
phase: 04-social-surfaces
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - supabase/migrations/0008_phase4_points_streaks_feed.sql
  - src/types/database.ts
autonomous: true
requirements: [PTS-01, PTS-02, PTS-03, LB-01, LB-02, FEED-01, FEED-02, FEED-03]
must_haves:
  truths:
    - "D-01: handle_submission_approval body uses NEW.local_date (server-derived, never client clock)"
    - "D-02: streak recurrence implements 4 branches (NULL/consecutive/gap/same-day) via single CASE expression"
    - "D-03: idempotency holds — re-fire is blocked by AFTER UPDATE WHEN clause + 0003 admin-immutable trigger"
    - "D-04: leaderboard read sorts via group_members_leaderboard_idx (no client-side sort cost)"
    - "D-17: 4 SECURITY DEFINER RPCs ship (get_pending_today, get_missed_yesterday, get_today_posted_count, get_group_leaderboard)"
    - "D-18: trigger body replaces the 0001 stub via create or replace function (envelope unchanged: language plpgsql, security definer, set search_path = public)"
    - "D-19: column-allowlist trigger on group_members blocks client-side counter mutations using pg_trigger_depth() > 1 to bypass for the definer-trigger path"
    - "CGF-1: alter publication supabase_realtime add table public.group_members lands at the TOP of 0008 (idempotent block, mirrors 0007)"
    - "Migration is applied via supabase db push BEFORE pnpm types:gen runs"
    - "src/types/database.ts is regenerated and committed AFTER push so frontend hooks (04-03) compile"
    - "supabase test db (full pgTAP) is green AFTER push"
    - "pnpm typecheck is green AFTER types:gen"
  artifacts:
    - path: supabase/migrations/0008_phase4_points_streaks_feed.sql
      provides: "Realtime publication add + trigger body + column-allowlist trigger + 4 RPCs + grants"
      contains: "alter publication supabase_realtime add table public.group_members"
    - path: src/types/database.ts
      provides: "Regenerated TypeScript types covering get_pending_today / get_missed_yesterday / get_today_posted_count / get_group_leaderboard RPC signatures"
      contains: "get_group_leaderboard"
  key_links:
    - from: supabase/migrations/0008_phase4_points_streaks_feed.sql
      to: pg_publication_tables
      via: "idempotent do $$ ... if not exists (...) then alter publication ... end $$"
      pattern: "alter publication supabase_realtime add table public.group_members"
    - from: handle_submission_approval body
      to: group_members counter columns
      via: "single UPDATE with v_new_streak variable"
      pattern: "update public.group_members\\s+set points\\s*=\\s*points\\s*\\+\\s*1"
    - from: group_members_counter_immutable trigger
      to: handle_submission_approval (definer call)
      via: "pg_trigger_depth() > 1 bypass"
      pattern: "pg_trigger_depth\\(\\)\\s*>\\s*1"
---

<objective>
Ship the Phase 4 server contract in a single migration: `0008_phase4_points_streaks_feed.sql`. The migration replaces the no-op `handle_submission_approval` stub from 0001 §10 with the real streak-math body (D-01..D-04, D-18), adds a defense-in-depth column-allowlist trigger on `group_members` (D-19), adds 4 SECURITY DEFINER RPCs (D-17 + D-04), and **places the `alter publication supabase_realtime add table public.group_members` block as step 1** (CGF-1 — without it, LB-02 + Today social-signal Realtime channels subscribe successfully but receive zero events). After the migration is applied via the [BLOCKING] `supabase db push` task, regenerate TypeScript types so 04-03 hooks compile.

Purpose: Phase 4 is server-mostly with a thin UI layer. This plan is the heart — every downstream hook (04-03), component (04-04), and screen integration (04-05/04-06) reads from the contract this migration creates. Without the publication add (CGF-1), pgTAP passes but production Realtime fails silently — exactly the failure mode P3 hit in pre-UAT before 0007 fixed it for `submissions`.

Output: 1 migration file + regenerated types file. The 4 pgTAP files from 04-01 flip from RED to GREEN after `supabase db push` completes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-social-surfaces/04-CONTEXT.md
@.planning/phases/04-social-surfaces/04-RESEARCH.md
@.planning/phases/04-social-surfaces/04-PATTERNS.md
@.planning/phases/04-social-surfaces/04-VALIDATION.md
@supabase/migrations/0001_foundation.sql
@supabase/migrations/0003_phase1_review_fixes_2.sql
@supabase/migrations/0006_phase3_capture_review.sql
@supabase/migrations/0007_phase3_realtime_publication.sql

<interfaces>
<!-- The handle_submission_approval STUB being replaced (0001 lines 365-384). -->
<!-- The trigger wiring is unchanged; only the function BODY changes. -->

From `supabase/migrations/0001_foundation.sql` lines 365-384:
```sql
create or replace function public.handle_submission_approval()
returns trigger
language plpgsql
security definer
set search_path = public
as $$
begin
  -- STUB: intentionally a no-op in P1. See plan 04 for the body.
  return new;
end;
$$;

drop trigger if exists on_submission_approved on public.submissions;
create trigger on_submission_approved
  after update of status on public.submissions
  for each row
  when (old.status is distinct from new.status and new.status = 'approved')
  execute function public.handle_submission_approval();
```

From `supabase/migrations/0001_foundation.sql` lines 102-118 (group_members table — schema reality):
```sql
-- Column names: points (int), current_streak (int), longest_streak (int), last_rolled_date (date)
-- Index: group_members_leaderboard_idx (group_id, points desc, current_streak desc)
-- (NOTE: arch doc says 'last_verified_local_date'; SCHEMA WINS — use 'last_rolled_date'.)
```

From `supabase/migrations/0001_foundation.sql` lines 199-227 (helper functions):
```sql
public.is_group_member(g uuid) returns boolean language sql security definer stable
public.is_group_admin(g uuid) returns boolean language sql security definer stable
```

From `supabase/migrations/0007_phase3_realtime_publication.sql` lines 14-24 (publication-add idempotent pattern — verbatim source for CGF-1 fix):
```sql
do $$
begin
  if not exists (
    select 1 from pg_publication_tables
    where pubname = 'supabase_realtime'
      and schemaname = 'public'
      and tablename  = 'submissions'
  ) then
    alter publication supabase_realtime add table public.submissions;
  end if;
end $$;
```

From `supabase/migrations/0006_phase3_capture_review.sql` (RPC permission boilerplate — applied to every new RPC):
```sql
revoke execute on function public.<rpc_name>(<args>) from public;
grant  execute on function public.<rpc_name>(<args>) to authenticated;
```

From `supabase/migrations/0003_phase1_review_fixes_2.sql` lines 23-93 (BEFORE UPDATE column-allowlist + drop+create trigger pattern):
```sql
-- The auth.uid() IS NULL early-return is the definer-bypass; for group_members
-- we use pg_trigger_depth() > 1 instead — see 04-RESEARCH.md §Appendix §3.
create or replace function public.submissions_owner_immutable()
returns trigger language plpgsql security definer set search_path = public ...
drop trigger if exists submissions_owner_immutable_trigger on public.submissions;
create trigger submissions_owner_immutable_trigger
  before update on public.submissions for each row execute function ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write migration 0008_phase4_points_streaks_feed.sql</name>
  <files>supabase/migrations/0008_phase4_points_streaks_feed.sql</files>
  <behavior>
    - 4 RED pgTAP files from 04-01 will all flip GREEN after this migration is applied via supabase db push (Task 2)
    - Realtime publication includes group_members (CGF-1 fix)
    - handle_submission_approval body implements D-01..D-04 streak math
    - group_members_counter_immutable trigger blocks client UPDATEs of counter columns but allows the definer-trigger path (pg_trigger_depth() > 1)
    - 4 SECURITY DEFINER RPCs follow the typed-error / lenient pattern from 0006
  </behavior>
  <read_first>
    - 04-RESEARCH.md §"Code Examples > Migration shape (0008 skeleton)" (lines 618-755 — the verbatim section-by-section blueprint; the executor uses this as the body)
    - 04-RESEARCH.md §Appendix §1 "Trigger body, exact SQL" (lines 919-921, references the §"Code Examples" body)
    - 04-RESEARCH.md §Appendix §2 "Three new SECURITY DEFINER RPCs, exact signatures + body shape" (lines 923-940)
    - 04-RESEARCH.md §Appendix §3 "Optional column-allowlist trigger on group_members" — **CRITICAL: USE THE pg_trigger_depth() > 1 BYPASS** (lines 942-987), NOT the auth.uid() IS NULL bypass (which is wrong for this trigger's path)
    - 04-RESEARCH.md §Appendix §6 (lines 1057-1107 for the get_group_leaderboard RPC body)
    - 04-RESEARCH.md §Pattern 2 (lines 285-388 — the get_pending_today + get_missed_yesterday + get_today_posted_count bodies)
    - 04-PATTERNS.md §"Pattern Assignments — DB Layer" §(a) (lines 39-59 — verbatim publication-add idempotent block)
    - 04-PATTERNS.md §"Pattern Assignments — DB Layer" §(b) (lines 61-89 — trigger body adaptation)
    - 04-PATTERNS.md §"Pattern Assignments — DB Layer" §(c) (lines 91-128 — column-allowlist trigger adaptation)
    - 04-PATTERNS.md §"Pattern Assignments — DB Layer" §(d) (lines 130-181 — RPC pattern adaptation)
    - 04-PATTERNS.md §"Shared Patterns" (the typed-error convention, RPC permission boilerplate, membership gates)
    - 04-CONTEXT.md D-01..D-21 (locked decisions)
    - supabase/migrations/0001_foundation.sql lines 365-384 (the stub being replaced) — see <interfaces> above
    - supabase/migrations/0003_phase1_review_fixes_2.sql lines 23-93 (column-allowlist trigger pattern)
    - supabase/migrations/0006_phase3_capture_review.sql lines 78-149, 200-245, 269-327 (RPC + composite type + revoke/grant patterns)
    - supabase/migrations/0007_phase3_realtime_publication.sql lines 14-24 (publication-add idempotent block)
  </read_first>
  <action>
Create `supabase/migrations/0008_phase4_points_streaks_feed.sql`. The file follows the section-numbered structure from 04-RESEARCH.md §"Code Examples" lines 618-755. Quote that section verbatim for the section bodies — do not paraphrase. The skeleton:

**Header comment block** (top-of-file, lines 1-50ish): copy verbatim from 04-RESEARCH.md lines 619-665 (the `-- =================` block + the comment listing every new function + replaces + adds + realtime publication + pgTAP coverage). Update the "Locked decisions" reference path to `.planning/phases/04-social-surfaces/04-CONTEXT.md`.

**Section 0 — Realtime publication add (CGF-1, MANDATORY FIRST):** copy the idempotent block from 04-RESEARCH.md lines 668-678 verbatim. The `tablename = 'group_members'` is the only differentiator from 0007. Mirror the comment "0. Realtime publication (CGF-1 — mandatory; parallels 0007)."

**Section 1 — handle_submission_approval body (D-01..D-04, D-18):** copy verbatim from 04-RESEARCH.md lines 681-713 (the `create or replace function ... declare v_new_streak int ... select case ... when last_rolled_date is null then 1 ... else 1 ... update public.group_members set points = points + 1, current_streak = v_new_streak, longest_streak = greatest(longest_streak, v_new_streak), last_rolled_date = new.local_date ...`). Use the variable-then-update form per RESEARCH §Subtlety #2 (testability). Use `last_rolled_date + 1` integer-day arithmetic (NOT `+ interval '1 day'` — RESEARCH §Subtlety #1).

Do NOT recreate the `on_submission_approved` trigger — the 0001 wiring is intact and the WHEN clause + AFTER UPDATE OF status semantics carry forward unchanged. Only the function body is replaced.

**Section 2 — group_members_counter_immutable trigger (D-19):** copy from 04-RESEARCH.md §Appendix §3 lines 953-987 verbatim — **use the `pg_trigger_depth() > 1` bypass**, NOT the `auth.uid() is null` bypass. The §"Code Examples" item 2 in lines 715-746 has the WRONG bypass (RESEARCH calls it out as a subtle bug); use the corrected Appendix §3 version.

The body checks `pg_trigger_depth() > 1` first (return new; bypass for the AFTER UPDATE definer call from `handle_submission_approval`). Otherwise: refuse if any of `points / current_streak / longest_streak / last_rolled_date` is distinct from old. Error message: `'group_members counter columns are server-managed'` (no errcode — this is a defense-in-depth raise, not a typed user error).

End the section with the trigger drop+create pair (lines 982-985 of Appendix §3): `drop trigger if exists group_members_counter_immutable_trigger on public.group_members; create trigger group_members_counter_immutable_trigger before update on public.group_members for each row execute function public.group_members_counter_immutable();`

**Section 3 — get_pending_today RPC (D-07, FEED-02):** copy verbatim from 04-RESEARCH.md §Pattern 2 lines 290-345. Returns `table (user_id uuid, display_name text, avatar_path text, updated_at timestamptz)`. Typed errors `not_authenticated`, `not_member`, `group_not_found` via `errcode = 'P0001'`. Inline `v_today := (now() AT TIME ZONE v_group_tz)::date`. End with `revoke execute on function public.get_pending_today(uuid) from public; grant execute on function public.get_pending_today(uuid) to authenticated;`.

**Section 4 — get_missed_yesterday RPC (D-05, FEED-03):** identical shape to Section 3 EXCEPT:
- `v_target_date := (now() AT TIME ZONE v_group_tz)::date - 1` (use `v_target_date` not `v_today` for clarity per RESEARCH §Pattern 2 note)
- The NOT-EXISTS subquery filters `s.status = 'approved' AND s.local_date = v_target_date` (rejected/pending count as misses per CONTEXT D-05)
- Same typed errors + revoke/grant

**Section 5 — get_today_posted_count RPC (D-13):** copy verbatim from 04-RESEARCH.md §Pattern 2 lines 352-383 (the lenient variant). Returns `int`. Soft-fails: `if caller is null then return 0; end if; if not is_group_member(p_group_id) then return 0; end if; if not found then return 0; end if;`. Counts approved submissions for today's local_date. End with revoke/grant.

**Section 6 — get_group_leaderboard RPC (D-04 + RESEARCH §6 recommendation):** copy verbatim from 04-RESEARCH.md §Appendix §6 lines 1059-1104. Returns `table (user_id uuid, display_name text, avatar_path text, updated_at timestamptz, points int, current_streak int, longest_streak int, last_rolled_date date, joined_at timestamptz)`. Typed errors `not_authenticated`, `not_member`. ORDER BY `gm.points desc, gm.current_streak desc` (uses `group_members_leaderboard_idx`). End with revoke/grant.

**Final newline + EOF.** No `commit` (Supabase CLI wraps in a transaction).

**Filename convention:** the file MUST be named exactly `0008_phase4_points_streaks_feed.sql` (lowercase, underscore-separated, sequential numeric prefix matching the migration sequence). Do not use any other format.
  </action>
  <verify>
    <automated>test -f supabase/migrations/0008_phase4_points_streaks_feed.sql && grep -c "alter publication supabase_realtime add table public.group_members" supabase/migrations/0008_phase4_points_streaks_feed.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/0008_phase4_points_streaks_feed.sql` exists
    - `grep -c "alter publication supabase_realtime add table public.group_members" supabase/migrations/0008_phase4_points_streaks_feed.sql` returns 1 (CGF-1 fix lands)
    - The publication-add block appears BEFORE the trigger body — verify via `grep -n "alter publication\|create or replace function public.handle_submission_approval" supabase/migrations/0008_phase4_points_streaks_feed.sql | head -2` shows the publication line FIRST
    - `grep -c "create or replace function public.handle_submission_approval" supabase/migrations/0008_phase4_points_streaks_feed.sql` returns 1
    - `grep -c "v_new_streak int" supabase/migrations/0008_phase4_points_streaks_feed.sql` returns at least 1
    - `grep -c "last_rolled_date + 1" supabase/migrations/0008_phase4_points_streaks_feed.sql` returns at least 1 (date arithmetic per RESEARCH §Subtlety #1)
    - `grep -c "pg_trigger_depth()" supabase/migrations/0008_phase4_points_streaks_feed.sql` returns at least 1 (the column-allowlist bypass — Appendix §3 corrected pattern)
    - `grep -c "create or replace function public.group_members_counter_immutable" supabase/migrations/0008_phase4_points_streaks_feed.sql` returns 1
    - `grep -c "create or replace function public.get_pending_today" supabase/migrations/0008_phase4_points_streaks_feed.sql` returns 1
    - `grep -c "create or replace function public.get_missed_yesterday" supabase/migrations/0008_phase4_points_streaks_feed.sql` returns 1
    - `grep -c "create or replace function public.get_today_posted_count" supabase/migrations/0008_phase4_points_streaks_feed.sql` returns 1
    - `grep -c "create or replace function public.get_group_leaderboard" supabase/migrations/0008_phase4_points_streaks_feed.sql` returns 1
    - `grep -c "revoke execute on function" supabase/migrations/0008_phase4_points_streaks_feed.sql` is at least 4 (one per RPC)
    - `grep -c "grant  execute on function\|grant execute on function" supabase/migrations/0008_phase4_points_streaks_feed.sql` is at least 4
    - `grep -c "errcode = 'P0001'" supabase/migrations/0008_phase4_points_streaks_feed.sql` is at least 7 (3 in get_pending_today, 3 in get_missed_yesterday, 2 in get_group_leaderboard — minimum 8; ≥ 7 leaves margin)
    - `grep -cv "^[[:space:]]*--\\|^[[:space:]]*$" supabase/migrations/0008_phase4_points_streaks_feed.sql` returns at least 100 (sanity-check non-comment line count)
  </acceptance_criteria>
  <done>0008 migration file created with all 6 sections (publication, trigger body, counter-immutable trigger, 4 RPCs); publication-add appears as section 0; pg_trigger_depth() bypass used.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Apply migration 0008 to local Supabase via supabase db push</name>
  <files>(no file modifications — this is the database-push step)</files>
  <read_first>
    - supabase/migrations/0008_phase4_points_streaks_feed.sql (just created in Task 1)
    - .planning/research/PITFALLS.md §"P4-G: Type regeneration timing breaks frontend compile" (the rationale for the strict ordering: push BEFORE types:gen BEFORE frontend hooks)
  </read_first>
  <action>
**This task is MANDATORY** — the phase CANNOT pass verification without it. Build and type checks will pass without the push (types come from config, not the live database), creating a false-positive verification state.

Run `supabase db push` (non-interactive on this dev machine — `SUPABASE_ACCESS_TOKEN` env var configured + project-ref set, per the gsd-init guidance).

If supabase CLI prompts for confirmation, the executor may pass `--include-all` if the prompt is non-blocking, OR if a TTY prompt actually blocks, escalate as a checkpoint and ask the user to run `supabase db push` from their terminal and paste back the output.

After `supabase db push` succeeds, run `supabase test db` (full pgTAP suite) to verify:
- The 4 NEW Phase 4 pgTAP files (handle_submission_approval_streak, handle_submission_approval_idempotency, phase4_rpc_permissions, phase4_rpc_correctness) flip from RED to GREEN
- All existing pgTAP files (107 prior tests) remain green
- Total assertion count: ~107 (P3) + ~29 (P4 new) = ~136

If any new pgTAP file fails, the migration body has a bug — read the failure output, correlate to the assertion in the relevant test file, fix the SQL in 0008 (re-issue `supabase db reset --local` then `supabase db push` if a re-do is needed; or stop and surface the failure as a checkpoint to the user).
  </action>
  <verify>
    <automated>supabase test db 2>&1 | tee /tmp/p4-pgtap.log | tail -5 && grep -c "^# ok\|^ok " /tmp/p4-pgtap.log</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` exits 0 (or, if interactive prompt blocked execution, the user has run it manually and confirmed migration was applied)
    - `supabase test db` exits 0
    - `supabase test db 2>&1 | grep -E "^(ok|not ok)" | wc -l` returns ≥ 130 assertions (P3's ~107 + P4's ~29)
    - `supabase test db 2>&1 | grep -E "^not ok" | wc -l` returns 0
    - Verify publication: `psql -c "select tablename from pg_publication_tables where pubname='supabase_realtime' and schemaname='public' order by tablename" | grep group_members` returns 1
  </acceptance_criteria>
  <done>Migration applied; full pgTAP suite green; group_members in supabase_realtime publication.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate src/types/database.ts and commit</name>
  <files>src/types/database.ts</files>
  <read_first>
    - package.json (the `types:gen` script — `supabase gen types typescript --local > src/types/database.ts`)
    - 04-RESEARCH.md §"Pitfall P4-G: Type regeneration timing breaks frontend compile" (lines 605-610 — the strict ordering rationale)
    - supabase/migrations/0008_phase4_points_streaks_feed.sql (the migration just applied)
  </read_first>
  <action>
Run `pnpm types:gen` (which runs `supabase gen types typescript --local > src/types/database.ts`).

The regenerated `src/types/database.ts` MUST include entries under `Database.public.Functions` for the 4 new RPCs:
- `get_pending_today`
- `get_missed_yesterday`
- `get_today_posted_count`
- `get_group_leaderboard`

After generation, run `pnpm typecheck` to verify the regenerated file is valid TypeScript and integrates cleanly with the existing source tree (no other files touch the `Functions` type yet — 04-03 is where the new RPC names get consumed).

If `pnpm types:gen` fails (e.g. local Supabase not running, or the migration didn't apply), do not silently fall back — surface the error.

After both commands succeed, the file is committed by the gsd commit step at the end of the plan.
  </action>
  <verify>
    <automated>pnpm types:gen && grep -c "get_pending_today\|get_missed_yesterday\|get_today_posted_count\|get_group_leaderboard" src/types/database.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm types:gen` exits 0
    - `grep -c "get_pending_today" src/types/database.ts` returns at least 1
    - `grep -c "get_missed_yesterday" src/types/database.ts` returns at least 1
    - `grep -c "get_today_posted_count" src/types/database.ts` returns at least 1
    - `grep -c "get_group_leaderboard" src/types/database.ts` returns at least 1
    - `pnpm typecheck` exits 0 (no regressions in existing source tree)
  </acceptance_criteria>
  <done>Types regenerated; 4 new RPC signatures present in database.ts; typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon → PostgREST RPC | Unauthenticated callers attempting to invoke any of the 4 new RPCs |
| authenticated non-member → PostgREST RPC | Authenticated user not in the target group querying group state |
| authenticated admin → REST UPDATE on group_members | Malicious admin attempting to set their own points = 9999 |
| Realtime CDC → Realtime client | Group state events flowing to subscribed clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | get_pending_today / get_missed_yesterday / get_group_leaderboard | mitigate | `is_group_member(p_group_id)` raise inside RPC body — non-members get `not_member` (typed P0001), NOT empty list (existence-vs-non-existence leak). Verified by `phase4_rpc_permissions.sql`. |
| T-04-02 | Tampering | group_members counter columns | mitigate | BEFORE UPDATE column-allowlist trigger `group_members_counter_immutable` (D-19) refuses any client UPDATE that changes points/current_streak/longest_streak/last_rolled_date. Bypass for definer-trigger path uses `pg_trigger_depth() > 1` (Appendix §3) — distinguishes the AFTER UPDATE definer call from a direct REST API UPDATE. Verified by `phase4_rpc_permissions.sql` + the streak/idempotency pgTAP files. |
| T-04-03 | Repudiation / Tampering | submissions status flip-back to re-fire trigger | mitigate | 0003's `submissions_owner_immutable` admin-immutable trigger forbids `approved → pending` status flips. Combined with 0001's WHEN clause `(old.status IS DISTINCT FROM new.status AND new.status = 'approved')` — trigger fires at most once per submission row. Verified by `handle_submission_approval_idempotency.sql`. |
| T-04-04 | Information Disclosure | Realtime CDC events on group_members | mitigate | Realtime checks RLS on the row before publishing. The user's session JWT must satisfy `group_members_select_own_or_same_group` (existing P3 policy). Single-column filter `group_id=eq.{X}` is a server-side constraint; client narrowing in the handler is a fail-safe. |
| T-04-05 | Denial of Service | Streak race on concurrent approvals | accept | Postgres MVCC + READ COMMITTED handles concurrent UPDATE on the same `group_members` row correctly (each transaction takes a row lock; the second re-reads after the first commits). Verified by Pitfall P4-B coverage in `handle_submission_approval_idempotency.sql`. |
| T-04-06 | Elevation of Privilege | SECURITY DEFINER RPC search_path injection | mitigate | Every new function declares `set search_path = public` per the established 0001/0006 pattern. `revoke execute on function ... from public; grant execute on function ... to authenticated;` lock the function surface to authenticated callers. |
</threat_model>

<verification>
- All 4 P4 pgTAP files green via `supabase test db` (post-push)
- All existing pgTAP files (107 from P1/P2/P3) remain green (no regressions)
- `psql ... select tablename from pg_publication_tables ...` returns `group_members` in the result set
- `pnpm typecheck` green after `pnpm types:gen`
- `src/types/database.ts` contains entries for all 4 new RPCs
- No new public-schema tables added (CI rls-check workflow stays green incidentally)
</verification>

<success_criteria>
- `supabase/migrations/0008_phase4_points_streaks_feed.sql` exists and is applied to local DB
- `group_members` is in the `supabase_realtime` publication (CGF-1 fix verified)
- 4 SECURITY DEFINER RPCs exist with revoke/grant pairs (`get_pending_today`, `get_missed_yesterday`, `get_today_posted_count`, `get_group_leaderboard`)
- `handle_submission_approval` body computes streak math per D-01..D-04 (NULL → 1, consecutive → +1, gap → 1, longest_streak preserved)
- `group_members_counter_immutable` trigger blocks client counter mutations and bypasses for the definer-trigger path via `pg_trigger_depth() > 1`
- All 29 P4 pgTAP assertions green
- `src/types/database.ts` regenerated with 4 new RPC entries; `pnpm typecheck` green
- 04-03 hooks (Wave 2) can now compile against the regenerated types
</success_criteria>

<output>
After completion, create `.planning/phases/04-social-surfaces/04-02-SUMMARY.md` capturing:
- Final pgTAP assertion count (P3 baseline + P4 new = total)
- Confirmation that `group_members` is in the publication (with the verifying SQL output snippet)
- The 4 new RPC signatures (verbatim from regenerated `src/types/database.ts`) for downstream plans to reference
- Any deviations from the planned migration body (e.g. if a subtle bug surfaced during pgTAP and required an inline fix)
</output>
