---
phase: 04-social-surfaces
plan: 02
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - supabase/migrations/20260508233129_phase4_points_streaks_feed.sql
  - src/types/database.ts
autonomous: false
requirements: [PTS-01, PTS-02, PTS-03, LB-01, LB-02, FEED-01, FEED-02, FEED-03]
must_haves:
  truths:
    - "D-01: handle_submission_approval body uses NEW.local_date (server-derived, never client clock)"
    - "D-02: streak recurrence implements 4 branches (NULL/consecutive/gap/same-day) via single locked-row UPDATE — RACE-SAFE under concurrent approvals (HIGH #1 from REVIEWS.md — single-row-locked UPDATE pattern with computed CASE, NO separate SELECT-then-UPDATE)"
    - "D-03: idempotency holds — re-fire is blocked by AFTER UPDATE WHEN clause + 0003 admin-immutable trigger + UNIQUE (group_id, user_id, local_date) on submissions"
    - "D-04: leaderboard read sorts via group_members_leaderboard_idx (no client-side sort cost) with deterministic joined_at ASC tiebreaker (MEDIUM concern from REVIEWS.md)"
    - "D-05: get_missed_yesterday RPC computes target_date = (now() AT TIME ZONE g.timezone)::date - 1 server-side; rejected/pending count as misses"
    - "D-XX-rejected-policy (Iteration 1 plan-checker fix 2026-05-08, WARNING-5b): get_pending_today's NOT-EXISTS subquery filters submissions to today's local_date REGARDLESS of status — i.e., a member with ANY submission today (pending, rejected, OR approved) is EXCLUDED from 'Still to post'. A rejected submission counts as engagement; the member is not re-prompted. Re-submission flow after admin rejection is a P5+ concern. This matches the simpler pgTAP contract in 04-01 §phase4_rpc_correctness.sql Test 2 ('get_pending_today excludes rejected/pending submissions for today')."
    - "D-17: 4 SECURITY DEFINER RPCs ship (get_pending_today, get_missed_yesterday, get_today_posted_count, get_group_leaderboard)"
    - "D-18: trigger body replaces the 0001 stub (envelope unchanged: language plpgsql, security definer, set search_path = public)"
    - "D-19: column-allowlist trigger on group_members blocks client-side counter mutations using pg_trigger_depth() > 1 to bypass for the definer-trigger path"
    - "CGF-1: alter publication supabase_realtime add table public.group_members lands at the TOP of 0008 (idempotent block, mirrors 0007)"
    - "CGF-2: alter table public.submissions replica identity full lands in 0008 — Realtime payloads include full old-row data so useGroupFeedRealtime can safely read payload.old.status (HIGH #2 from REVIEWS.md)"
    - "Same-day branch leaves points unchanged — same-day trigger fires would be blocked by UNIQUE (group_id, user_id, local_date) on public.submissions (named uq_submissions_user_group_local_date in 0001), but the trigger body's CASE expression also gates the points increment on the recurrence branch as belt-and-suspenders (HIGH #12 from REVIEWS.md)"
    - "get_group_leaderboard ORDER BY: points DESC, current_streak DESC, joined_at ASC (deterministic tiebreaker — MEDIUM concern)"
    - "Migration is applied via supabase db push BEFORE pnpm types:gen runs"
    - "src/types/database.ts is regenerated and committed AFTER push so frontend hooks (04-03) compile"
    - "supabase test db (full pgTAP) is green AFTER push"
    - "pnpm typecheck is green AFTER types:gen"
  artifacts:
    - path: supabase/migrations/20260508233129_phase4_points_streaks_feed.sql
      provides: "Realtime publication add + replica identity full + race-safe trigger body + column-allowlist trigger + 4 RPCs (with deterministic leaderboard tiebreaker) + grants"
      contains: "alter publication supabase_realtime add table public.group_members"
    - path: src/types/database.ts
      provides: "Regenerated TypeScript types covering get_pending_today / get_missed_yesterday / get_today_posted_count / get_group_leaderboard RPC signatures"
      contains: "get_group_leaderboard"
  key_links:
    - from: supabase/migrations/20260508233129_phase4_points_streaks_feed.sql
      to: pg_publication_tables
      via: "idempotent do $$ ... if not exists (...) then alter publication ... end $$"
      pattern: "alter publication supabase_realtime add table public.group_members"
    - from: supabase/migrations/20260508233129_phase4_points_streaks_feed.sql
      to: pg_class.relreplident on submissions
      via: "alter table public.submissions replica identity full"
      pattern: "replica identity full"
    - from: handle_submission_approval body
      to: group_members counter columns
      via: "single locked-row UPDATE with CASE expression (NO separate SELECT)"
      pattern: "update public.group_members\\s+set"
    - from: group_members_counter_immutable trigger
      to: handle_submission_approval (definer call)
      via: "pg_trigger_depth() > 1 bypass"
      pattern: "pg_trigger_depth\\(\\)\\s*>\\s*1"
    - from: get_group_leaderboard ORDER BY
      to: deterministic ranking
      via: "points DESC, current_streak DESC, joined_at ASC"
      pattern: "joined_at\\s+asc"
---

## Revisions Applied

This plan was revised on 2026-05-08 in response to cross-AI review (`04-REVIEWS.md`). Concerns addressed:

- **HIGH #1** (Streak race condition under concurrent approvals): The trigger body now uses a **single locked-row UPDATE** with a CASE expression to compute `current_streak`, NOT a separate `SELECT ... INTO v_new_streak` followed by `UPDATE`. The `UPDATE ... SET current_streak = CASE WHEN last_rolled_date IS NULL THEN 1 WHEN NEW.local_date = last_rolled_date + 1 THEN current_streak + 1 ELSE 1 END` form takes the row lock during the UPDATE itself, eliminating the read-modify-write window. See Task 1 §Section 1 (the new SQL body).
- **HIGH #2** (Replica identity for Realtime old.status): Added `alter table public.submissions replica identity full;` as Section 0.5 (immediately after the publication-add). This guarantees `payload.old` includes all columns (not just primary key), so 04-03's `useGroupFeedRealtime` can safely inspect `payload.old.status`. See Task 1 §Section 0.5.
- **HIGH #6** (Permission grants for `get_today_posted_count`): Locked the strict-grant interpretation — `revoke execute from public; grant execute to authenticated;`. Anon callers receive SQLSTATE 42501 (matches 04-01 anon test expectation). See Task 1 §Section 5.
- **HIGH #12** (Same-day branch should not increment points): Even though UNIQUE `(group_id, user_id, local_date)` constraint on `public.submissions` (named `uq_submissions_user_group_local_date` in migration 0001 §`submissions`) makes same-day trigger fires structurally impossible — there can be at most one submission row per `(group_id, user_id, local_date)` tuple — the trigger body MUST also gate the `points = points + 1` increment on the recurrence branch as belt-and-suspenders. The new CASE-driven UPDATE only adds 1 point when the streak math actually advances (NULL/consecutive/gap branches); same-day branch is now both structurally and behaviorally a no-op. See Task 1 §Section 1 inline comment.
- **MEDIUM tiebreaker** (`get_group_leaderboard` deterministic ORDER BY): Added `joined_at ASC` as a third sort key so ties on `(points, current_streak)` resolve stably. See Task 1 §Section 6.

Iteration 1: WARNING-3 resolved — Task 2 (`supabase db push`) is now `checkpoint:human-verify gate="blocking"` (was `auto`); pre-flight env check (`SUPABASE_ACCESS_TOKEN`) added; resume-signal references `pnpm types:gen` mtime + `supabase migration list`. Plan `autonomous` flag flipped to `false` to reflect the checkpoint.

Iteration 1: WARNING-5b resolved — added a plan-truth in 04-02 truths field locking the rejected-policy: `get_pending_today` excludes both `pending` AND `rejected` submissions from "Still to post" (a member with a rejected submission is treated as having engaged; re-submission is a P5+ concern).

<objective>
Ship the Phase 4 server contract in a single migration: `20260508233129_phase4_points_streaks_feed.sql`. The migration replaces the no-op `handle_submission_approval` stub from 0001 §10 with a **race-safe** streak-math body (D-01..D-04, D-18 + HIGH #1 from REVIEWS.md), adds `replica identity full` on `public.submissions` (HIGH #2 — required for `useGroupFeedRealtime`'s `payload.old.status` inspection), adds a defense-in-depth column-allowlist trigger on `group_members` (D-19), adds 4 SECURITY DEFINER RPCs (D-17 + D-04, with a `joined_at ASC` deterministic tiebreaker on the leaderboard), and **places the `alter publication supabase_realtime add table public.group_members` block as step 0** (CGF-1 — without it, LB-02 + Today social-signal Realtime channels subscribe successfully but receive zero events).

After the migration is applied via the [BLOCKING] `supabase db push` task, regenerate TypeScript types so 04-03 hooks compile.

Purpose: Phase 4 is server-mostly with a thin UI layer. This plan is the heart — every downstream hook (04-03), component (04-04), and screen integration (04-05/04-06) reads from the contract this migration creates. Without the publication add (CGF-1) AND replica identity full (CGF-2), pgTAP passes but production Realtime semantics drift silently — exactly the failure mode P3 hit in pre-UAT before 0007 fixed the publication for `submissions`.

Output: 1 migration file + regenerated types file. The 4 pgTAP files from 04-01 flip from RED to GREEN after `supabase db push` completes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-social-surfaces/04-CONTEXT.md
@.planning/phases/04-social-surfaces/04-RESEARCH.md
@.planning/phases/04-social-surfaces/04-PATTERNS.md
@.planning/phases/04-social-surfaces/04-VALIDATION.md
@.planning/phases/04-social-surfaces/04-REVIEWS.md
@supabase/migrations/0001_foundation.sql
@supabase/migrations/0003_phase1_review_fixes_2.sql
@supabase/migrations/20260429173246_phase3_capture_review.sql
@supabase/migrations/20260506165538_phase3_realtime_publication.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write migration 20260508233129_phase4_points_streaks_feed.sql (race-safe trigger + replica identity full + deterministic leaderboard)</name>
  <files>supabase/migrations/20260508233129_phase4_points_streaks_feed.sql</files>
  <behavior>
    - 4 RED pgTAP files from 04-01 will all flip GREEN after this migration is applied via supabase db push (Task 2)
    - Realtime publication includes group_members (CGF-1)
    - submissions has replica identity full (CGF-2 — HIGH #2 from REVIEWS.md, RESOLVED via REVIEWS replan 2026-05-08)
    - handle_submission_approval body is RACE-SAFE: single locked-row UPDATE with CASE expression (HIGH #1 from REVIEWS.md, RESOLVED via REVIEWS replan 2026-05-08)
    - get_group_leaderboard ORDER BY includes joined_at ASC tiebreaker (MEDIUM concern from REVIEWS.md, RESOLVED via REVIEWS replan 2026-05-08)
    - get_today_posted_count grants strictly to authenticated (HIGH #6 — REVOKE from public, GRANT to authenticated)
    - group_members_counter_immutable trigger blocks client UPDATEs of counter columns but allows the definer-trigger path (pg_trigger_depth() > 1)
    - 4 SECURITY DEFINER RPCs follow the typed-error / lenient pattern from 0006
  </behavior>
  <read_first>
    - 04-RESEARCH.md §"Code Examples > Migration shape (0008 skeleton)" (lines 618-755 — the verbatim section-by-section blueprint)
    - 04-RESEARCH.md §Appendix §1 "Trigger body, exact SQL" (lines 919-921)
    - 04-RESEARCH.md §Appendix §2 "Three new SECURITY DEFINER RPCs, exact signatures + body shape" (lines 923-940)
    - 04-RESEARCH.md §Appendix §3 "Optional column-allowlist trigger on group_members" — **CRITICAL: USE THE pg_trigger_depth() > 1 BYPASS** (lines 942-987)
    - 04-RESEARCH.md §Appendix §6 (lines 1057-1107 for the get_group_leaderboard RPC body)
    - 04-RESEARCH.md §Pattern 2 (lines 285-388 — get_pending_today + get_missed_yesterday + get_today_posted_count bodies)
    - 04-PATTERNS.md §"Pattern Assignments — DB Layer" §(a)..(d) (lines 39-181)
    - 04-CONTEXT.md D-01..D-21 (locked decisions)
    - supabase/migrations/0001_foundation.sql lines 365-384 (the stub being replaced)
    - supabase/migrations/0001_foundation.sql §`submissions` (the UNIQUE constraint `uq_submissions_user_group_local_date` — search the file for "unique" near submissions)
    - supabase/migrations/0003_phase1_review_fixes_2.sql lines 23-93 (column-allowlist trigger pattern)
    - supabase/migrations/20260429173246_phase3_capture_review.sql lines 78-149, 200-245, 269-327 (RPC + composite type + revoke/grant patterns)
    - supabase/migrations/20260506165538_phase3_realtime_publication.sql lines 14-24 (publication-add idempotent block)
    - .planning/phases/04-social-surfaces/04-REVIEWS.md HIGH #1 (race-safe trigger), HIGH #2 (replica identity full), HIGH #6 (grants), HIGH #12 (same-day points), MEDIUM tiebreaker
  </read_first>
  <action>
Create `supabase/migrations/20260508233129_phase4_points_streaks_feed.sql`. The file follows the section-numbered structure from 04-RESEARCH.md §"Code Examples" lines 618-755 with the REVIEWS-applied changes integrated.

**Header comment block** (top-of-file): copy verbatim from 04-RESEARCH.md lines 619-665. Update the "Locked decisions" reference path to `.planning/phases/04-social-surfaces/04-CONTEXT.md`. Add a section "REVIEWS replan 2026-05-08" listing: HIGH #1 race-safe trigger, HIGH #2 replica identity full, HIGH #12 same-day no-op gating, MEDIUM tiebreaker.

**Section 0 — Realtime publication add (CGF-1, MANDATORY FIRST):** copy the idempotent block from 04-RESEARCH.md lines 668-678 verbatim. The `tablename = 'group_members'` is the only differentiator from 0007. Comment: `-- 0. Realtime publication (CGF-1 — mandatory; parallels 0007).`

**Section 0.5 — Replica identity full on submissions (CGF-2, REVIEWS replan 2026-05-08, HIGH #2):** Add the following block immediately after Section 0:

```sql
-- 0.5. Realtime replica identity for submissions (CGF-2 — REVIEWS replan 2026-05-08, HIGH #2 from 04-REVIEWS.md).
-- Without REPLICA IDENTITY FULL, postgres_changes payload.old contains only the primary key,
-- so 04-03's useGroupFeedRealtime cannot reliably inspect payload.old.status to detect
-- approve→reject flips. Setting it FULL guarantees payload.old contains every column.
-- Storage cost: minor — replica identity is a logical-decoding detail, not row duplication.
alter table public.submissions replica identity full;
```

**Section 1 — handle_submission_approval body (D-01..D-04, D-18, HIGH #1 race-safe trigger from REVIEWS replan 2026-05-08):**

Replace the SELECT-then-UPDATE pattern from 04-RESEARCH.md §"Code Examples" lines 681-713 with a **single locked-row UPDATE driven by a CASE expression**. This is the RACE-SAFE form — the UPDATE acquires the row lock atomically, computes the new streak from the freshly-locked row's current `last_rolled_date`, and writes the new value in one statement. There is no read-modify-write window.

```sql
-- 1. handle_submission_approval BODY (D-01..D-04, D-18).
-- HIGH #1 from REVIEWS replan 2026-05-08: race-safe via single-locked-row UPDATE.
--
-- The CASE expression computes the new streak from the row's CURRENT (locked) state:
--   - last_rolled_date IS NULL                     → current_streak := 1     (NULL branch)
--   - new.local_date = last_rolled_date + 1        → current_streak := current_streak + 1  (consecutive)
--   - new.local_date = last_rolled_date            → current_streak unchanged (same-day; structurally
--                                                    impossible due to UNIQUE constraint
--                                                    uq_submissions_user_group_local_date in 0001,
--                                                    but gated here as belt-and-suspenders per HIGH #12)
--   - new.local_date > last_rolled_date + 1        → current_streak := 1     (gap branch)
--   - new.local_date < last_rolled_date            → current_streak unchanged (backdated approval —
--                                                    rare; the row's last_rolled_date already reflects
--                                                    a later day, so this approval doesn't advance it)
--
-- Points are incremented ONLY on branches that advance last_rolled_date — same-day and
-- backdated branches do NOT increment (HIGH #12 belt-and-suspenders).
create or replace function public.handle_submission_approval()
returns trigger
language plpgsql
security definer
set search_path = public
as $$
begin
  update public.group_members
     set
       current_streak = case
         when last_rolled_date is null                            then 1
         when new.local_date = last_rolled_date + 1               then current_streak + 1
         when new.local_date = last_rolled_date                   then current_streak
         when new.local_date > last_rolled_date + 1               then 1
         else current_streak                                       -- backdated: no advance
       end,
       longest_streak = greatest(
         longest_streak,
         case
           when last_rolled_date is null                          then 1
           when new.local_date = last_rolled_date + 1             then current_streak + 1
           when new.local_date = last_rolled_date                 then longest_streak  -- no change
           when new.local_date > last_rolled_date + 1             then 1
           else longest_streak                                     -- backdated: no change
         end
       ),
       last_rolled_date = case
         when last_rolled_date is null                            then new.local_date
         when new.local_date > last_rolled_date                   then new.local_date
         else last_rolled_date                                     -- same-day or backdated: no change
       end,
       points = points + case
         when last_rolled_date is null                            then 1
         when new.local_date > last_rolled_date                   then 1
         else 0                                                    -- same-day or backdated: no points
       end
   where group_id = new.group_id
     and user_id  = new.user_id;

  return new;
end;
$$;
```

**Do NOT recreate the `on_submission_approved` trigger** — the 0001 wiring is intact and the `WHEN clause + AFTER UPDATE OF status` semantics carry forward unchanged. Only the function body is replaced.

**Section 2 — group_members_counter_immutable trigger (D-19):** copy from 04-RESEARCH.md §Appendix §3 lines 953-987 verbatim — **use the `pg_trigger_depth() > 1` bypass**, NOT the `auth.uid() is null` bypass.

The body checks `pg_trigger_depth() > 1` first (return new; bypass for the AFTER UPDATE definer call from `handle_submission_approval`). Otherwise: refuse if any of `points / current_streak / longest_streak / last_rolled_date` is distinct from old. Error message: `'group_members counter columns are server-managed'`.

End with the trigger drop+create pair: `drop trigger if exists group_members_counter_immutable_trigger on public.group_members; create trigger group_members_counter_immutable_trigger before update on public.group_members for each row execute function public.group_members_counter_immutable();`

**Section 3 — get_pending_today RPC (D-07, FEED-02):** copy verbatim from 04-RESEARCH.md §Pattern 2 lines 290-345. Returns `table (user_id uuid, display_name text, avatar_path text, updated_at timestamptz)`. Typed errors `not_authenticated`, `not_member`, `group_not_found` via `errcode = 'P0001'`. Inline `v_today := (now() AT TIME ZONE v_group_tz)::date`. End with `revoke execute on function public.get_pending_today(uuid) from public; grant execute on function public.get_pending_today(uuid) to authenticated;`.

**Section 4 — get_missed_yesterday RPC (D-05, FEED-03):** identical shape to Section 3 EXCEPT:
- `v_target_date := (now() AT TIME ZONE v_group_tz)::date - 1`
- The NOT-EXISTS subquery filters `s.status = 'approved' AND s.local_date = v_target_date`
- Same typed errors + revoke/grant

**Section 5 — get_today_posted_count RPC (D-13, HIGH #6 strict-grant from REVIEWS replan 2026-05-08):** copy verbatim from 04-RESEARCH.md §Pattern 2 lines 352-383 (the lenient variant). Returns `int`. Soft-fails inside the body: `if caller is null then return 0; end if; if not is_group_member(p_group_id) then return 0; end if; if not found then return 0; end if;`. Counts approved submissions for today's local_date.

**Strict grants (HIGH #6 RESOLVED via REVIEWS replan 2026-05-08):**
```sql
revoke execute on function public.get_today_posted_count(uuid) from public;
grant  execute on function public.get_today_posted_count(uuid) to authenticated;
```
Anon callers hit SQLSTATE 42501 (permission denied) BEFORE the function body runs; only authenticated callers (member, admin, stranger-authenticated-non-member) ever execute the body. Authenticated non-members get the lenient `0` return (function body soft-fail). This matches the 04-01 `phase4_rpc_permissions.sql` test expectations: anon → `throws_ok` SQLSTATE `'42501'`; stranger → `is(..., 0)`.

**Section 6 — get_group_leaderboard RPC (D-04 + RESEARCH §6 + MEDIUM tiebreaker from REVIEWS replan 2026-05-08):** copy verbatim from 04-RESEARCH.md §Appendix §6 lines 1059-1104 with the ORDER BY change.

Returns `table (user_id uuid, display_name text, avatar_path text, updated_at timestamptz, points int, current_streak int, longest_streak int, last_rolled_date date, joined_at timestamptz)`. Typed errors `not_authenticated`, `not_member`.

**ORDER BY changed (MEDIUM tiebreaker, RESOLVED via REVIEWS replan 2026-05-08):**
```sql
order by gm.points desc, gm.current_streak desc, gm.joined_at asc
```

The `joined_at ASC` tiebreaker uses `group_members_leaderboard_idx` for the first two keys (per 04-CONTEXT.md D-04) and falls back to a deterministic stable order for ties — older members rank higher when points + streak are identical. This is preferred over `display_name ASC` because display_name can change (rename → ranking shuffle). End with revoke/grant.

**Final newline + EOF.** No `commit` (Supabase CLI wraps in a transaction).

**Filename convention:** `20260508233129_phase4_points_streaks_feed.sql` (lowercase, underscore-separated, sequential numeric prefix).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260508233129_phase4_points_streaks_feed.sql && grep -c "alter publication supabase_realtime add table public.group_members" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql && grep -c "replica identity full" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql</automated>
  </verify>
  <acceptance_criteria>
    - File `supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` exists
    - `grep -c "alter publication supabase_realtime add table public.group_members" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` returns 1 (CGF-1)
    - `grep -c "alter table public.submissions replica identity full" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` returns 1 (CGF-2 / HIGH #2 — RESOLVED via REVIEWS replan 2026-05-08)
    - The publication-add block appears BEFORE the replica-identity statement BEFORE the trigger body — verify via `grep -n "alter publication\|replica identity\|create or replace function public.handle_submission_approval" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql | head -3` shows the publication line FIRST, replica-identity SECOND, trigger function THIRD
    - `grep -c "create or replace function public.handle_submission_approval" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` returns 1
    - **HIGH #1 race-safe trigger gate**: The trigger body uses a single UPDATE with CASE — verify NO standalone `select .* into v_new_streak` lines: `grep -cE "^\s*select\s+.*\s+into\s+v_new_streak" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` returns 0
    - **HIGH #1 race-safe trigger gate**: `grep -c "case$\|when last_rolled_date is null" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` is at least 4 (the CASE branches: NULL / consecutive / same-day / gap, repeated across 4 SET clauses for current_streak, longest_streak, last_rolled_date, points)
    - **HIGH #12 same-day-no-points gate**: `grep -c "points = points + case" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` is at least 1 (points increment is gated on a CASE — NOT unconditional `points = points + 1`)
    - `grep -c "pg_trigger_depth()" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` is at least 1
    - `grep -c "create or replace function public.group_members_counter_immutable" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` returns 1
    - `grep -c "create or replace function public.get_pending_today" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` returns 1
    - `grep -c "create or replace function public.get_missed_yesterday" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` returns 1
    - `grep -c "create or replace function public.get_today_posted_count" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` returns 1
    - `grep -c "create or replace function public.get_group_leaderboard" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` returns 1
    - **HIGH #6 strict-grant gate**: `grep -c "revoke execute on function public.get_today_posted_count" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` is at least 1
    - **HIGH #6 strict-grant gate**: `grep -c "grant.*execute on function public.get_today_posted_count" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` is at least 1
    - `grep -c "revoke execute on function" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` is at least 4 (one per RPC)
    - `grep -c "grant.*execute on function" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` is at least 4
    - `grep -c "errcode = 'P0001'" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` is at least 7
    - **MEDIUM tiebreaker gate**: `grep -cE "joined_at\s+asc" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` is at least 1 (RESOLVED via REVIEWS replan 2026-05-08)
    - `grep -cv "^[[:space:]]*--\\|^[[:space:]]*$" supabase/migrations/20260508233129_phase4_points_streaks_feed.sql | grep -v "^#" | head -1` returns at least 100 (sanity-check non-comment line count; per Nyquist `grep_gate_hygiene` for non-empty grep results)
  </acceptance_criteria>
  <done>0008 migration file created with all 7 sections (publication, replica identity full, race-safe trigger body, counter-immutable trigger, 4 RPCs); publication-add is section 0; replica-identity-full is section 0.5; pg_trigger_depth() bypass; joined_at ASC tiebreaker; same-day no-points gating.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Apply migration 0008 to local Supabase via supabase db push</name>
  <files>(no file modifications — this is the database-push step)</files>
  <read_first>
    - supabase/migrations/20260508233129_phase4_points_streaks_feed.sql (just created in Task 1)
    - .planning/research/PITFALLS.md §"P4-G: Type regeneration timing breaks frontend compile" (the rationale for the strict ordering: push BEFORE types:gen BEFORE frontend hooks)
  </read_first>
  <action>
**This task is MANDATORY** — the phase CANNOT pass verification without it. Build and type checks will pass without the push (types come from config, not the live database), creating a false-positive verification state.

**Pre-flight env check (Iteration 1 plan-checker fix 2026-05-08, WARNING-3)**: Before invoking `supabase db push`, run:

```bash
[ -n "$SUPABASE_ACCESS_TOKEN" ] || { echo "FAIL: SUPABASE_ACCESS_TOKEN missing — set it (and SUPABASE_DB_PASSWORD if interactive prompt blocks) before retrying."; exit 2; }
```

If this exits with 2, the migration cannot be applied non-interactively — the human reviewer is expected to set the env var (or run `supabase db push` from their own terminal and paste back the output) and resume the checkpoint with `approved`.

After push, run `pnpm types:gen` and confirm the regenerated `src/types/database.ts` mtime updated (i.e., the file was actually rewritten — not a no-op). If the types file did not update, the push didn't apply the migration; surface as a checkpoint failure.

Run `supabase db push` (non-interactive on this dev machine — `SUPABASE_ACCESS_TOKEN` env var configured + project-ref set, per the gsd-init guidance).

If supabase CLI prompts for confirmation, the executor may pass `--include-all` if the prompt is non-blocking, OR if a TTY prompt actually blocks, escalate as a checkpoint and ask the user to run `supabase db push` from their terminal and paste back the output.

After `supabase db push` succeeds, run `supabase test db` (full pgTAP suite) to verify:
- The 4 NEW Phase 4 pgTAP files (handle_submission_approval_streak, handle_submission_approval_idempotency, phase4_rpc_permissions, phase4_rpc_correctness) flip from RED to GREEN
- All existing pgTAP files (107 prior tests) remain green
- Total assertion count: ~107 (P3) + ~29 (P4 new) = ~136

**Verify replica identity full applied** (HIGH #2 — RESOLVED via REVIEWS replan 2026-05-08):
```bash
psql -c "select relreplident from pg_class where oid = 'public.submissions'::regclass;"
```
Expected output: `f` (FULL). The values `d` (default — primary-key only), `n` (nothing), `i` (index) all indicate the migration didn't apply correctly.

If any new pgTAP file fails, the migration body has a bug — read the failure output, correlate to the assertion in the relevant test file, fix the SQL in 0008. (For HIGH #1 race-safe failures: check that `current_streak` and `points` use the same recurrence-branch CASE expression. For HIGH #12 failures: confirm `points = points + case ... else 0 end`.)
  </action>
  <verify>
    <automated>supabase test db 2>&1 | tee /tmp/p4-pgtap.log | tail -5 && grep -c "^# ok\|^ok " /tmp/p4-pgtap.log</automated>
  </verify>
  <acceptance_criteria>
    - `supabase db push` exits 0 (or, if interactive prompt blocked execution, the user has run it manually and confirmed migration was applied)
    - `supabase test db` exits 0
    - `supabase test db 2>&1 | grep -E "^(ok|not ok)" | wc -l` returns ≥ 130 assertions (P3's ~107 + P4's ~29)
    - `supabase test db 2>&1 | grep -E "^not ok" | wc -l` returns 0
    - Verify publication: `psql -c "select tablename from pg_publication_tables where pubname='supabase_realtime' and schemaname='public' order by tablename" | grep group_members` returns 1
    - **HIGH #2 verification**: `psql -c "select relreplident from pg_class where oid = 'public.submissions'::regclass;" | grep -c "^ f$\|^f$"` returns at least 1 (RESOLVED via REVIEWS replan 2026-05-08 — replica identity = FULL)
    - **WARNING-3 type-attribute gate (Iteration 1 plan-checker fix 2026-05-08)**: `grep -E '<task type="checkpoint:human-verify"[^>]+gate="blocking"[^>]*>' .planning/phases/04-social-surfaces/04-02-PLAN.md` returns at least 1 line (the task is now a blocking human-verify checkpoint, not autonomous). Verify pre-flight env check is present: `grep -c "SUPABASE_ACCESS_TOKEN missing" .planning/phases/04-social-surfaces/04-02-PLAN.md` is at least 1.
  </acceptance_criteria>
  <done>Migration applied; full pgTAP suite green; group_members in supabase_realtime publication; submissions replica identity = FULL.</done>
  <resume-signal>"approved" once the human reviewer confirms `supabase db push` exit 0 (or pasted output of a manual run), `supabase test db` green, `supabase migration list` shows `20260508233129_phase4_points_streaks_feed` applied, AND the regenerated `src/types/database.ts` mtime is fresh. If any check fails, describe the failure mode (push exit code, pgTAP not-ok rows, or migration-list missing the new file) so the executor can fix the SQL and retry.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Regenerate src/types/database.ts and commit</name>
  <files>src/types/database.ts</files>
  <read_first>
    - package.json (the `types:gen` script — `supabase gen types typescript --local > src/types/database.ts`)
    - 04-RESEARCH.md §"Pitfall P4-G: Type regeneration timing breaks frontend compile" (lines 605-610)
    - supabase/migrations/20260508233129_phase4_points_streaks_feed.sql (the migration just applied)
  </read_first>
  <action>
Run `pnpm types:gen` (which runs `supabase gen types typescript --local > src/types/database.ts`).

The regenerated `src/types/database.ts` MUST include entries under `Database.public.Functions` for the 4 new RPCs:
- `get_pending_today`
- `get_missed_yesterday`
- `get_today_posted_count`
- `get_group_leaderboard`

After generation, run `pnpm typecheck` to verify the regenerated file is valid TypeScript and integrates cleanly with the existing source tree.

If `pnpm types:gen` fails (e.g. local Supabase not running, or the migration didn't apply), do not silently fall back — surface the error.

After both commands succeed, the file is committed by the gsd commit step at the end of the plan.
  </action>
  <verify>
    <automated>pnpm types:gen && grep -c "get_pending_today\|get_missed_yesterday\|get_today_posted_count\|get_group_leaderboard" src/types/database.ts && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm types:gen` exits 0
    - `grep -c "get_pending_today" src/types/database.ts` returns at least 1
    - `grep -c "get_missed_yesterday" src/types/database.ts` returns at least 1
    - `grep -c "get_today_posted_count" src/types/database.ts` returns at least 1
    - `grep -c "get_group_leaderboard" src/types/database.ts` returns at least 1
    - `pnpm typecheck` exits 0 (no regressions in existing source tree)
  </acceptance_criteria>
  <done>Types regenerated; 4 new RPC signatures present in database.ts; typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon → PostgREST RPC | Unauthenticated callers attempting to invoke any of the 4 new RPCs |
| authenticated non-member → PostgREST RPC | Authenticated user not in the target group querying group state |
| authenticated admin → REST UPDATE on group_members | Malicious admin attempting to set their own points = 9999 |
| Realtime CDC → Realtime client | Group state events flowing to subscribed clients |
| Concurrent admin approvals → trigger row-lock | Two admins approve different submissions for the same member simultaneously |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | get_pending_today / get_missed_yesterday / get_group_leaderboard | mitigate | `is_group_member(p_group_id)` raise inside RPC body — non-members get `not_member` (typed P0001), NOT empty list (existence-vs-non-existence leak). Verified by `phase4_rpc_permissions.sql`. |
| T-04-02 | Tampering | group_members counter columns | mitigate | BEFORE UPDATE column-allowlist trigger `group_members_counter_immutable` (D-19) refuses any client UPDATE that changes points/current_streak/longest_streak/last_rolled_date. Bypass for definer-trigger path uses `pg_trigger_depth() > 1`. |
| T-04-03 | Repudiation / Tampering | submissions status flip-back to re-fire trigger | mitigate | 0003's `submissions_owner_immutable` admin-immutable trigger forbids `approved → pending` status flips. Combined with 0001's WHEN clause + UNIQUE constraint `uq_submissions_user_group_local_date` — trigger fires at most once per submission AND cannot fire twice for the same (group_id, user_id, local_date). |
| T-04-04 | Information Disclosure | Realtime CDC events on group_members | mitigate | Realtime checks RLS on the row before publishing. The user's session JWT must satisfy `group_members_select_own_or_same_group` (existing P3 policy). Single-column filter `group_id=eq.{X}` is a server-side constraint; client narrowing in the handler is a fail-safe. |
| T-04-05 | Race Condition / Tampering | Concurrent approvals for same (group_id, user_id) producing stale streak math | mitigate (HIGH #1 RESOLVED via REVIEWS replan 2026-05-08) | Trigger body uses single locked-row UPDATE with CASE expression — there is NO read-modify-write window. Postgres MVCC + the BEFORE/AFTER trigger both see a consistent locked snapshot during the UPDATE. Verified by `handle_submission_approval_idempotency.sql` two-day serial test (sequential proxy; pgTAP cannot harness true concurrency, so the test file documents this limitation inline). |
| T-04-06 | Elevation of Privilege | SECURITY DEFINER RPC search_path injection | mitigate | Every new function declares `set search_path = public`. `revoke execute on function ... from public; grant execute on function ... to authenticated;` lock the function surface to authenticated callers. |
| T-04-07 | Information Disclosure | Realtime payload.old leaking pre-update column values across RLS boundaries | accept | `replica identity full` (HIGH #2 fix) means payload.old contains all columns. Realtime still applies RLS on the row before publishing (existing Supabase guarantee). The full-row payload is only delivered to clients whose session JWT satisfies SELECT RLS on the new row's group_id, so cross-group leak via old-row data is not possible. |
</threat_model>

<verification>
- All 4 P4 pgTAP files green via `supabase test db` (post-push)
- All existing pgTAP files (107 from P1/P2/P3) remain green (no regressions)
- `psql ... select tablename from pg_publication_tables ...` returns `group_members` in the result set
- `psql ... select relreplident from pg_class ...` returns `f` for `public.submissions` (HIGH #2)
- `pnpm typecheck` green after `pnpm types:gen`
- `src/types/database.ts` contains entries for all 4 new RPCs
- No new public-schema tables added (CI rls-check workflow stays green incidentally)
</verification>

<success_criteria>
- `supabase/migrations/20260508233129_phase4_points_streaks_feed.sql` exists and is applied to local DB
- `group_members` is in the `supabase_realtime` publication (CGF-1 fix verified)
- `public.submissions` has `replica identity full` (CGF-2 / HIGH #2 verified)
- 4 SECURITY DEFINER RPCs exist with revoke/grant pairs
- `handle_submission_approval` body computes streak math via single locked-row UPDATE with CASE (HIGH #1 race-safe)
- Same-day branch leaves points and streak unchanged (HIGH #12 belt-and-suspenders)
- `get_group_leaderboard` ORDER BY includes `joined_at ASC` deterministic tiebreaker (MEDIUM)
- `group_members_counter_immutable` trigger blocks client counter mutations and bypasses for the definer-trigger path via `pg_trigger_depth() > 1`
- All 29 P4 pgTAP assertions green
- `src/types/database.ts` regenerated with 4 new RPC entries; `pnpm typecheck` green
- 04-03 hooks (Wave 2) can now compile against the regenerated types
</success_criteria>

<output>
After completion, create `.planning/phases/04-social-surfaces/04-02-SUMMARY.md` capturing:
- Final pgTAP assertion count (P3 baseline + P4 new = total)
- Confirmation that `group_members` is in the publication (with the verifying SQL output snippet)
- Confirmation that `public.submissions` has `replica identity full` (with `psql -c` output snippet)
- The 4 new RPC signatures (verbatim from regenerated `src/types/database.ts`) for downstream plans to reference
- Documentation of the race-safe trigger pattern (single-locked-row UPDATE with CASE) so future maintainers know NOT to refactor back to SELECT-then-UPDATE
- Any deviations from the planned migration body (e.g. if a subtle bug surfaced during pgTAP and required an inline fix)
</output>
