---
phase: 04-social-surfaces
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - jest.setup.ts
  - tests/_helpers/mockSupabaseChannel.ts
  - tests/submissions/useTodaySubmissionRealtime.test.tsx
  - supabase/tests/handle_submission_approval_streak.sql
  - supabase/tests/handle_submission_approval_idempotency.sql
  - supabase/tests/phase4_rpc_permissions.sql
  - supabase/tests/phase4_rpc_correctness.sql
  - tests/features/groups/useGroupLeaderboard.test.tsx
  - tests/features/groups/useGroupLeaderboardRealtime.test.tsx
  - tests/features/submissions/useGroupFeed.test.tsx
  - tests/features/groups/useGroupTombstones.test.tsx
  - tests/features/groups/useGroupSocialCounts.test.tsx
autonomous: true
requirements: [PTS-01, PTS-02, PTS-03, LB-01, LB-02, FEED-01, FEED-02, FEED-03]
must_haves:
  truths:
    - "Wave 0 test scaffolds exist (RED state) for every PTS-01..03, LB-01..02, FEED-01..03 hook + trigger + RPC behavior"
    - "Channel-chain Jest mock helper extracted once and reused across all P4 Realtime hook tests AND the existing useTodaySubmissionRealtime test (single canonical mock — eliminates Gemini drift concern)"
    - "LayoutAnimation has a Jest-safe mock so component tests do not crash on RN's native animation calls; mock exposes `Presets.easeInEaseOut` so 04-05's `LayoutAnimation.Presets.easeInEaseOut` call resolves (MEDIUM #4 from REVIEWS.md)"
    - "Wave 0 RED Jest tests do NOT break `pnpm typecheck` — every import of a not-yet-existing Phase 4 production hook uses `jest.mock(MODULE_PATH, () => ({ <hook_name>: jest.fn() }), { virtual: true })` so TS resolves the symbol against the virtual mock (HIGH #7 from REVIEWS.md)"
    - "RPC permission tests align with 04-02 grants: anon callers of get_today_posted_count receive `permission denied` SQLSTATE 42501 (HIGH #6 — kept tight grants, anon test asserts permission denied)"
    - "Channel-chain mock helper exposes a single canonical accessor named `getChannelName()` (LOW concern — drift fix)"
    - "Tombstone hook test asserts the locked return shape `{ pendingToday, missedYesterday, isPending, error }` (MEDIUM #3 from REVIEWS.md — pinned)"
  artifacts:
    - path: tests/_helpers/mockSupabaseChannel.ts
      provides: "Reusable supabase.channel/on/subscribe/removeChannel mock factory"
      exports: ["setupChannelMock"]
    - path: supabase/tests/handle_submission_approval_streak.sql
      provides: "RED pgTAP scaffold for D-02 streak recurrence (NULL/consecutive/gap/longest_streak)"
    - path: supabase/tests/handle_submission_approval_idempotency.sql
      provides: "RED pgTAP scaffold for D-03 idempotency + Pitfall P4-B concurrency"
    - path: supabase/tests/phase4_rpc_permissions.sql
      provides: "RED pgTAP scaffold for membership gating across 4 RPCs"
    - path: supabase/tests/phase4_rpc_correctness.sql
      provides: "RED pgTAP scaffold for RPC happy paths + DST edge"
    - path: tests/features/groups/useGroupLeaderboard.test.tsx
      provides: "RED Jest scaffold for LB-01 ordering"
    - path: tests/features/groups/useGroupLeaderboardRealtime.test.tsx
      provides: "RED Jest scaffold for LB-02 channel + setQueryData"
    - path: tests/features/submissions/useGroupFeed.test.tsx
      provides: "RED Jest scaffold for FEED-01 today/approved filter"
    - path: tests/features/groups/useGroupTombstones.test.tsx
      provides: "RED Jest scaffold for FEED-02/03 tombstone reads (pinned shape)"
    - path: tests/features/groups/useGroupSocialCounts.test.tsx
      provides: "RED Jest scaffold for D-13 posted-today count"
  key_links:
    - from: tests/features/groups/useGroupLeaderboardRealtime.test.tsx
      to: tests/_helpers/mockSupabaseChannel.ts
      via: "import { setupChannelMock }"
      pattern: "from '../../_helpers/mockSupabaseChannel'"
    - from: tests/submissions/useTodaySubmissionRealtime.test.tsx (refactored)
      to: tests/_helpers/mockSupabaseChannel.ts
      via: "import { setupChannelMock }"
      pattern: "from '../_helpers/mockSupabaseChannel'"
---

## Revisions Applied

This plan was revised on 2026-05-08 in response to cross-AI review (`04-REVIEWS.md`). Concerns addressed:

- **HIGH #6** (`get_today_posted_count` permission mismatch): Locked the strict-grant interpretation — anon callers receive SQLSTATE 42501 `permission denied` because 04-02 revokes execute from `public` and grants only to `authenticated`. The `phase4_rpc_permissions.sql` anon test for `get_today_posted_count` asserts `throws_ok` with SQLSTATE 42501, NOT `is(..., 0)`. See Task 3 File 3 §Anon row.
- **HIGH #7** (Wave 0 RED Jest may break typecheck): Verified `tsconfig.json` includes `**/*.tsx` and does NOT exclude `tests/`, so RED imports of nonexistent production hooks WILL break `pnpm typecheck`. Mitigation: every Wave 0 RED Jest test file uses `jest.mock(MODULE_PATH, () => ({ <hook_name>: jest.fn() }), { virtual: true })` for the not-yet-existing modules at the TOP of the file BEFORE the import of the hook. The `jest.mock` factory provides a virtual implementation that satisfies TypeScript's module resolution. See Task 4 §Common boilerplate.
- **MEDIUM #4** (LayoutAnimation.Presets.easeInEaseOut missing from mock): The jest.setup.ts mock now exposes `Presets: { easeInEaseOut: { duration: 250, ... } }` so 04-05's `LayoutAnimation.configureNext(LayoutAnimation.Presets.easeInEaseOut)` resolves. See Task 2.
- **MEDIUM #3** (`useGroupTombstones` return-shape drift): The tombstone test now asserts the canonical shape `{ pendingToday, missedYesterday, isPending, error }` — same shape pinned in 04-03. See Task 4 File 4.
- **MEDIUM tiebreaker** (`get_group_leaderboard` ORDER BY): Added `joined_at ASC` deterministic tiebreaker assertion to `phase4_rpc_correctness.sql`. See Task 3 File 4 §Test 6.
- **LOW** (helper accessor drift `channelName` vs `getChannelName`): Helper exports `getChannelName(): string | null` only — no `channelName` alias. See Task 1.
- **LOW** (Gemini drift concern — `useTodaySubmissionRealtime.test.tsx` keeps inline mock alongside extracted helper): The existing test file IS refactored to consume `setupChannelMock` so there is exactly one canonical helper across the codebase. See Task 1 final step.

<objective>
Wave 0 infrastructure: lay the test scaffolding so every Phase 4 task in Waves 1–3 has a RED test to drive against (Nyquist compliance — see 04-VALIDATION.md). Extract the existing per-test channel-chain mock pattern from `tests/submissions/useTodaySubmissionRealtime.test.tsx` (lines 51-91) into a single reusable helper, **and refactor that source test file to consume the helper** so there is exactly one canonical mock implementation. Add a `LayoutAnimation` Jest mock with `Presets.easeInEaseOut` for component + Realtime tests in 04-04 / 04-05.

Purpose: Without these scaffolds, downstream waves would create tests + production code in the same task — the RED→GREEN signal collapses. With the scaffolds in place, every implementation task in 04-02..04-06 has a single failing test command that flips green when the code is correct.

Output: 1 helper file + 1 jest.setup.ts addition + 4 RED pgTAP files + 5 RED Jest test files + 1 refactored existing test file. No new dependencies — all libraries (`@supabase/supabase-js`, `@tanstack/react-query`, `expo-router`, jest-expo, pgTAP) are already pinned.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-social-surfaces/04-CONTEXT.md
@.planning/phases/04-social-surfaces/04-RESEARCH.md
@.planning/phases/04-social-surfaces/04-PATTERNS.md
@.planning/phases/04-social-surfaces/04-VALIDATION.md
@.planning/phases/04-social-surfaces/04-REVIEWS.md
@tests/submissions/useTodaySubmissionRealtime.test.tsx
@jest.setup.ts
@tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract channel-chain mock helper to tests/_helpers/mockSupabaseChannel.ts AND refactor source test file to consume it</name>
  <files>
    tests/_helpers/mockSupabaseChannel.ts,
    tests/submissions/useTodaySubmissionRealtime.test.tsx
  </files>
  <read_first>
    - tests/submissions/useTodaySubmissionRealtime.test.tsx (lines 1-150 — the existing inline pattern that this helper extracts AND the refactor target)
    - 04-PATTERNS.md §"Realtime Channel Lifecycle" (the 5 invariants the helper must preserve)
    - 04-RESEARCH.md §Pattern 3 (verbatim shape — lines 391-456)
    - .planning/phases/04-social-surfaces/04-REVIEWS.md (LOW concern — drift between helper and existing test file)
  </read_first>
  <action>
**Step 1: Create `tests/_helpers/mockSupabaseChannel.ts`** exporting `setupChannelMock(supabase: { channel: unknown; removeChannel: unknown })` returning the same fixture shape currently inlined in `useTodaySubmissionRealtime.test.tsx` lines 51-91.

The helper MUST:
1. Define and export the `RealtimePayload` and `Handler` types verbatim from the existing test file.
2. Define and export the `RealtimeFixture` interface verbatim (lines 51-59) plus a single canonical accessor `getChannelName(): string | null` returning the FIRST argument passed to `supabase.channel(...)`. **Accessor name is exactly `getChannelName` — NOT `channelName` (LOW concern from REVIEWS.md).**
3. Implement `setupChannelMock` so it calls `jest.spyOn(supabase, 'channel')` and `jest.spyOn(supabase, 'removeChannel')` exactly as the source pattern. The `on` mock captures the handler so tests can drive synthetic payloads via `getHandler()`. The `subscribe` mock returns `{ unsubscribe: jest.fn() }`.
4. Track the channel name: `channel = jest.fn((name: string) => { capturedChannelName = name; return { on }; })` and add `getChannelName: () => capturedChannelName` to the fixture.

Add a doc comment at the top: `// Extracted from tests/submissions/useTodaySubmissionRealtime.test.tsx (lines 51-91) for reuse across Phase 4 Realtime hook tests (D-15, D-20, D-21). Per 04-PATTERNS.md §"Realtime Channel Lifecycle".`

ESLint discipline: file must compile under strict TS (no `any`; use `unknown` + casts where needed — mirror the source pattern's `as never` casts on `'postgres_changes'` literal).

**Step 2: Refactor `tests/submissions/useTodaySubmissionRealtime.test.tsx`** to consume the helper:
- DELETE the inline `setup()` function (lines ~51-91) and the `RealtimePayload` / `Handler` / `RealtimeFixture` type declarations (lines ~48-59).
- ADD `import { setupChannelMock, type RealtimeFixture } from '../_helpers/mockSupabaseChannel';` at the top.
- REPLACE every existing call site of `setup()` (likely 4-6 sites) with `const fixture = setupChannelMock(supabase);` followed by destructuring (or inline destructure).
- Test BODIES (`expect(...)`) MUST stay byte-identical — preserves green state.
- Run `pnpm test --testPathPattern="tests/submissions/useTodaySubmissionRealtime"` after refactor to confirm.

This eliminates the LOW Gemini drift concern.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test --testPathPattern="tests/submissions/useTodaySubmissionRealtime" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/_helpers/mockSupabaseChannel.ts` exists
    - `grep -c "export function setupChannelMock" tests/_helpers/mockSupabaseChannel.ts` returns 1
    - `grep -c "jest.spyOn" tests/_helpers/mockSupabaseChannel.ts` returns 2 (channel + removeChannel)
    - `grep -c "getChannelName" tests/_helpers/mockSupabaseChannel.ts` is at least 2
    - `grep -E "^[^/]*\\bchannelName\\b" tests/_helpers/mockSupabaseChannel.ts | grep -v "getChannelName"` returns nothing (no bare `channelName` alias outside comments)
    - `grep -c "setupChannelMock" tests/submissions/useTodaySubmissionRealtime.test.tsx` is at least 1
    - `grep -c "function setup()" tests/submissions/useTodaySubmissionRealtime.test.tsx` returns 0 (inline factory removed)
    - `pnpm typecheck` exits 0
    - `pnpm test --testPathPattern="tests/submissions/useTodaySubmissionRealtime"` exits 0
  </acceptance_criteria>
  <done>Helper file created; existing test refactored; typecheck green; existing test still passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add LayoutAnimation Jest mock (with Presets.easeInEaseOut) to jest.setup.ts</name>
  <files>jest.setup.ts</files>
  <behavior>
    - LeaderboardRow component test (Wave 2) can call code that invokes `LayoutAnimation.configureNext(...)` without throwing
    - 04-05's `LayoutAnimation.configureNext(LayoutAnimation.Presets.easeInEaseOut)` resolves at test time
    - Existing tests continue to pass (additive change only)
  </behavior>
  <read_first>
    - jest.setup.ts (the entire file)
    - 04-UI-SPEC.md §"Interaction Contracts" lines 783-784
    - 04-REVIEWS.md MEDIUM #4 (Presets.easeInEaseOut requirement)
  </read_first>
  <action>
Append to `jest.setup.ts` a `jest.mock('react-native', ...)` partial mock that preserves the real react-native exports while replacing `LayoutAnimation` with a no-op mock that **includes the `Presets` namespace** (RESOLVED via REVIEWS replan 2026-05-08 — MEDIUM #4):

```ts
// react-native LayoutAnimation — Phase 4 hooks call configureNext() to animate
// leaderboard row reorders and feed prepends. The native bridge is unavailable
// in jest, so stub the API. All other react-native exports remain real.
// MEDIUM #4 (REVIEWS replan 2026-05-08): Presets.easeInEaseOut MUST be present
// because 04-05 calls LayoutAnimation.configureNext(LayoutAnimation.Presets.easeInEaseOut).
jest.mock('react-native', () => {
  const actual = jest.requireActual('react-native');
  return {
    ...actual,
    LayoutAnimation: {
      configureNext: jest.fn(),
      create: jest.fn((duration: number, type: string, prop: string) => ({
        duration,
        update: { type, property: prop },
        create: { type, property: prop },
        delete: { type, property: prop },
      })),
      Types: { easeInEaseOut: 'easeInEaseOut', linear: 'linear', spring: 'spring' },
      Properties: { opacity: 'opacity', scaleXY: 'scaleXY' },
      easeInEaseOut: 'easeInEaseOut',
      Presets: {
        easeInEaseOut: { duration: 250, update: { type: 'easeInEaseOut' }, create: { type: 'easeInEaseOut', property: 'opacity' }, delete: { type: 'easeInEaseOut', property: 'opacity' } },
        linear: { duration: 500, update: { type: 'linear' }, create: { type: 'linear', property: 'opacity' }, delete: { type: 'linear', property: 'opacity' } },
        spring: { duration: 700, update: { type: 'spring', springDamping: 0.4 }, create: { type: 'linear', property: 'opacity' }, delete: { type: 'linear', property: 'opacity' } },
      },
    },
  };
});
```

Place AFTER the `react-native-reanimated` mock and BEFORE end-of-file.

**Do NOT replace the existing `jest.mock('react-native', () => ({ AppState: ... }))` shim used inside individual test files** — those are scoped to test files and override this global mock per-file. Adding the global mock is additive; per-file mocks continue to win.
  </action>
  <verify>
    <automated>pnpm test --testPathPattern=tests/groups/groupDetailScreen 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LayoutAnimation" jest.setup.ts` is at least 3
    - `grep -c "Presets:" jest.setup.ts` is at least 1 (MEDIUM #4 fix — REVIEWS replan 2026-05-08)
    - `grep -c "easeInEaseOut" jest.setup.ts` is at least 3 (Types entry + bare entry + Presets entry)
    - `pnpm test --testPathPattern=tests/groups/groupDetailScreen` exits 0
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>jest.setup.ts has a LayoutAnimation mock with Presets.easeInEaseOut; existing tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create 4 RED pgTAP scaffolds for Phase 4 DB layer</name>
  <files>
    supabase/tests/handle_submission_approval_streak.sql,
    supabase/tests/handle_submission_approval_idempotency.sql,
    supabase/tests/phase4_rpc_permissions.sql,
    supabase/tests/phase4_rpc_correctness.sql
  </files>
  <behavior>
    - All 4 files exist and run via `supabase test db`
    - All 4 files FAIL initially (RED state)
    - `phase4_rpc_permissions.sql` asserts SQLSTATE `42501` permission denied (NOT `is(..., 0)`) for anon callers of `get_today_posted_count` (HIGH #6)
    - `phase4_rpc_correctness.sql` includes a deterministic-tiebreaker assertion using `joined_at ASC` (MEDIUM tiebreaker fix)
  </behavior>
  <read_first>
    - supabase/tests/get_pending_review_count.sql (JWT claims pattern — lines 55-139)
    - supabase/tests/get_pending_review_queue.sql (multi-row seed + ordering — lines 60-100)
    - supabase/tests/submissions_admin_immutable.sql (multi-persona seed — lines 17-95)
    - 04-RESEARCH.md §Appendix §5 (lines 1031-1042)
    - 04-VALIDATION.md §"Wave 0 Requirements"
    - 04-CONTEXT.md D-02, D-03
    - .planning/phases/04-social-surfaces/04-REVIEWS.md (HIGH #6 anon strict-grant; HIGH #12 same-day branch; MEDIUM tiebreaker)
  </read_first>
  <action>
Create 4 pgTAP files. Each follows the standard `begin; select plan(N); ...; select * from finish(); rollback;` structure used in every existing `supabase/tests/*.sql` file.

**File 1: `supabase/tests/handle_submission_approval_streak.sql`** — D-02 four-branch coverage + D-01 server-derived local_date:

Seed (multi-persona pattern from `submissions_admin_immutable.sql` lines 17-95, with **two groups in different timezones** to prove D-01):
- Admin (`11111111-1111-1111-1111-111111111111`) + member (`44444444-4444-4444-4444-444444444444`) per group
- Group A: `aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa`, photo, `America/Los_Angeles`
- Group B: `bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb`, photo, `America/New_York`
- 4+ pending submissions for the member at distinct local_dates (today, today-1, today-2, today-7)

Test plan(8):
1. NULL branch — initial `last_rolled_date IS NULL`; approve `<today-7 sub>` → assert `current_streak=1, points=1, longest_streak=1, last_rolled_date=<today-7>`
2. Consecutive branch — approve `<today-6>` → `current_streak=2, points=2, longest_streak=2`
3. Gap branch — approve `<today-2>` → `current_streak=1` (reset), `points=3`, `longest_streak=2` (preserved)
4. longest_streak preserved — approve `<today-1>` then `<today>` → `current_streak=3, longest_streak=3`
5. D-01 cross-tz proof — approve in Group B, assert `last_rolled_date` matches **submission's local_date** (NOT now()::date)
6. Points monotonic — `points = 5` after 5 approvals
7. Other group untouched — Group A counters unaffected by Group B approvals
8. AFTER UPDATE WHEN clause: status flip `pending → approved` increments; re-issue `update set status='approved' where status='approved'` does NOT increment again

Use raw `update public.submissions set status = 'approved' where id = ...` (NOT `review_submission` RPC).

**File 2: `supabase/tests/handle_submission_approval_idempotency.sql`** — D-03 + Pitfall P4-B sequential proxy:

Seed: 1 admin + 1 member, group `America/Los_Angeles`, two pending submissions (today-1, today).

Test plan(5):
1. WHEN clause blocks no-op — approve once, points=1; re-issue with same status, points still=1
2. 0003 admin-immutable blocks status flip-back — `update set status='pending' where id=<approved-sub>` raises; no change to group_members
3. AFTER UPDATE WHEN no-fire on UPDATE that doesn't touch status — `update set caption='changed'`; points unchanged
4. Two-day serial coverage — approve `<today-1>` then `<today>` in same connection; assert `points=2, current_streak=2, longest_streak=2`. **Append a comment**: `-- NOTE: pgTAP is single-session; true concurrent-approval coverage requires advisory locks or two psql sessions. The trigger body's race-safety relies on the locked-row UPDATE pattern from 04-02 §Section 1 (HIGH #1 from REVIEWS.md).`
5. Direct group_members.points UPDATE blocked — `set local role authenticated` admin claims, `update group_members set points = 9999`; assert raises (verifies column-allowlist trigger from 04-02; in Wave 0 RED state this fails)

**File 3: `supabase/tests/phase4_rpc_permissions.sql`** — 5-persona × 4-RPC permission matrix:

Seed: admin (`11111111...`), member (`44444444...`), stranger (`99999999...`), ex-member (`88888888...`), one group `aaaaaaaa...`.

Test plan(~16):

For each of 4 RPCs (`get_pending_today`, `get_missed_yesterday`, `get_today_posted_count`, `get_group_leaderboard`):
- **Anon (no `request.jwt.claims`):**
  - `get_pending_today` / `get_missed_yesterday` / `get_group_leaderboard`: `throws_ok($$select get_pending_today(...)$$, 'P0001', 'not_authenticated', '...')`
  - `get_today_posted_count`: assert via `throws_ok($$select get_today_posted_count('aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa'::uuid)$$, '42501', NULL, 'anon receives permission denied because public has no execute grant')`. **HIGH #6 LOCKED INTERPRETATION (RESOLVED via REVIEWS replan 2026-05-08): the strict-grant choice from 04-02 means anon hits permission denied BEFORE the function body runs.**
- **Stranger (authenticated, not in group):**
  - typed-error RPCs: `throws_ok` SQLSTATE `P0001` `'not_member'`
  - lenient `get_today_posted_count`: returns `0` (function body soft-fails for authenticated non-member)
- **Member (authenticated, in group):** returns successfully (any non-null result)
- **Admin (authenticated, admin of group):** returns successfully

Plus one assertion: direct `update group_members set points = 9999` from `set local role authenticated` admin → raises `'group_members counter columns are server-managed'` (verifies column-allowlist trigger + `pg_trigger_depth() > 1` bypass).

Use `throws_ok($$select ... $$, '<sqlstate>', '<typed_message>', '<description>')` per `supabase/tests/get_pending_review_queue.sql` lines 85-90. Typed-error RPCs use `'P0001'`; anon-grant path uses `'42501'`.

**File 4: `supabase/tests/phase4_rpc_correctness.sql`** — RPC happy-path + DST + cross-tz + tiebreaker:

Seed: 4 members in one group (`America/Los_Angeles`); 1 has approved for today, 1 has rejected for today, 1 has pending for today, 1 has nothing for today; 2 of those 4 have approved for yesterday.

**Additional tiebreaker seed**: 2 members with identical `points = 5, current_streak = 3` but different `joined_at` (one earlier, one later) — to drive Test 6.

Test plan(~10):
1. `get_pending_today(group)` returns the 1 member with NO submission for today (alphabetical by display_name)
2. `get_pending_today` excludes rejected/pending submissions for today
3. `get_missed_yesterday(group)` returns the 2 members without an APPROVED row for yesterday
4. `get_missed_yesterday` treats pending and rejected as missing
5. `get_today_posted_count(group)` returns 1 (only approved counts)
6. `get_group_leaderboard(group)` returns 4 rows ordered by `points DESC, current_streak DESC, joined_at ASC` (deterministic tiebreaker — RESOLVED via REVIEWS replan 2026-05-08, MEDIUM concern). Assert via `results_eq` with the known-stable seed: the two members with identical points + streak appear in `joined_at ASC` order (older member first).
7. `get_group_leaderboard` returns columns: `display_name`, `avatar_path`, `updated_at`, `points`, `current_streak`, `longest_streak`, `last_rolled_date`, `joined_at`
8. DST edge: second group in `America/New_York` near spring-forward; assert `(now() AT TIME ZONE 'America/New_York')::date - 1` returns the expected calendar day
9. Cross-tz isolation — Group B's RPCs do not return Group A's members
10. `get_pending_today` order alphabetical by display_name (assert via `results_eq`)

**Every file MUST end with `select * from finish(); rollback;`.** Every file uses the JWT claims + `set local role authenticated` + `reset role` + `select set_config('request.jwt.claims', NULL, true);` pattern.

These files MUST fail in Wave 0. 04-02 makes them green.
  </action>
  <verify>
    <automated>ls supabase/tests/handle_submission_approval_streak.sql supabase/tests/handle_submission_approval_idempotency.sql supabase/tests/phase4_rpc_permissions.sql supabase/tests/phase4_rpc_correctness.sql && supabase test db 2>&1 | grep -E "FAIL|not ok|PASS|ok " | head -10</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist
    - `grep -c "select plan" supabase/tests/handle_submission_approval_streak.sql` returns 1
    - `grep -c "select plan" supabase/tests/handle_submission_approval_idempotency.sql` returns 1
    - `grep -c "select plan" supabase/tests/phase4_rpc_permissions.sql` returns 1
    - `grep -c "select plan" supabase/tests/phase4_rpc_correctness.sql` returns 1
    - `grep -c "request.jwt.claims" supabase/tests/phase4_rpc_permissions.sql` is at least 4 (one per persona)
    - `grep -c "throws_ok" supabase/tests/phase4_rpc_permissions.sql` is at least 4
    - `grep -c "42501" supabase/tests/phase4_rpc_permissions.sql` is at least 1 (HIGH #6 strict-grant anon path — RESOLVED via REVIEWS replan 2026-05-08)
    - `grep -c "joined_at" supabase/tests/phase4_rpc_correctness.sql` is at least 2 (the seed + the deterministic-tiebreaker assertion — MEDIUM tiebreaker fix)
    - `grep -c "America/New_York\|America/Los_Angeles" supabase/tests/phase4_rpc_correctness.sql` is at least 2
    - `grep -c "HIGH #1 from REVIEWS" supabase/tests/handle_submission_approval_idempotency.sql` is at least 1 (the inline note about pgTAP single-session)
    - `supabase test db` exits non-zero (RED state expected)
  </acceptance_criteria>
  <done>4 RED pgTAP files committed; supabase test db reports failures in exactly the expected new files (and no regressions in existing tests).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create 5 RED Jest scaffolds for Phase 4 hooks (with virtual-mock typecheck guard)</name>
  <files>
    tests/features/groups/useGroupLeaderboard.test.tsx,
    tests/features/groups/useGroupLeaderboardRealtime.test.tsx,
    tests/features/submissions/useGroupFeed.test.tsx,
    tests/features/groups/useGroupTombstones.test.tsx,
    tests/features/groups/useGroupSocialCounts.test.tsx
  </files>
  <behavior>
    - Each test file `jest.mock`s the not-yet-existing production module with `{ virtual: true }` so `pnpm typecheck` passes (HIGH #7)
    - Each test file imports the not-yet-existing hook from its production path AFTER the virtual mock; jest resolves the import to the mock factory's exports
    - The Realtime test file imports `setupChannelMock` from Task 1
    - Tombstone test asserts the locked return shape `{ pendingToday, missedYesterday, isPending, error }` (MEDIUM #3)
    - Tests assert the contract: queryKey shape, RPC name called, returned data shape, channel name, filter, setQueryData behavior
  </behavior>
  <read_first>
    - tests/submissions/useTodaySubmissionRealtime.test.tsx (refactored in Task 1 — copy QueryClientProvider wrapper, expo-router mock, AppState mock)
    - tests/submissions/useReviewQueue.test.tsx (RPC mock pattern — `jest.spyOn(supabase, 'rpc').mockResolvedValueOnce({ data: ..., error: null })`)
    - 04-PATTERNS.md §"Pattern Assignments — Data Hooks" (the 5 hook contracts)
    - 04-RESEARCH.md §Appendix §6 (the 4-hook table — queryKey shapes)
    - 04-VALIDATION.md §"Per-Task Verification Map" (rows 5-9)
    - tests/_helpers/mockSupabaseChannel.ts (created in Task 1)
    - .planning/phases/04-social-surfaces/04-REVIEWS.md (HIGH #7 virtual-mock guard; MEDIUM #3 tombstone shape)
    - tsconfig.json (the typecheck-include glob that drives the virtual-mock requirement)
  </read_first>
  <action>
Create 5 RED Jest test files. Each follows the existing pattern from `tests/submissions/useReviewQueue.test.tsx` (read hooks) or `tests/submissions/useTodaySubmissionRealtime.test.tsx` (Realtime hooks).

**Common boilerplate** at the top of each file (mirror `useTodaySubmissionRealtime.test.tsx` lines 18-42, plus the **HIGH #7 virtual-mock guard — RESOLVED via REVIEWS replan 2026-05-08**):

```ts
process.env.EXPO_PUBLIC_SUPABASE_URL = 'https://test.supabase.co';
process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY = 'test-anon-key';

// HIGH #7 (REVIEWS replan 2026-05-08): tsconfig.json includes tests/, so RED imports
// of nonexistent production modules WILL break `pnpm typecheck`. Virtual-mock the
// not-yet-existing module here so TS resolves the symbol against the factory's exports.
// 04-03 will create the real module; the virtual mock then becomes a no-op (jest.mock
// continues to work whether the file exists or not).
jest.mock('../../../src/features/groups/useGroupLeaderboard', () => ({
  useGroupLeaderboard: jest.fn(),
}), { virtual: true });

jest.mock('react-native', () => ({
  AppState: { addEventListener: jest.fn() },
}));

jest.mock('expo-router', () => {
  const React = require('react');
  return {
    useFocusEffect: (effect: () => void | (() => void)) => {
      React.useEffect(() => {
        const cleanup = effect();
        return typeof cleanup === 'function' ? cleanup : undefined;
      }, [effect]);
    },
  };
});
```

The exact `jest.mock` MODULE_PATH must match the import path the test uses for the not-yet-existing hook (e.g. `'../../../src/features/groups/useGroupLeaderboard'`). Each test file should virtual-mock ITS specific hook module path.

Each test file uses `renderHook` from `@testing-library/react-native` + a `QueryClientProvider` wrapper + `jest.spyOn(supabase, 'rpc')` or `jest.spyOn(supabase, 'from')` per the hook's data path.

**File 1: `tests/features/groups/useGroupLeaderboard.test.tsx`** (LB-01 — 4 tests):
- virtual-mock: `'../../../src/features/groups/useGroupLeaderboard'` exporting `useGroupLeaderboard: jest.fn()`
- imports `useGroupLeaderboard` from `../../../src/features/groups/useGroupLeaderboard` (resolves to virtual mock at typecheck; will resolve to real file at test time once 04-03 ships)
- Test 1: `does not query when groupId is undefined`
- Test 2: `calls supabase.rpc('get_group_leaderboard', { p_group_id })` — `jest.spyOn(supabase, 'rpc').mockResolvedValueOnce({ data: [...], error: null })`
- Test 3: `returns rows in the order the RPC delivers them (server-side sort)` — RPC returns 3 rows already ordered; hook must NOT re-sort
- Test 4: `surfaces error.message` — RPC returns `{ data: null, error: { message: 'not_member' } }`

**File 2: `tests/features/groups/useGroupLeaderboardRealtime.test.tsx`** (LB-02 — 5 tests):
- virtual-mock: `'../../../src/features/groups/useGroupLeaderboardRealtime'`
- imports `useGroupLeaderboardRealtime` and `setupChannelMock` from `../../_helpers/mockSupabaseChannel`
- Test 1: `subscribes with channel name 'group-lb:{groupId}'` — assert via `getChannelName() === 'group-lb:abc-123'`
- Test 2: `subscribes with filter 'group_id=eq.{groupId}' on table 'group_members' event 'UPDATE'`
- Test 3: `does NOT subscribe when groupId is undefined`
- Test 4: `patches the leaderboard cache via setQueryData on UPDATE event`
- Test 5: `cleans up channel on unmount via removeChannel`

**File 3: `tests/features/submissions/useGroupFeed.test.tsx`** (FEED-01 — 4 tests):
- virtual-mock: `'../../../src/features/submissions/useGroupFeed'`
- Test 1: `does not query when groupId is undefined OR today is undefined`
- Test 2: `calls supabase.from('submissions').select(...).eq('group_id').eq('local_date', today).eq('status', 'approved')`
- Test 3: `orders by created_at descending`
- Test 4: `maps embedded profile fields into flat shape`

**File 4: `tests/features/groups/useGroupTombstones.test.tsx`** (FEED-02/03 — 4 tests):
- virtual-mock: `'../../../src/features/groups/useGroupTombstones'`
- Test 1: `does not query when groupId is undefined`
- Test 2: `today scope calls supabase.rpc('get_pending_today', { p_group_id })`
- Test 3: `yesterday scope calls supabase.rpc('get_missed_yesterday', { p_group_id })`
- Test 4: `returns the locked shape { pendingToday, missedYesterday, isPending, error }` — render the combined hook; assert `data` exposes `pendingToday: TombstoneRow[]`, `missedYesterday: TombstoneRow[]`, `isPending: boolean`, `error: Error | null`. **MEDIUM #3 (RESOLVED via REVIEWS replan 2026-05-08): this is the canonical shape pinned in 04-03 — NOT `data.pendingToday` / NOT `{ today, yesterday }`.**

**File 5: `tests/features/groups/useGroupSocialCounts.test.tsx`** (D-13/D-15 — 3 tests):
- virtual-mock: `'../../../src/features/groups/useGroupSocialCounts'`
- Test 1: `does not query when groupId is undefined`
- Test 2: `calls supabase.rpc('get_today_posted_count', { p_group_id })`
- Test 3: `returns the integer count from the RPC (defaulting to 0 when null)`

**Every test file**: typecheck MUST pass after the virtual-mock declaration (HIGH #7). Test execution will fail at runtime when the mocked function returns `undefined` and the test assertion expects a real return — that's the RED signal. 04-03 will replace the virtual mock with the real module and the tests will turn GREEN.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test --testPathPattern="tests/features/(groups|submissions)/(useGroupLeaderboard|useGroupLeaderboardRealtime|useGroupFeed|useGroupTombstones|useGroupSocialCounts)" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist
    - `grep -c "renderHook\|QueryClientProvider" tests/features/groups/useGroupLeaderboard.test.tsx` is at least 2
    - `grep -c "setupChannelMock" tests/features/groups/useGroupLeaderboardRealtime.test.tsx` is at least 1
    - `grep -c "group-lb:" tests/features/groups/useGroupLeaderboardRealtime.test.tsx` is at least 1
    - `grep -c "get_today_posted_count" tests/features/groups/useGroupSocialCounts.test.tsx` is at least 1
    - `grep -c "get_pending_today\|get_missed_yesterday" tests/features/groups/useGroupTombstones.test.tsx` is at least 2
    - `grep -c "virtual: true" tests/features/groups/useGroupLeaderboard.test.tsx tests/features/groups/useGroupLeaderboardRealtime.test.tsx tests/features/submissions/useGroupFeed.test.tsx tests/features/groups/useGroupTombstones.test.tsx tests/features/groups/useGroupSocialCounts.test.tsx` returns 5 (one per file — HIGH #7 mitigation, RESOLVED via REVIEWS replan 2026-05-08)
    - `grep -c "pendingToday\|missedYesterday" tests/features/groups/useGroupTombstones.test.tsx` is at least 2 (MEDIUM #3 — locked shape, RESOLVED via REVIEWS replan 2026-05-08)
    - **HIGH #7 GATE: `pnpm typecheck` exits 0** — typecheck cannot fail because of the RED scaffolds; the virtual mocks satisfy module resolution
    - `pnpm test --testPathPattern="tests/features/(groups|submissions)/(useGroupLeaderboard|useGroupLeaderboardRealtime|useGroupFeed|useGroupTombstones|useGroupSocialCounts)"` exits non-zero (RED at runtime — the virtual mock returns `undefined`; 04-03 makes it real)
  </acceptance_criteria>
  <done>5 RED Jest files committed; typecheck green (virtual mocks); test execution RED at runtime for 04-03 to drive against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Test infrastructure only — no new trust boundaries; threat-model lives in 04-02 (the migration plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W0-01 | Information Disclosure | Wave 0 test seeds with placeholder JWT claims | accept | All test seeds use deterministic UUIDs (`11111111-...`), no real PII. Pattern matches existing `supabase/tests/*.sql` precedent. Tests run in `begin/rollback` blocks against a local Supabase instance. |
</threat_model>

<verification>
Run `pnpm typecheck` — exits 0 (the virtual-mock guard from HIGH #7 keeps TS green even though the production hooks don't exist yet).
Run `pnpm test --testPathPattern="tests/features/(groups|submissions)/(useGroupLeaderboard|useGroupLeaderboardRealtime|useGroupFeed|useGroupTombstones|useGroupSocialCounts)"` — exits non-zero with RED-state assertion failures (the virtual mocks return `undefined`; tests assert specific return shapes and behaviors that fail until 04-03 ships).
Run `supabase test db` — exits non-zero with failures in exactly the 4 new pgTAP files (and zero regressions in existing pgTAP files).
Run existing test smoke check — `pnpm test --testPathPattern="tests/(submissions/useTodaySubmissionRealtime|groups/groupDetailScreen)"` — exits 0 (the helper extraction + LayoutAnimation mock are non-breaking).
</verification>

<success_criteria>
- 1 helper file (`tests/_helpers/mockSupabaseChannel.ts`) committed
- jest.setup.ts has a LayoutAnimation partial mock with Presets.easeInEaseOut
- 4 RED pgTAP scaffolds committed (HIGH #6 + MEDIUM tiebreaker covered)
- 5 RED Jest scaffolds committed (HIGH #7 virtual-mock guard + MEDIUM #3 tombstone shape covered)
- `tests/submissions/useTodaySubmissionRealtime.test.tsx` refactored to consume helper (LOW drift fix)
- `pnpm typecheck` green (HIGH #7 enforcement)
- Every Wave 1+ task in 04-02..04-06 has at least one matching RED test to drive against (Nyquist compliance)
</success_criteria>

<output>
After completion, create `.planning/phases/04-social-surfaces/04-01-SUMMARY.md` capturing:
- Test count breakdown (4 pgTAP files / N assertions, 5 Jest files / M tests)
- Confirmed RED state (which assertions fail, naming the missing production functions/files)
- Channel-chain helper API surface (the function signature) so downstream plans reference it consistently
- Confirmation that `pnpm typecheck` passes via virtual mocks (HIGH #7 mitigation verified)
- Confirmation that anon `get_today_posted_count` test asserts SQLSTATE 42501 (HIGH #6 strict-grant interpretation locked)
</output>
