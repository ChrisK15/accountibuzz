---
phase: 04-social-surfaces
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - jest.setup.ts
  - tests/_helpers/mockSupabaseChannel.ts
  - supabase/tests/handle_submission_approval_streak.sql
  - supabase/tests/handle_submission_approval_idempotency.sql
  - supabase/tests/phase4_rpc_permissions.sql
  - supabase/tests/phase4_rpc_correctness.sql
  - tests/features/groups/useGroupLeaderboard.test.tsx
  - tests/features/groups/useGroupLeaderboardRealtime.test.tsx
  - tests/features/submissions/useGroupFeed.test.tsx
  - tests/features/groups/useGroupTombstones.test.tsx
  - tests/features/groups/useGroupSocialCounts.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Wave 0 test scaffolds exist (RED state) for every PTS-01..03, LB-01..02, FEED-01..03 hook + trigger + RPC behavior"
    - "Channel-chain Jest mock helper extracted once and reused across all P4 Realtime hook tests (D-15, D-20, D-21)"
    - "LayoutAnimation has a Jest-safe mock so component tests do not crash on RN's native animation calls"
  artifacts:
    - path: tests/_helpers/mockSupabaseChannel.ts
      provides: "Reusable supabase.channel/on/subscribe/removeChannel mock factory"
      exports: ["setupChannelMock"]
    - path: supabase/tests/handle_submission_approval_streak.sql
      provides: "RED pgTAP scaffold for D-02 streak recurrence (NULL/consecutive/gap/longest_streak)"
    - path: supabase/tests/handle_submission_approval_idempotency.sql
      provides: "RED pgTAP scaffold for D-03 idempotency + Pitfall P4-B concurrency"
    - path: supabase/tests/phase4_rpc_permissions.sql
      provides: "RED pgTAP scaffold for membership gating across 4 RPCs"
    - path: supabase/tests/phase4_rpc_correctness.sql
      provides: "RED pgTAP scaffold for RPC happy paths + DST edge"
    - path: tests/features/groups/useGroupLeaderboard.test.tsx
      provides: "RED Jest scaffold for LB-01 ordering"
    - path: tests/features/groups/useGroupLeaderboardRealtime.test.tsx
      provides: "RED Jest scaffold for LB-02 channel + setQueryData"
    - path: tests/features/submissions/useGroupFeed.test.tsx
      provides: "RED Jest scaffold for FEED-01 today/approved filter"
    - path: tests/features/groups/useGroupTombstones.test.tsx
      provides: "RED Jest scaffold for FEED-02/03 tombstone reads"
    - path: tests/features/groups/useGroupSocialCounts.test.tsx
      provides: "RED Jest scaffold for D-13 posted-today count"
  key_links:
    - from: tests/features/groups/useGroupLeaderboardRealtime.test.tsx
      to: tests/_helpers/mockSupabaseChannel.ts
      via: "import { setupChannelMock }"
      pattern: "from '../../_helpers/mockSupabaseChannel'"
    - from: tests/features/submissions/useGroupFeedRealtime test (created in 04-03)
      to: tests/_helpers/mockSupabaseChannel.ts
      via: "import { setupChannelMock }"
      pattern: "from '../../_helpers/mockSupabaseChannel'"
---

<objective>
Wave 0 infrastructure: lay the test scaffolding so every Phase 4 task in Waves 1–3 has a RED test to drive against (Nyquist compliance — see 04-VALIDATION.md). Extract the existing per-test channel-chain mock pattern from `tests/submissions/useTodaySubmissionRealtime.test.tsx` (lines 61–91) into a single reusable helper so the three new Realtime hooks (D-15, D-20, D-21) consume it without copy-paste drift. Add a `LayoutAnimation` Jest mock for component tests in 04-04.

Purpose: Without these scaffolds, downstream waves would create tests + production code in the same task — the RED→GREEN signal collapses. With the scaffolds in place, every implementation task in 04-02..04-06 has a single failing test command that flips green when the code is correct.

Output: 1 helper file + 1 jest.setup.ts addition + 4 RED pgTAP files + 5 RED Jest test files. No new dependencies — all libraries (`@supabase/supabase-js`, `@tanstack/react-query`, `expo-router`, jest-expo, pgTAP) are already pinned.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-social-surfaces/04-CONTEXT.md
@.planning/phases/04-social-surfaces/04-RESEARCH.md
@.planning/phases/04-social-surfaces/04-PATTERNS.md
@.planning/phases/04-social-surfaces/04-VALIDATION.md
@tests/submissions/useTodaySubmissionRealtime.test.tsx
@jest.setup.ts

<interfaces>
<!-- Existing channel-chain mock pattern (verbatim source the helper extracts from). -->

From `tests/submissions/useTodaySubmissionRealtime.test.tsx` (lines 51-91 — the inline pattern):
```ts
type RealtimePayload = { new?: unknown; old?: unknown };
type Handler = (payload: RealtimePayload) => void;

interface RealtimeFixture {
  qc: QueryClient;
  wrapper: ComponentType<{ children: ReactNode }>;
  channel: jest.Mock;
  on: jest.Mock;
  subscribe: jest.Mock;
  removeChannel: jest.Mock;
  getHandler: () => Handler | null;
}

function setup(): RealtimeFixture {
  const { supabase } = require('../../src/lib/supabase');
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  const removeChannel = jest.fn();
  const subscribe = jest.fn(() => ({ unsubscribe: jest.fn() }));
  let capturedHandler: Handler | null = null;
  const on = jest.fn().mockImplementation((_event, _config, handler) => {
    capturedHandler = handler;
    return { subscribe };
  });
  const channel = jest.fn().mockReturnValue({ on });
  jest.spyOn(supabase, 'channel').mockImplementation(channel as never);
  jest.spyOn(supabase, 'removeChannel').mockImplementation(removeChannel as never);
  ...
}
```

From `tests/submissions/useTodaySubmissionRealtime.test.tsx` (lines 30-42 — expo-router useFocusEffect mock):
```ts
jest.mock('expo-router', () => {
  const React = require('react');
  return {
    useFocusEffect: (effect: () => void | (() => void)) => {
      React.useEffect(() => {
        const cleanup = effect();
        return typeof cleanup === 'function' ? cleanup : undefined;
      }, [effect]);
    },
  };
});
```

From `supabase/tests/get_pending_review_count.sql` (the JWT-claims + role-set + reset pattern P4 pgTAP files mirror):
```sql
select set_config(
  'request.jwt.claims',
  '{"sub":"11111111-1111-1111-1111-111111111111","role":"authenticated"}',
  true
);
set local role authenticated;
select is(...);
reset role;
select set_config('request.jwt.claims', NULL, true);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract channel-chain mock helper to tests/_helpers/mockSupabaseChannel.ts</name>
  <files>tests/_helpers/mockSupabaseChannel.ts</files>
  <read_first>
    - tests/submissions/useTodaySubmissionRealtime.test.tsx (lines 1-100, the existing inline pattern that this helper extracts)
    - 04-PATTERNS.md §"Realtime Channel Lifecycle" (the 5 invariants the helper must preserve)
    - 04-RESEARCH.md §Pattern 3 (verbatim shape — lines 391-456)
  </read_first>
  <action>
Create `tests/_helpers/mockSupabaseChannel.ts` exporting a single function `setupChannelMock(supabase: { channel: unknown; removeChannel: unknown })` that returns the same fixture shape currently inlined in `useTodaySubmissionRealtime.test.tsx` lines 51-91.

The helper MUST:
1. Define and export the same `RealtimePayload` and `Handler` types verbatim from the existing test file (lines 48-49).
2. Define and export the `RealtimeFixture` interface verbatim (lines 51-59) plus an additional `channelName: () => string | null` accessor that returns the FIRST argument passed to `supabase.channel(...)` so callers can assert channel names like `group-lb:abc-123`.
3. Implement `setupChannelMock` so it calls `jest.spyOn(supabase, 'channel')` and `jest.spyOn(supabase, 'removeChannel')` exactly as the source pattern does (lines 75-76). The `on` mock captures the handler so tests can drive synthetic payloads via `getHandler()`. The `subscribe` mock returns `{ unsubscribe: jest.fn() }`.
4. Track the channel name: change `channel = jest.fn().mockReturnValue({ on })` to `channel = jest.fn((name: string) => { capturedChannelName = name; return { on }; })` and add `getChannelName: () => capturedChannelName` to the fixture.

Add a single doc comment at the top citing the source: `// Extracted from tests/submissions/useTodaySubmissionRealtime.test.tsx (lines 51-91) for reuse across Phase 4 Realtime hook tests (D-15, D-20, D-21). Per 04-PATTERNS.md §"Realtime Channel Lifecycle".`

Do NOT modify the existing `useTodaySubmissionRealtime.test.tsx` — it can keep its inlined helper. The new helper is purely additive for new tests.

ESLint discipline: file must compile under the project's strict TS config (no `any`; use `unknown` + casts where needed — mirror the source pattern's `as never` casts on `'postgres_changes'` literal).
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test --listTests tests/_helpers/mockSupabaseChannel.ts 2>/dev/null || pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/_helpers/mockSupabaseChannel.ts` exists
    - `grep -c "export function setupChannelMock" tests/_helpers/mockSupabaseChannel.ts` returns 1
    - `grep -c "jest.spyOn" tests/_helpers/mockSupabaseChannel.ts` returns 2 (one for channel, one for removeChannel)
    - `grep -c "capturedChannelName\|getChannelName" tests/_helpers/mockSupabaseChannel.ts` returns at least 2
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Helper file created with the extracted channel-chain mock; typecheck green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add LayoutAnimation Jest mock to jest.setup.ts</name>
  <files>jest.setup.ts</files>
  <behavior>
    - LeaderboardRow component test (Wave 2) can call code that invokes `LayoutAnimation.configureNext(...)` without throwing
    - Existing tests continue to pass (additive change only)
  </behavior>
  <read_first>
    - jest.setup.ts (the entire file — to find a place to add the mock alongside the other RN-native mocks like `expo-blur`, `react-native-reanimated`)
    - 04-UI-SPEC.md §"Interaction Contracts" lines 783-784 (the LeaderboardRow row reorder + new-item slide-in animations driven by `LayoutAnimation.easeInEaseOut`)
  </read_first>
  <action>
Append to `jest.setup.ts` a `jest.mock('react-native', ...)` partial mock that preserves the real react-native exports while replacing `LayoutAnimation` with a no-op mock:

```ts
// react-native LayoutAnimation — Phase 4 hooks call configureNext() to animate
// leaderboard row reorders and feed prepends. The native bridge is unavailable
// in jest, so stub the API. All other react-native exports remain real.
jest.mock('react-native', () => {
  const actual = jest.requireActual('react-native');
  return {
    ...actual,
    LayoutAnimation: {
      configureNext: jest.fn(),
      create: jest.fn((duration: number, type: string, prop: string) => ({
        duration,
        update: { type, property: prop },
        create: { type, property: prop },
        delete: { type, property: prop },
      })),
      Types: { easeInEaseOut: 'easeInEaseOut', linear: 'linear', spring: 'spring' },
      Properties: { opacity: 'opacity', scaleXY: 'scaleXY' },
      easeInEaseOut: 'easeInEaseOut',
    },
  };
});
```

Place the block AFTER the `react-native-reanimated` mock (line ~209) and BEFORE the final `// end of jest.setup.ts` (or end-of-file). This ordering matches the existing convention: native-only API mocks come after library mocks.

**Do NOT replace the existing `jest.mock('react-native', () => ({ AppState: ... }))` shim used inside individual test files** — those are scoped to test files (e.g. `useTodaySubmissionRealtime.test.tsx` line 21-23) and override this global mock per-file. Adding the global mock is additive; per-file mocks continue to win.
  </action>
  <verify>
    <automated>pnpm test --testPathPattern=tests/groups/groupDetailScreen 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "LayoutAnimation" jest.setup.ts` returns at least 3 (the comment + the mock block + the Types/Properties references)
    - `pnpm test --testPathPattern=tests/groups/groupDetailScreen` exits 0 (existing test suite still green — no regression from the new RN mock)
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>jest.setup.ts has a LayoutAnimation mock that preserves real react-native exports; existing tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create 4 RED pgTAP scaffolds for Phase 4 DB layer</name>
  <files>
    supabase/tests/handle_submission_approval_streak.sql,
    supabase/tests/handle_submission_approval_idempotency.sql,
    supabase/tests/phase4_rpc_permissions.sql,
    supabase/tests/phase4_rpc_correctness.sql
  </files>
  <behavior>
    - All 4 files exist and run via `supabase test db`
    - All 4 files FAIL initially (the trigger body is still the no-op stub from 0001; the 4 new RPCs do not yet exist) — this is the expected RED state
    - Each file uses the verbatim JWT-claims + `set local role authenticated` + `reset role` + `set_config(... NULL ...)` pattern from `supabase/tests/get_pending_review_count.sql`
  </behavior>
  <read_first>
    - supabase/tests/get_pending_review_count.sql (JWT claims pattern — lines 55-139)
    - supabase/tests/get_pending_review_queue.sql (multi-row seed + ordering + LEFT-JOIN edge cases — lines 60-100)
    - supabase/tests/submissions_admin_immutable.sql (multi-persona seed + raw-UPDATE trigger coverage — lines 17-95)
    - 04-RESEARCH.md §Appendix §5 ("pgTAP coverage, concrete file split" — lines 1031-1042)
    - 04-VALIDATION.md §"Wave 0 Requirements"
    - 04-CONTEXT.md D-02 (streak recurrence branches), D-03 (idempotency basis)
  </read_first>
  <action>
Create 4 pgTAP files. Each follows the standard `begin; select plan(N); ...; select * from finish(); rollback;` structure used in every existing `supabase/tests/*.sql` file.

**File 1: `supabase/tests/handle_submission_approval_streak.sql`** — D-02 four-branch coverage + D-01 server-derived local_date:

Seed (use the multi-persona pattern from `submissions_admin_immutable.sql` lines 17-95, with **two groups in different timezones** to prove D-01):
- One admin (`11111111-1111-1111-1111-111111111111`) + one member (`44444444-4444-4444-4444-444444444444`) per group
- Group A: `aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa`, photo, `America/Los_Angeles`
- Group B: `bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb`, photo, `America/New_York`
- 4+ pending submissions for the member at distinct local_dates (today, today-1, today-2, today-7) — the seeds drive each branch

Test plan(8):
1. NULL branch — initial state has `last_rolled_date IS NULL`; `update submissions set status='approved' where id=<today-7 sub>` → assert `current_streak = 1, points = 1, longest_streak = 1, last_rolled_date = <today-7>`
2. Consecutive branch — approve `<today-6>` after 7-day branch; assert `current_streak = 2, points = 2, longest_streak = 2`
3. Gap branch — leave a gap, approve `<today-2>`; assert `current_streak = 1` (reset), `points = 3`, `longest_streak = 2` (preserved!)
4. longest_streak preserved — approve `<today-1>` then `<today>`; assert `current_streak = 3, longest_streak = 3` (the new high-water)
5. D-01 cross-tz proof — approve a submission in Group B (different tz), assert that group's `last_rolled_date` matches the **submission's local_date** (NOT now()::date or the caller's wall clock)
6. Points monotonic — assert `points = 5` after the 5 approvals
7. Other group untouched — assert Group A's counter columns are unaffected by Group B's approvals
8. Trigger fires inside transaction — verify the AFTER UPDATE WHEN clause: a status flip from `pending → approved` increments the counter; a re-issuing of the same `update submissions set status='approved' where status='approved'` does NOT increment again (covered more thoroughly in file 2)

Use raw `update public.submissions set status = 'approved' where id = ...` (NOT `review_submission` RPC) — file 4 covers the RPC chain; this file isolates the trigger body.

**File 2: `supabase/tests/handle_submission_approval_idempotency.sql`** — D-03 + Pitfall P4-B concurrency:

Seed: 1 admin + 1 member, group `America/Los_Angeles`, two pending submissions on consecutive dates (today-1, today).

Test plan(5):
1. WHEN clause blocks no-op — approve once, points = 1; re-issue `update submissions set status='approved' where status='approved' and id=<sub>` (no-op since status didn't change); assert points still = 1
2. 0003 admin-immutable blocks status flip-back — `update submissions set status='pending' where id=<approved-sub>` raises (admin may not modify status reverse); assert no change to group_members
3. AFTER UPDATE WHEN no-fire on UPDATE that doesn't touch status — `update submissions set caption='changed' where id=<approved-sub>`; assert points unchanged
4. Concurrency / two adjacent days — approve `<today-1>` then `<today>` serially in the same test (single connection — pgTAP is single-session, but two sequential triggers prove the streak math reads the just-committed `last_rolled_date`); assert `points = 2, current_streak = 2, longest_streak = 2`
5. Direct update of group_members.points blocked (forward reference to file 3) — `set local role authenticated` with admin claims, `update group_members set points = 9999 where ...`; assert raises (this verifies the column-allowlist trigger from 04-02 once it ships; in Wave 0 RED state this assertion fails)

**File 3: `supabase/tests/phase4_rpc_permissions.sql`** — 5-persona × 4-RPC permission matrix:

Seed: admin (`11111111...`), member (`44444444...`), stranger (`99999999...`), ex-member (`88888888...` — was in group, now removed), one group `aaaaaaaa...`.

Test plan(~16):
- For each of 4 RPCs (`get_pending_today`, `get_missed_yesterday`, `get_today_posted_count`, `get_group_leaderboard`):
  - Anon (no `request.jwt.claims`) → typed-error RPCs raise `not_authenticated` via `throws_ok`; lenient `get_today_posted_count` returns `is(..., 0, ...)`
  - Stranger (authenticated, not in group) → typed-error RPCs raise `not_member`; lenient returns 0
  - Member (authenticated, in group) → returns successfully (any non-null result)
  - Admin (authenticated, admin of group) → returns successfully
- Plus one assertion: direct `update group_members set points = 9999` from `set local role authenticated` admin → raises `'group_members counter columns are server-managed'` (verifies the column-allowlist trigger from 04-02 + the `pg_trigger_depth() > 1` bypass in Appendix §3)

Use `throws_ok($$select ... $$, 'P0001', '<typed_code>', '<description>')` per the precedent in `supabase/tests/get_pending_review_queue.sql` lines 85-90.

**File 4: `supabase/tests/phase4_rpc_correctness.sql`** — RPC happy-path + DST + cross-tz:

Seed: 4 members in one group (`America/Los_Angeles`); 1 has approved submission for today, 1 has rejected for today, 1 has pending for today, 1 has nothing for today; 2 of those 4 have approved for yesterday.

Test plan(~10):
1. `get_pending_today(group)` returns exactly the 1 member with NO submission for today (alphabetical by display_name)
2. `get_pending_today` excludes members with rejected/pending submissions for today
3. `get_missed_yesterday(group)` returns exactly the 2 members without an APPROVED row for yesterday
4. `get_missed_yesterday` treats pending and rejected as missing (not just no-row)
5. `get_today_posted_count(group)` returns 1 (only approved counts)
6. `get_group_leaderboard(group)` returns 4 rows ordered by points DESC, current_streak DESC
7. `get_group_leaderboard` includes `display_name`, `avatar_path`, `updated_at`, `points`, `current_streak`, `longest_streak`, `last_rolled_date`, `joined_at`
8. DST edge: create a second group in `America/New_York`, set up data around the spring-forward day; assert `(now() AT TIME ZONE 'America/New_York')::date - 1` returns the correct calendar day (just verify the RPC doesn't error and returns expected count)
9. Cross-tz isolation — Group B's RPCs do not return Group A's members
10. `get_pending_today` order is alphabetical by display_name (assert via `results_eq`)

**Every file MUST end with `select * from finish(); rollback;`.** Every file uses the JWT claims + `set local role authenticated` + `reset role` + `select set_config('request.jwt.claims', NULL, true);` pattern verbatim from `get_pending_review_count.sql` lines 58-72.

These files MUST fail when run via `supabase test db` in Wave 0 (the trigger stub is no-op; the 4 RPCs don't exist; the column-allowlist trigger doesn't exist). 04-02 makes them green.
  </action>
  <verify>
    <automated>ls supabase/tests/handle_submission_approval_streak.sql supabase/tests/handle_submission_approval_idempotency.sql supabase/tests/phase4_rpc_permissions.sql supabase/tests/phase4_rpc_correctness.sql && supabase test db 2>&1 | grep -E "FAIL|not ok|PASS|ok " | head -10</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist
    - `grep -c "select plan" supabase/tests/handle_submission_approval_streak.sql` returns 1
    - `grep -c "select plan" supabase/tests/handle_submission_approval_idempotency.sql` returns 1
    - `grep -c "select plan" supabase/tests/phase4_rpc_permissions.sql` returns 1
    - `grep -c "select plan" supabase/tests/phase4_rpc_correctness.sql` returns 1
    - `grep -c "request.jwt.claims" supabase/tests/phase4_rpc_permissions.sql` is at least 4 (one per persona)
    - `grep -c "throws_ok" supabase/tests/phase4_rpc_permissions.sql` is at least 4
    - `grep -c "America/New_York\|America/Los_Angeles" supabase/tests/phase4_rpc_correctness.sql` is at least 2
    - `supabase test db` exits non-zero (RED state expected — these tests fail until 04-02 ships)
  </acceptance_criteria>
  <done>4 RED pgTAP files committed; supabase test db reports failures in exactly the expected new files (and no regressions in existing tests).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Create 5 RED Jest scaffolds for Phase 4 hooks</name>
  <files>
    tests/features/groups/useGroupLeaderboard.test.tsx,
    tests/features/groups/useGroupLeaderboardRealtime.test.tsx,
    tests/features/submissions/useGroupFeed.test.tsx,
    tests/features/groups/useGroupTombstones.test.tsx,
    tests/features/groups/useGroupSocialCounts.test.tsx
  </files>
  <behavior>
    - Each test file imports the not-yet-existing hook from its production path (the import will fail until 04-03 ships → expected RED)
    - The Realtime test file imports `setupChannelMock` from Task 1
    - Tests assert the contract: queryKey shape, RPC name called, returned data shape, channel name, filter, setQueryData behavior
  </behavior>
  <read_first>
    - tests/submissions/useTodaySubmissionRealtime.test.tsx (verbatim shape — copy the QueryClientProvider wrapper, the expo-router mock, and the AppState mock)
    - tests/submissions/useReviewQueue.test.tsx (RPC mock pattern — `jest.spyOn(supabase, 'rpc').mockResolvedValueOnce({ data: ..., error: null })`)
    - 04-PATTERNS.md §"Pattern Assignments — Data Hooks" (the 5 hook contracts)
    - 04-RESEARCH.md §Appendix §6 (the 4-hook table — queryKey shapes)
    - 04-VALIDATION.md §"Per-Task Verification Map" (rows 5-9)
    - tests/_helpers/mockSupabaseChannel.ts (created in Task 1 — used by the Realtime test)
  </read_first>
  <action>
Create 5 RED Jest test files. Each follows the existing pattern from `tests/submissions/useReviewQueue.test.tsx` (for read hooks) or `tests/submissions/useTodaySubmissionRealtime.test.tsx` (for the Realtime hook).

**Common boilerplate** at the top of each file (mirror `useTodaySubmissionRealtime.test.tsx` lines 18-42):
```ts
process.env.EXPO_PUBLIC_SUPABASE_URL = 'https://test.supabase.co';
process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY = 'test-anon-key';

jest.mock('react-native', () => ({
  AppState: { addEventListener: jest.fn() },
}));

jest.mock('expo-router', () => {
  const React = require('react');
  return {
    useFocusEffect: (effect: () => void | (() => void)) => {
      React.useEffect(() => {
        const cleanup = effect();
        return typeof cleanup === 'function' ? cleanup : undefined;
      }, [effect]);
    },
  };
});
```

Each test file uses `renderHook` from `@testing-library/react-native` + a `QueryClientProvider` wrapper + `jest.spyOn(supabase, 'rpc')` or `jest.spyOn(supabase, 'from')` per the hook's data path.

**File 1: `tests/features/groups/useGroupLeaderboard.test.tsx`** (LB-01 — 4 tests):
- imports `useGroupLeaderboard` from `../../../src/features/groups/useGroupLeaderboard` (will fail Wave 0 — no such file yet)
- Test 1: `does not query when groupId is undefined` — assert `supabase.rpc` not called
- Test 2: `calls supabase.rpc('get_group_leaderboard', { p_group_id })` — `jest.spyOn(supabase, 'rpc').mockResolvedValueOnce({ data: [...], error: null })`; render hook; `await waitFor` data; assert `rpc` was called with the expected args
- Test 3: `returns rows in the order the RPC delivers them (server-side sort)` — RPC returns 3 rows already ordered; hook must NOT re-sort
- Test 4: `surfaces error.message` — RPC returns `{ data: null, error: { message: 'not_member' } }`; assert hook enters error state with that message

**File 2: `tests/features/groups/useGroupLeaderboardRealtime.test.tsx`** (LB-02 — 5 tests):
- imports `useGroupLeaderboardRealtime` from `../../../src/features/groups/useGroupLeaderboardRealtime` (will fail Wave 0)
- imports `setupChannelMock` from `../../_helpers/mockSupabaseChannel`
- Test 1: `subscribes with channel name 'group-lb:{groupId}'` — render; assert `getChannelName() === 'group-lb:abc-123'`
- Test 2: `subscribes with filter 'group_id=eq.{groupId}' on table 'group_members' event 'UPDATE'` — assert the `on` mock was called with config matching `{ event: 'UPDATE', schema: 'public', table: 'group_members', filter: 'group_id=eq.abc-123' }`
- Test 3: `does NOT subscribe when groupId is undefined` — assert `channel` mock not called
- Test 4: `patches the leaderboard cache via setQueryData on UPDATE event` — pre-populate `qc.setQueryData(['groupLeaderboard', 'abc-123'], [...])` with a leader row; trigger handler with synthetic payload `{ new: { user_id: 'X', points: 99, current_streak: 5, longest_streak: 7, last_rolled_date: '2026-05-08' } }`; assert the cached row was updated AND the array re-sorted by points DESC then current_streak DESC
- Test 5: `cleans up channel on unmount via removeChannel` — render then unmount; assert `removeChannel` was called once

**File 3: `tests/features/submissions/useGroupFeed.test.tsx`** (FEED-01 — 4 tests):
- imports `useGroupFeed` from `../../../src/features/submissions/useGroupFeed`
- Test 1: `does not query when groupId is undefined OR today is undefined`
- Test 2: `calls supabase.from('submissions').select(...).eq('group_id').eq('local_date', today).eq('status', 'approved')` — chain the `.eq().eq().eq().order()` mock and assert each call. Use `jest.spyOn(supabase, 'from').mockReturnValue(<chain>)`.
- Test 3: `orders by created_at descending` — assert `.order('created_at', { ascending: false })` was called
- Test 4: `maps embedded profile fields into flat shape` — RPC returns `[{ id, user_id, caption, ..., profiles: { display_name, avatar_path, updated_at } }]`; hook returns flat row with `display_name`, `avatar_path`, `updated_at` extracted

**File 4: `tests/features/groups/useGroupTombstones.test.tsx`** (FEED-02/03 — 4 tests):
- imports `useGroupTombstones` from `../../../src/features/groups/useGroupTombstones`
- Test 1: `does not query when groupId is undefined`
- Test 2: `today scope calls supabase.rpc('get_pending_today', { p_group_id })` — assert exact RPC name + args
- Test 3: `yesterday scope calls supabase.rpc('get_missed_yesterday', { p_group_id })` — assert exact RPC name + args
- Test 4: `returns BOTH pendingToday and missedYesterday in a combined object` — render the combined hook (per CONTEXT D-07 separation: two queries, one returned object); assert `data` has shape `{ pendingToday: [...], missedYesterday: [...] }` OR `{ today: [...], yesterday: [...] }` (whichever the hook author picks; the test asserts the documented contract)

**File 5: `tests/features/groups/useGroupSocialCounts.test.tsx`** (D-13/D-15 — 3 tests):
- imports `useGroupSocialCounts` from `../../../src/features/groups/useGroupSocialCounts`
- Test 1: `does not query when groupId is undefined`
- Test 2: `calls supabase.rpc('get_today_posted_count', { p_group_id })` — assert exact RPC name + args
- Test 3: `returns the integer count from the RPC (defaulting to 0 when null)` — RPC returns `{ data: 4, error: null }` → hook returns `4`; RPC returns `{ data: null, error: null }` → hook returns `0`

**Every test file MUST fail to compile/run in Wave 0** (the imports point at production files that don't exist yet). 04-03 creates those files, flipping each test green individually.
  </action>
  <verify>
    <automated>pnpm test --testPathPattern="tests/features/(groups|submissions)/(useGroupLeaderboard|useGroupLeaderboardRealtime|useGroupFeed|useGroupTombstones|useGroupSocialCounts)" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist
    - `grep -c "renderHook\|QueryClientProvider" tests/features/groups/useGroupLeaderboard.test.tsx` is at least 2
    - `grep -c "setupChannelMock" tests/features/groups/useGroupLeaderboardRealtime.test.tsx` is at least 1
    - `grep -c "group-lb:" tests/features/groups/useGroupLeaderboardRealtime.test.tsx` is at least 1
    - `grep -c "get_today_posted_count" tests/features/groups/useGroupSocialCounts.test.tsx` is at least 1
    - `grep -c "get_pending_today\|get_missed_yesterday" tests/features/groups/useGroupTombstones.test.tsx` is at least 2
    - `pnpm test --testPathPattern="tests/features/(groups|submissions)/(useGroupLeaderboard|useGroupLeaderboardRealtime|useGroupFeed|useGroupTombstones|useGroupSocialCounts)"` exits non-zero (RED — production hooks don't exist yet; this is the expected Wave 0 state)
  </acceptance_criteria>
  <done>5 RED Jest files committed; the import errors confirm RED state for Waves 2 to drive against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Test infrastructure only — no new trust boundaries; threat-model lives in 04-02 (the migration plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-W0-01 | Information Disclosure | Wave 0 test seeds with placeholder JWT claims | accept | All test seeds use deterministic UUIDs (`11111111-...`), no real PII. Pattern matches existing `supabase/tests/*.sql` precedent. Tests run in `begin/rollback` blocks against a local Supabase instance. |
</threat_model>

<verification>
Run `pnpm typecheck` — exits 0.
Run `pnpm test --testPathPattern="tests/features/(groups|submissions)/(useGroupLeaderboard|useGroupLeaderboardRealtime|useGroupFeed|useGroupTombstones|useGroupSocialCounts)"` — exits non-zero with import-error messages naming the 5 not-yet-created production files.
Run `supabase test db` — exits non-zero with failures in exactly the 4 new pgTAP files (and zero regressions in existing pgTAP files; check via `supabase test db 2>&1 | grep "not ok" | wc -l` returning a count consistent with ~29 expected failing assertions).
Run existing test smoke check — `pnpm test --testPathPattern=tests/groups/groupDetailScreen` — exits 0 (no regression from the new LayoutAnimation mock).
</verification>

<success_criteria>
- 1 new helper file (`tests/_helpers/mockSupabaseChannel.ts`) committed
- jest.setup.ts has a LayoutAnimation partial mock without breaking existing tests
- 4 RED pgTAP scaffolds committed and visible to `supabase test db`
- 5 RED Jest scaffolds committed and visible to `pnpm test`
- `pnpm typecheck` green
- Every Wave 1+ task in 04-02..04-06 has at least one matching RED test to drive against (Nyquist compliance)
</success_criteria>

<output>
After completion, create `.planning/phases/04-social-surfaces/04-01-SUMMARY.md` capturing:
- Test count breakdown (4 pgTAP files / N assertions, 5 Jest files / M tests)
- Confirmed RED state (which assertions fail, naming the missing production functions/files)
- Channel-chain helper API surface (the function signature) so downstream plans reference it consistently
</output>
