---
status: testing
phase: 03.1-p3-polish-realtime-hardening
source: [03.1-01-SUMMARY.md, 03.1-02-SUMMARY.md, 03.1-03-SUMMARY.md]
started: 2026-05-10T04:35:00Z
updated: 2026-05-10T04:35:00Z
---

## Current Test

number: 1
name: CK-01 Badge auto-increments within ~2s on Device A (re-test after Issue B fix `211916e`)
expected: |
  Issue B (phantom Capture tab) fixed inline. Rebuild + reinstall the dev client on
  BOTH devices, then re-run CK-01: Device A on group-detail, Device B submits a fresh
  photo/video. Within ~2s, Device A's "Pending Review (N)" badge increments by 1 and
  Device B post-submit navigation should now return cleanly (no phantom tab).
awaiting: user response

## Tests

### 1. CK-01 Badge auto-increments within ~2s on Device A
expected: Two iOS devices on the same WiFi. Device A (admin) on group-detail. Device B submits a fresh photo/video. Within ~2s, Device A's "Pending Review (N)" badge increments by 1 — no manual back-out + re-enter required.
result: issue
reported: "submitted video to daily push ups group, device A did not update, and something strange happened with device b. On pressing submit, device B navigated to a new 'capture' tab, dont know where this came from. The tab is just a spinning loading sign so i dont even know whats in it"
severity: blocker
notes: "Two distinct regressions surfaced by this single test — see Gaps section."

### 2. CK-02 Queue list auto-prepends within ~2s on Device A
expected: Device A (admin) on `/groups/[id]/review` swipe queue. Device B submits another fresh submission. Within ~2s, the new pending row prepends to Device A's queue list with the correct member display_name + avatar — no manual refresh.
result: [pending]

### 3. CK-03 Count decrements after admin review action
expected: Device A approves or rejects one submission from the swipe queue, then navigates back to group-detail. The "Pending Review (N)" badge immediately reflects the new (decremented) count via the same Realtime channel.
result: [pending]

### 4. CK-04 Non-admin Metro log shows zero `review-queue:` subscribe entries
expected: On Device B (non-admin member) with Metro logs open (Expo DevTools → Logs), navigate to the group's group-detail screen. Metro log shows ZERO entries containing `review-queue:` — proves the admin gate at the mount point is honored and non-admins never open the Realtime subscription.
result: [pending]

### 5. CK-05 Capture screen slides up from bottom (modal animation)
expected: Restart Metro for a fresh bundle. Build/install the latest dev client. From group-detail, tap the Capture entry point. The capture screen slides UP from the bottom of the screen with a smooth animation; the tab bar is fully covered while the modal is presented. Does NOT appear instantly inline or slide-from-right.
result: [pending]

### 6. CK-06 Downward swipe does NOT dismiss the capture modal
expected: With the capture modal presented, attempt a downward swipe gesture from the top of the screen (typical iOS modal-dismiss). The modal is NOT dismissed — the gesture is absorbed/ignored. The modal closes ONLY via the explicit Cancel button or after a successful submission.
result: [pending]

### 7. CK-07 Capture flow regression check (photo + video → submit → dismiss)
expected: Open the capture modal, run the full happy-path of recording + submitting both a PHOTO and a VIDEO. Confirm the dismiss happens via the explicit Cancel/Done path and the underlying submission flow still reaches the pending-review state. Functionally identical to pre-modal-restoration behavior — only the presentation/animation has changed.
result: [pending]

### 8. CK-08 No `sceneStyleInterpolator` runtime crash on capture-modal open
expected: Open the capture modal repeatedly across cold starts and warm navigations. No runtime crash, no red-screen error referencing `sceneStyleInterpolator` (the original regression that commit 298fe64 was patching when it stripped the modal options).
result: [pending]

### 9. CK-09 Shutter — photo variant renders Feather camera glyph in theme-primary circle
expected: Navigate to the Capture screen for a PHOTO group. Shutter shows a 72pt circle: 4pt white outer ring + 52pt theme-primary (yellow) inner circle with a white Feather `camera` glyph centered. Should NOT read as "blocky" or "square inside a circle". Tap fires onPress (preview/upload flow proceeds).
result: [pending]

### 10. CK-10 Shutter — video-idle variant renders Feather video glyph in theme-primary circle
expected: Navigate to Capture for a VIDEO group (or toggle to video mode). Shutter matches the photo variant geometry but the inner circle now contains a Feather `video` (filmstrip) glyph centered. Clean circle-inside-circle, no nested square, no 90-degree corners.
result: [pending]

### 11. CK-11 Shutter — video-recording variant renders clean ring + filled red circle
expected: From video-idle, tap the Shutter to start recording. PASS: 72pt outer ring (4pt white border with 1.4s opacity pulse 0.85↔1.0) + 52pt SOLID FILLED RED CIRCLE inside it. NO inner glyph. NO inner white square. NO 90-degree corners. Reads as "ring + red dot". Outer ring opacity pulse is visibly animating. Tap again returns to video-idle (Feather video glyph).
result: [pending]

### 12. CK-12 Shutter accessibility labels announced via VoiceOver/TalkBack
expected: Enable VoiceOver (iOS) or TalkBack (Android). Focus on the Shutter at each variant. Should announce: "Take photo" (photo), "Start recording" (video-idle), "Stop recording" (video-recording).
result: [pending]

## Summary

total: 12
passed: 0
issues: 1
pending: 11
skipped: 0

## Gaps

- truth: "Device A admin pending-review badge auto-increments within ~2s when Device B submits a fresh photo/video"
  status: failed
  reason: "User reported: device A did not update when device B submitted to 'daily push ups' group"
  severity: blocker
  test: 1
  surface: Plan 03.1-01 — `useReviewQueueRealtime` hook
  artifacts: [src/features/submissions/useReviewQueueRealtime.ts, app/(app)/groups/[id]/index.tsx]
  hypotheses:
    - "Channel subscription not actually opening on Device A — admin gate may be misfiring (`isAdmin` evaluating false even for the admin)"
    - "Channel subscription opens but `postgres_changes` event never arrives — RLS on the publication denying SELECT to admin's auth, or `replica identity full` not actually applied to remote DB"
    - "Event arrives but `qc.invalidateQueries` is keyed wrong — `usePendingReviewCount`'s queryKey may not be exactly `['pendingReviewCount', groupId]`"
    - "Hook mounts but `useFocusEffect` cleanup teardown is firing too aggressively and removing the channel before the event arrives"
  missing: []

- truth: "Tapping submit on the Capture screen returns the user to the prior screen via the existing post-submit navigation; no new tab appears in the tab bar"
  status: resolved
  reason: "User reported: on pressing submit, device B navigated to a new 'capture' tab in the tab bar — a tab that did not exist before this phase. The tab shows only a spinning loader."
  severity: blocker
  test: 1
  surface: Plan 03.1-02 — nested Stack layout regression
  artifacts: [app/(app)/_layout.tsx, app/(app)/capture/_layout.tsx]
  resolved_by:
    commit: 211916e
    file: app/(app)/_layout.tsx
    change: |
      Re-targeted the parent Tabs.Screen registration from `name="capture/[groupId]"` to
      `name="capture"`. Once `app/(app)/capture/_layout.tsx` exists, Expo Router treats the
      folder as a single nested route group — `href: null` had to key against the folder
      name, not the inner [groupId] file, otherwise the new group surfaced as a phantom tab.
    verification: "tsc --noEmit clean; jest 57/57 suites, 341/341 tests"
  missing: []

