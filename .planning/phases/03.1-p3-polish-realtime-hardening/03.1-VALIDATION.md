---
phase: 03.1
slug: p3-polish-realtime-hardening
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-09
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `jest` `^29.7.0` + `jest-expo` `~55.0.0` (Realtime hook + Shutter component tests). No new pgTAP — phase ships zero schema/RPC changes. |
| **Config file** | `jest.config.js` (existing) — no new config needed |
| **Quick run command** | `pnpm test --findRelatedTests <changed-files>` |
| **Full suite command** | `pnpm test` (Jest only — `supabase test db` baseline unchanged) |
| **Estimated runtime** | ~30 seconds (Jest full suite, P3 baseline 240/240 green) |

---

## Sampling Rate

- **After every task commit:** Run `pnpm test --findRelatedTests <changed-files>`
- **After every plan wave:** Run `pnpm test`
- **Before `/gsd-verify-work`:** `pnpm test` must be green; manual UAT receipts captured for the modal-presentation and cross-device-realtime items (mirror P3 CK-1 + CK-5 shape)
- **Max feedback latency:** ~30 seconds (Jest), ~2 minutes for cross-device UAT

---

## Per-Task Verification Map

> Phase has no requirement IDs — REQUIREMENTS.md is unchanged in this phase. The map tracks deliverables (D-01..D-10 from CONTEXT.md), not REQ-IDs.

| Task ID | Plan | Wave | Deliverable Ref | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-----------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 03.1-01-XX | 01 | 1 | D-01..D-04 (`useReviewQueueRealtime` hook) | T-Info-Disclosure (admin gate + RLS + RPC raise) | Non-admin call site → no subscription; channel filter `group_id=eq.{groupId}` exact match; `useFocusEffect` cleanup via `removeChannel` | hook unit (channel-chain mock) | `pnpm test --testPathPattern useReviewQueueRealtime` | ❌ W0 (`tests/submissions/useReviewQueueRealtime.test.tsx`) | ⬜ pending |
| 03.1-01-XX | 01 | 1 | D-04 admin-only mount on group-detail | T-Info-Disclosure | `isAdmin ? id : undefined` short-circuits subscription on group-detail | hook unit + manual UAT (Metro log on non-admin device) | `pnpm test --testPathPattern useReviewQueueRealtime` | ❌ W0 (same file) | ⬜ pending |
| 03.1-01-XX | 01 | 1 | D-01..D-04 cross-device auto-update | — | Device B submits → Device A badge + queue update within ~2s without manual back-out | manual UAT (mirror P3 CK-5 receipt) | UAT walkthrough on iOS dev client | ✅ existing UAT pattern | ⬜ pending |
| 03.1-02-XX | 02 | 1 | D-05..D-07 modal presentation | — | Capture screen presents with slide-from-bottom; gesture-dismiss disabled; tab bar covered | manual UAT (mirror P3 CK-1 receipt) | UAT walkthrough on iOS dev client | ✅ existing UAT pattern | ⬜ pending |
| 03.1-03-XX | 03 | 1 | D-08..D-10 Shutter visual rewrite | — | Renders Feather `camera` glyph on `variant='photo'`; Feather `video` glyph on `variant='video-idle'`; clean filled red circle on `variant='video-recording'`; outer-ring 1.4s opacity pulse retained | component snapshot + render assertion | `pnpm test --testPathPattern Shutter` | ❌ W0 (`tests/components/Shutter.test.tsx`) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

> Specific Task IDs (`-01-01`, `-01-02`, etc.) are assigned by the planner during plan generation. The map will be re-keyed during plan creation.

---

## Wave 0 Requirements

- [ ] `tests/submissions/useReviewQueueRealtime.test.tsx` — covers the new hook. Reference shape: `tests/submissions/useTodaySubmissionRealtime.test.tsx` (1:1 — channel-chain mock + narrowing predicate). Channel-chain mock helper already shipped at `tests/_helpers/mockSupabaseChannel.ts` (P4-01).
- [ ] `tests/components/Shutter.test.tsx` — covers the visual rewrite. Existing repo grep returns zero hits for `Shutter.test`; new file. Reference shape: any existing primitive component test under `tests/components/`.
- Framework install: **none** — `jest` + `jest-expo` already in `devDependencies`.

---

## Manual-Only Verifications

| Behavior | Deliverable Ref | Why Manual | Test Instructions |
|----------|-----------------|------------|-------------------|
| Capture modal presents with slide-from-bottom + gesture disabled | D-05..D-07 | Visual + gesture interaction; not assertable from Jest | iOS dev client: tap a group's "Capture" entry → screen slides from bottom; downward swipe does NOT dismiss; tap Cancel returns to caller. Repeat on Android. |
| Cross-device pending-review Realtime auto-update | D-01..D-04 | Two devices required; mirrors P3 CK-5 + P4 CK-05 reproduction | Device A (admin) opens group-detail; Device B (member) submits a fresh photo via capture flow. Within ~2s, Device A's "Pending Review (N)" badge increments AND `/groups/[id]/review` queue list prepends the new row WITHOUT manual back-out. Repeat the inverse for the count-decrement path: admin approves on A → B's pending count refreshes. |
| Non-admin does NOT receive Realtime events through this hook | D-04 admin gate | Negative test — Metro log inspection | Member device on the same group: open group-detail; observe Metro logs — should see ZERO `subscribe` calls for `review-queue:{groupId}:*`. |
| Shutter visual reads cleanly at all three variants | D-08..D-10 | Visual judgment — needs human eyes | iOS dev client + Android: enter capture; observe `photo` (camera glyph), tap toggle to `video-idle` (video glyph), start recording → `video-recording` (clean red circle, no nested square). Confirm "blocky" complaint resolved. |

---

## Validation Sign-Off

- [ ] All deliverables have `<automated>` verify or Wave 0 dependencies (Manual-Only items captured above)
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING test files (`useReviewQueueRealtime.test.tsx`, `Shutter.test.tsx`)
- [ ] No watch-mode flags in commands
- [ ] Feedback latency < 30s for Jest, < 2min for cross-device UAT
- [ ] `nyquist_compliant: true` set in frontmatter once planner has assigned Task IDs and re-keyed the map

**Approval:** pending
