---
status: partial
phase: 03.1-p3-polish-realtime-hardening
source: [03.1-01-PLAN.md, 03.1-02-PLAN.md, 03.1-03-PLAN.md]
started: 2026-05-09T00:00:00Z
updated: 2026-05-10T04:30:00Z
total: 12
passed: 0
issues: 0
pending: 12
---

# Phase 03.1 — UAT Verification Receipts

**Phase:** 03.1 — P3 Polish & Realtime Hardening
**Owner:** human (run on iOS dev client; Android optional if available)
**Created:** 2026-05-09 — populated by Plan 01 / 02 / 03 executors during wave 1.

This file accumulates UAT receipts across all three Phase 03.1 plans. Build
gates (Jest, typecheck, grep acceptance) are already green and recorded in the
respective `*-SUMMARY.md` files; the receipts below are the human-in-the-loop
gates that close the phase.

| CK ID range | Plan | Surface |
|-------------|------|---------|
| CK-01..CK-04 | 03.1-01 | Admin Realtime review-queue patcher |
| CK-05..CK-08 | 03.1-02 | Capture screen nested-Stack modal layout |
| CK-09..CK-12 | 03.1-03 | Shutter visual rewrite |

---

## Plan 03.1-01 — Cross-device UAT (admin Realtime review queue)

**Status:** PENDING — needs two physical iOS devices on the same WiFi.

**Build receipts (already green; recorded in `03.1-01-SUMMARY.md`):**
- 6/6 `useReviewQueueRealtime` unit tests pass
- Full Jest suite: 337/337 across 56 suites
- `tsc --noEmit` clean

**Walkthrough:**

Setup:
- Device A: signed in as the admin of a test group
- Device B: signed in as a regular member of the same test group
- Both devices on the latest dev client build with the new hook deployed (rebuild required after this plan's commits land on `main`)

Walkthrough — badge auto-increment (group-detail):
1. Device A: open the group, land on group-detail. Note the "Pending Review (N)" badge value (likely 0 if no pending submissions). Stay on group-detail.
2. Device B: tap "Capture", record/take a fresh photo or video, submit. Observe Device B's status pill flip to "pending".
3. Device A: WITHOUT navigating away, observe the "Pending Review (N)" badge — it should increment by 1 within ~2 seconds.

Walkthrough — queue list auto-prepend (review-screen):
4. Device A: tap the "Pending Review (N)" badge to navigate to `/groups/[id]/review`. Note the queue list. Stay on review-screen.
5. Device B: submit ANOTHER fresh submission (use a second test member account if needed, or have admin's group be 3+ members).
6. Device A: WITHOUT navigating away, observe the queue list — the new pending row should appear within ~2 seconds, with the correct member's display_name + avatar.

Walkthrough — count-decrement on review (back to badge):
7. Device A: approve or reject one submission from the swipe queue.
8. Device A: navigate back to group-detail. Observe the "Pending Review (N)" badge — it should reflect the new (decremented) count immediately.

Negative test — non-admin device does NOT subscribe:
9. On Device B (regular member), open Metro logs (Expo DevTools → Logs). Navigate to the group's group-detail screen.
10. Observe Metro logs — there should be ZERO log entries containing `review-queue:` for Device B.

### Receipts

| ID    | Description                                         | Captured | Timestamp | Notes |
| ----- | --------------------------------------------------- | -------- | --------- | ----- |
| CK-01 | Badge auto-increment within ~2s on Device A         | PENDING  | —         | —     |
| CK-02 | Queue list auto-prepend within ~2s on Device A      | PENDING  | —         | —     |
| CK-03 | Count-decrement after admin review action           | PENDING  | —         | —     |
| CK-04 | Non-admin Metro log shows ZERO `review-queue:` subscribe entries | PENDING  | —         | —     |

### Fail signals (per Plan 03.1-01)

- Badge does NOT update on Device A → Q1 channel-name regression OR mount call missing admin gate OR P4 publication regression
- "subscribe can only be called a single time per channel instance" in Metro logs → Q1 regression (distinct `'badge'` vs `'list'` mountPoint values at each call site verified by acceptance grep — should not happen)
- Non-admin device DOES log `review-queue:` subscribe → admin gate broken

---

## Plan 03.1-02 — Capture screen: nested Stack modal layout (D-05..D-07)

**Status:** PENDING — needs an iOS dev client.

**Build receipts (already green; recorded in `03.1-02-SUMMARY.md`):**
- `pnpm typecheck` → 0 errors
- `pnpm test` → 55/55 suites, 331/331 tests
- Grep gates: zero `stackScreen`, `PER Plan 03-06`, `Stack-modal options removed`, `Phase 3 inline UAT fix`, `Restoring proper modal-style` references in `app/(app)/capture/[groupId].tsx`
- `app/(app)/capture/_layout.tsx` exists with all 4 modal screenOptions (presentation 'modal', animation 'slide_from_bottom', gestureEnabled false, headerShown false)
- Parent `app/(app)/_layout.tsx` Tabs.Screen `name="capture/[groupId]"` registration UNCHANGED (`href: null` preserved)

### CK-05 — Slide-from-bottom presentation

- **Steps:**
  1. Restart Metro (the new `_layout.tsx` requires a fresh bundle)
  2. Build and install the latest dev client on the test device
  3. Sign in to a group where you are a member
  4. Navigate to that group's group-detail screen
  5. Tap the capture/submit entry point (the path that opens `/capture/[groupId]`)
  6. Observe the screen presentation
- **Expected:** Screen slides up from the bottom of the screen with a smooth animation; the tab bar is fully covered while the modal is presented (no tab bar visible at the bottom). Does NOT appear instantly inline.

### CK-06 — Downward swipe gesture absorbed (gesture-dismiss disabled)

- **Steps:**
  1. While the capture modal is presented, attempt a downward swipe gesture from the top of the screen (the typical iOS modal-dismiss gesture)
  2. The screen should NOT dismiss — the swipe is absorbed/ignored
  3. The screen should ONLY dismiss via the explicit Cancel button (or after a successful submission)
- **Expected:** Downward swipe does not close the modal; only Cancel/submit closes it

### CK-07 — Capture flow regression check (photo OR video → submit → dismiss)

- **Steps:** Open capture modal, run through full happy-path of recording + submitting (photo and video), confirm dismiss happens via the explicit Cancel/Done path and the underlying submission flow still reaches the pending-review state.
- **Expected:** Capture flow is functionally identical to pre-modal-restoration behavior; only the presentation/animation has changed.

### CK-08 — No `sceneStyleInterpolator` runtime crash on open

- **Steps:** Open the capture modal repeatedly across cold starts and warm navigations.
- **Expected:** No runtime crash, no red-screen error referencing `sceneStyleInterpolator` (the original regression that commit `298fe64` was patching when it stripped the modal options).

### Receipts

| ID    | Description                                          | Captured | Timestamp | Notes |
| ----- | ---------------------------------------------------- | -------- | --------- | ----- |
| CK-05 | Capture screen slides up from bottom (modal anim)    | PENDING  | —         | —     |
| CK-06 | Downward swipe does NOT dismiss the modal            | PENDING  | —         | —     |
| CK-07 | Photo + video capture flows still reach pending      | PENDING  | —         | —     |
| CK-08 | No `sceneStyleInterpolator` crash on open            | PENDING  | —         | —     |

---

## Plan 03.1-03 — Shutter Visual Rewrite (D-08..D-10)

**Status:** PENDING — iOS dev client primary, Android optional.

**Closes:** `.planning/notes/2026-05-06T01-05-10Z.md` ("fix record button UI it looks bad — very blocky, square inside a circle").

**Build receipts (already green; recorded in `03.1-03-SUMMARY.md`):**
- 4/4 Shutter component tests pass (3 variants + onPress)
- Full Jest suite: 335/335 GREEN
- `tsc --noEmit` clean
- Acceptance grep: `borderRadius: 0` is physically removed

### Walkthrough

1. **Setup**
   - Build + install latest dev client with the rewrite deployed
   - Sign in to a group where the user is a member
   - Have one PHOTO group and one VIDEO group available (or a video-capable group with mode toggling)

2. **CK-09 — photo variant**
   - Navigate to capture screen for a PHOTO group
   - PASS: 72pt circle, 4pt white outer ring, 52pt theme-primary (yellow) inner circle with white Feather `camera` glyph centered. Should NOT read as "blocky" or "square inside a circle"
   - Capture a photo by tapping — confirm onPress still fires (preview/upload flow proceeds)

3. **CK-10 — video-idle variant**
   - Navigate to capture screen for a VIDEO group (or toggle to video mode)
   - PASS: clean circle-inside-circle with a Feather `video` (filmstrip) glyph centered

4. **CK-11 — video-recording variant**
   - From video-idle, tap the Shutter to start recording
   - PASS: 72pt outer ring (4pt white border with 1.4s opacity pulse 0.85↔1.0) + 52pt SOLID FILLED RED CIRCLE inside it. NO inner glyph. NO inner white square. NO 90-degree corners. Should read as "ring + red dot"
   - Confirm outer ring opacity pulse is visibly animating

5. **Recording → stopped**
   - Tap Shutter again to stop recording
   - PASS: transition back to video-idle state with the Feather video glyph

6. **CK-12 — accessibility labels**
   - Enable VoiceOver (iOS) or TalkBack (Android)
   - Focus on Shutter at each variant — should announce "Take photo" / "Start recording" / "Stop recording"
   - PASS: all 3 labels announced correctly

### Receipts

| ID    | Description                                                          | Captured | Timestamp | Notes |
| ----- | -------------------------------------------------------------------- | -------- | --------- | ----- |
| CK-09 | photo variant — Feather `camera` glyph centered in theme-primary circle | PENDING  | —         | —     |
| CK-10 | video-idle variant — Feather `video` glyph centered in theme-primary circle | PENDING  | —         | —     |
| CK-11 | video-recording variant — clean filled red circle, no nested square  | PENDING  | —         | —     |
| CK-12 | VoiceOver/TalkBack — all 3 a11y labels announced correctly           | PENDING  | —         | —     |

### Pass criteria (gate)

- [ ] User confirms the "blocky / square inside a circle" complaint from 2026-05-06 is RESOLVED

---

## Phase pass criteria

- All 12 CK receipts captured (PENDING → PASS) on a real device.
- No "subscribe can only be called a single time per channel instance" errors in Metro logs across either device during the Realtime walkthrough.
- No `sceneStyleInterpolator` regression on capture-screen open.
- User confirms the Shutter no longer reads as "blocky".
