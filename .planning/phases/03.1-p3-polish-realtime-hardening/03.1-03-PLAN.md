---
phase: 03.1-p3-polish-realtime-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/components/Shutter.test.tsx
  - src/components/Shutter.tsx
autonomous: false
requirements: []
decisions_addressed: [D-08, D-09, D-10]
tags: [ui, component, react-native, expo, vector-icons, jest]

must_haves:
  truths:
    - "User-visible record button no longer reads as a square inside a circle (the original 2026-05-06 complaint)"
    - "Photo idle variant displays a Feather camera glyph centered inside the inner fill"
    - "Video idle variant displays a Feather video glyph centered inside the inner fill"
    - "Video recording variant displays a clean filled red circle (no nested inner square)"
    - "Outer-ring opacity pulse animation (1.4s loop) is preserved while recording"
    - "Public ShutterProps API ({ variant, onPress }) is unchanged so the capture screen caller does NOT need to be edited"
  artifacts:
    - path: "src/components/Shutter.tsx"
      provides: "Visual rewrite of Shutter — Feather-icon-based variants with uniform 52pt inner circle"
      exports: ["Shutter", "ShutterProps"]
      contains: "Feather"
    - path: "tests/components/Shutter.test.tsx"
      provides: "4 unit tests covering all 3 variants + onPress firing"
      contains: "Take photo"
  key_links:
    - from: "src/components/Shutter.tsx"
      to: "@expo/vector-icons Feather"
      via: "import { Feather } from '@expo/vector-icons'"
      pattern: "Feather"
    - from: "src/components/Shutter.tsx"
      to: "useTheme (theme tokens for inner fill color)"
      via: "import { useTheme } from '../theme/useTheme'"
      pattern: "useTheme"
    - from: "app/(app)/capture/[groupId].tsx"
      to: "src/components/Shutter.tsx"
      via: "Shutter caller — public ShutterProps API unchanged"
      pattern: "Shutter"
---

<objective>
Close `notes/2026-05-06T01-05-10Z.md` user complaint: "fix record button UI it looks bad" — clarified during the 03.1 discussion as "very blocky, its a square inside a circle which makes it look bad. Find some cool icon we can use maybe."

Purpose: The current `Shutter` component's recording variant stacks 3 concentric shapes (4pt outer ring + 44pt red inner SQUARE with `borderRadius: 0` + 16pt white center square) — that geometry is the "square inside a circle" the user flagged. Replace the inner-fill stack with:
- Photo idle: Feather `camera` glyph centered inside a 52pt theme-primary circle
- Video idle: Feather `video` glyph centered inside a 52pt theme-primary circle
- Video recording: Clean filled 52pt destructive-red circle (no nested square, no glyph)

Outer-ring + opacity pulse are preserved verbatim. Public `ShutterProps` API is unchanged (`variant: 'photo' | 'video-idle' | 'video-recording'`, `onPress: () => void`) — the capture screen caller does not need to be touched.

Output:
- `tests/components/Shutter.test.tsx` (NEW — 4 test cases, RED-first, GREEN after rewrite)
- `src/components/Shutter.tsx` (MODIFIED — full visual rewrite per D-08..D-10; ~99 lines current, ~85-90 lines after)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-p3-polish-realtime-hardening/03.1-CONTEXT.md
@.planning/phases/03.1-p3-polish-realtime-hardening/03.1-RESEARCH.md
@.planning/phases/03.1-p3-polish-realtime-hardening/03.1-PATTERNS.md
@.planning/phases/03.1-p3-polish-realtime-hardening/03.1-VALIDATION.md
@.planning/notes/2026-05-06T01-05-10Z.md
@src/components/Shutter.tsx
@src/components/StatusPill.tsx
@src/components/TypeChip.tsx
@src/components/CaptureTopBar.tsx
@tests/components/StatusPill.test.tsx
@CLAUDE.md

<interfaces>
Verbatim contracts the executor must follow.

Public API (UNCHANGED — DO NOT modify):

    export interface ShutterProps {
      variant: 'photo' | 'video-idle' | 'video-recording';
      onPress: () => void;
    }

Feather icon names (verbatim — no substitutions unless rendering issues surface):
- `photo` variant → Feather name `camera`
- `video-idle` variant → Feather name `video`
- `video-recording` variant → NO icon glyph (clean filled red circle)

Icon size constant (per D-09 sizing — 28-34pt range; 30 is the centerpoint):

    const ICON_SIZE = 30;

Icon color (verbatim — `#FFFFFF` literal, NOT a theme token):
The inner fill is `t.colors.primary` (saturated yellow) or `t.colors.destructive` (red), both of which require `#FFFFFF` for AA contrast — matches `CaptureTopBar.tsx` line 62 precedent.

Inner fill (verbatim — uniform 52pt circle for ALL three variants):

    <View
      style={{
        width: 52,
        height: 52,
        borderRadius: 26,
        backgroundColor: innerColor,
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
      {variant === 'photo' && (
        <Feather name="camera" size={ICON_SIZE} color="#FFFFFF" />
      )}
      {variant === 'video-idle' && (
        <Feather name="video" size={ICON_SIZE} color="#FFFFFF" />
      )}
      {/* video-recording: clean filled red circle, no glyph */}
    </View>

Where `innerColor` is:
- `t.colors.primary` for `photo` and `video-idle`
- `t.colors.destructive` for `video-recording`

Outer ring + Pressable + Pulse animation (PRESERVED VERBATIM from current implementation — DO NOT modify):

    // Pressable (current lines 51-63):
    <Pressable
      accessibilityRole="button"
      accessibilityLabel={a11yLabel}
      onPress={onPress}
      style={({ pressed }) => ({
        width: 72, height: 72, borderRadius: 36,
        alignItems: 'center', justifyContent: 'center',
        transform: [{ scale: pressed ? (isRecording ? 0.95 : 0.92) : 1 }],
      })}
    >

    // Outer ring (current lines 64-75):
    <Animated.View
      style={{
        position: 'absolute',
        width: 72, height: 72, borderRadius: 36,
        borderWidth: 4, borderColor: '#FFFFFF',
        opacity: pulseOpacity,
      }}
    />

    // Pulse animation (current lines 25-48 — keep verbatim)

Accessibility labels (verbatim — preserve exact strings for the test):
- `photo` variant: "Take photo"
- `video-idle` variant: "Start recording"
- `video-recording` variant: "Stop recording"

The `a11yLabel` derivation pattern (preserve from current Shutter):

    const a11yLabel =
      variant === 'photo' ? 'Take photo' :
      variant === 'video-idle' ? 'Start recording' :
      'Stop recording';

`isRecording` derivation:

    const isRecording = variant === 'video-recording';
</interfaces>
</context>

<read_first>
- `src/components/Shutter.tsx` (full — 99 lines current state. The blocky "square inside a circle" geometry to be replaced is at lines 81-96)
- `src/components/StatusPill.tsx` (full — 147 lines, sibling Feather-using component; lines 8 (import), 61, 81, 116, 135 demonstrate Feather usage with theme color)
- `src/components/TypeChip.tsx` (full — 44 lines, sibling Feather user; line 5 (import), 32 (Feather + size + color))
- `src/components/CaptureTopBar.tsx` (full — 108 lines, sibling Feather user OVER A DARK SURFACE; line 8 (import), 62 (`color="#FFFFFF"`), 99)
- `tests/components/StatusPill.test.tsx` (full — 62 lines, the closest test analog: variant-driven render coverage with `withTheme` wrapper, no QueryClient or supabase needed)
- `.planning/phases/03.1-p3-polish-realtime-hardening/03.1-PATTERNS.md` section "MOD src/components/Shutter.tsx" + section "NEW tests/components/Shutter.test.tsx" (verbatim code excerpts)
- `.planning/phases/03.1-p3-polish-realtime-hardening/03.1-RESEARCH.md` section "Pattern 3: Icon-Based Shutter Variants" + section "Code Examples 4: Redesigned Shutter variants" (lines 561-575) + section "Pitfall 5" (Shutter inner fill clipping the outer ring)
- `.planning/phases/03.1-p3-polish-realtime-hardening/03.1-CONTEXT.md` section D-08..D-10
- `.planning/notes/2026-05-06T01-05-10Z.md` (the original user complaint — confirms scope is geometric, not animation-based)
- `app/(app)/capture/[groupId].tsx` lines 548-557 (caller — confirms public ShutterProps API consumers; no edits needed if API preserved)
- `CLAUDE.md` section "Process — JIRA + commit convention"
</read_first>

<user_setup>
This plan reuses the SCRUM ID captured by Plan 01 Task 1. If Plan 01 has not yet run, the executor should defer the JIRA-update step to Task 4 and ask the user to provide the SCRUM ID at that point. The Plan 01/02/03 file sets are DISJOINT — all three plans can execute in parallel (Wave 1).
</user_setup>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bootstrap Wave 0 Shutter component test (RED-first)</name>
  <files>tests/components/Shutter.test.tsx</files>
  <read_first>
    - tests/components/StatusPill.test.tsx (full — closest analog; copy `withTheme` wrapper + theme construction lines 1-22)
    - src/components/Shutter.tsx (full — current implementation; confirms accessibility labels + variant types)
    - .planning/phases/03.1-p3-polish-realtime-hardening/03.1-PATTERNS.md section "NEW tests/components/Shutter.test.tsx" (verbatim test scaffolds)
    - .planning/phases/03.1-p3-polish-realtime-hardening/03.1-VALIDATION.md (test strategy)
  </read_first>
  <behavior>
    - Test 1: photo variant renders with accessibilityLabel "Take photo"
    - Test 2: video-idle variant renders with accessibilityLabel "Start recording"
    - Test 3: video-recording variant renders with accessibilityLabel "Stop recording"
    - Test 4: pressing the Shutter fires the onPress callback exactly once
  </behavior>
  <action>
    Create `tests/components/Shutter.test.tsx` by copying the structure of `tests/components/StatusPill.test.tsx` and adapting:

    1. **Imports + theme wrapper** — copy lines 1-22 of StatusPill.test.tsx VERBATIM, then change the component import:

           import type { ReactElement } from 'react';
           import { render, fireEvent } from '@testing-library/react-native';
           import { ThemeContext, type Theme } from '../../src/theme/useTheme';
           import { colors, spacing, radii, fonts } from '../../src/theme/tokens';
           import { Shutter } from '../../src/components/Shutter';

           const theme: Theme = {
             colors: colors.light,
             spacing,
             radii,
             fonts,
             name: 'light',
           };

           function withTheme(node: ReactElement) {
             return <ThemeContext.Provider value={theme}>{node}</ThemeContext.Provider>;
           }

       Note `fireEvent` import is required for Test 4. If the analog test doesn't import it, add it explicitly to the testing-library import.

    2. **Test cases** — implement all 4:

           describe('Shutter', () => {
             it('photo variant renders with "Take photo" accessibility label', () => {
               const { getByLabelText } = render(
                 withTheme(<Shutter variant="photo" onPress={() => {}} />),
               );
               expect(getByLabelText('Take photo')).toBeTruthy();
             });

             it('video-idle variant renders with "Start recording" accessibility label', () => {
               const { getByLabelText } = render(
                 withTheme(<Shutter variant="video-idle" onPress={() => {}} />),
               );
               expect(getByLabelText('Start recording')).toBeTruthy();
             });

             it('video-recording variant renders with "Stop recording" accessibility label', () => {
               const { getByLabelText } = render(
                 withTheme(<Shutter variant="video-recording" onPress={() => {}} />),
               );
               expect(getByLabelText('Stop recording')).toBeTruthy();
             });

             it('fires onPress when pressed', () => {
               const onPress = jest.fn();
               const { getByRole } = render(
                 withTheme(<Shutter variant="photo" onPress={onPress} />),
               );
               fireEvent.press(getByRole('button'));
               expect(onPress).toHaveBeenCalledTimes(1);
             });
           });

    3. **Run tests**: `pnpm test --testPathPattern Shutter` — should PASS even before the rewrite (the current Shutter already exposes these accessibility labels per src/components/Shutter.tsx). The test file is GREEN-first — that's intentional. The tests are a SAFETY NET capturing the public-API contract that the rewrite must preserve.

    4. NOTE: this is a behavior-preservation test, not a RED-first TDD test. The Shutter rewrite changes VISUAL geometry, not the public API. The test ensures the rewrite doesn't accidentally regress accessibility labels or onPress wiring.

    Commit message: `test(03.1-03): add Shutter component test covering 3 variants + onPress`. After commit, log 15-min work entry on JIRA story ("Shutter behavior-preservation test scaffolded").
  </action>
  <verify>
    <automated>test -f tests/components/Shutter.test.tsx && pnpm test --testPathPattern Shutter</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/components/Shutter.test.tsx` exits 0
    - 4 test cases declared: `grep -cE "^\s*it\(" tests/components/Shutter.test.tsx` returns >= 4
    - All 3 accessibility labels present: `grep -q "Take photo" tests/components/Shutter.test.tsx` AND `grep -q "Start recording" tests/components/Shutter.test.tsx` AND `grep -q "Stop recording" tests/components/Shutter.test.tsx`
    - onPress assertion present: `grep -q "toHaveBeenCalledTimes(1)" tests/components/Shutter.test.tsx`
    - withTheme wrapper used (theme context required for inner-fill color resolution): `grep -q "withTheme" tests/components/Shutter.test.tsx`
    - Tests pass against current Shutter (behavior preservation gate): `pnpm test --testPathPattern Shutter` exits 0
    - 4/4 tests passing: `pnpm test --testPathPattern Shutter 2>&1 | grep -qE "Tests:.*4 passed"`
  </acceptance_criteria>
  <done>4-case Shutter test committed; tests pass against current implementation (behavior preservation safety net in place).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite Shutter visual geometry (D-08..D-10)</name>
  <files>src/components/Shutter.tsx</files>
  <read_first>
    - src/components/Shutter.tsx (full — current 99-line implementation; need to know what to preserve and what to replace)
    - src/components/CaptureTopBar.tsx (full — 108 lines, the closest sibling Feather-over-dark-surface analog: line 8 import, line 62 `color="#FFFFFF"` literal)
    - src/components/StatusPill.tsx (full — Feather usage with theme color tokens)
    - src/components/TypeChip.tsx (full — Feather + size + color pattern at line 32)
    - .planning/phases/03.1-p3-polish-realtime-hardening/03.1-PATTERNS.md section "MOD src/components/Shutter.tsx" (verbatim code excerpts including Pressable/outer-ring preservation + new inner-fill block)
    - .planning/phases/03.1-p3-polish-realtime-hardening/03.1-RESEARCH.md section "Pattern 3" + section "Code Examples 4" (full rewrite scaffold) + section "Pitfall 5" (inner fill clipping outer ring)
    - .planning/phases/03.1-p3-polish-realtime-hardening/03.1-CONTEXT.md section D-08..D-10
    - tests/components/Shutter.test.tsx (the safety-net test from Task 1 — must remain GREEN after the rewrite)
  </read_first>
  <behavior>
    - Photo variant: shows Feather `camera` glyph centered in a 52pt theme-primary circle
    - Video-idle variant: shows Feather `video` glyph centered in a 52pt theme-primary circle
    - Video-recording variant: shows a clean filled 52pt destructive-red circle (no glyph, no nested inner square)
    - Outer-ring opacity pulse animation continues to fire while recording (1.4s loop preserved)
    - Pressable Pressable behavior + accessibility labels + onPress wiring all preserved
    - All 4 tests from Task 1 remain GREEN
  </behavior>
  <action>
    Full visual rewrite of `src/components/Shutter.tsx`. PRESERVE the public API, accessibility labels, outer ring, Pressable, and pulse animation. REPLACE the inner-fill stack (current lines 81-96) with the new uniform 52pt circle + Feather glyph design.

    **Step 1 — Update imports.** Add Feather and useTheme to the existing imports:

        import { Feather } from '@expo/vector-icons';
        import { useTheme } from '../theme/useTheme';

    Keep the existing `Animated`, `Pressable`, `View`, `useEffect`, `useRef` imports from `react-native`/`react`.

    **Step 2 — Add icon size constant** at the top of the component file (after imports, before the component):

        const ICON_SIZE = 30;

    **Step 3 — Inside the component, derive inner-fill color from theme:**

        const t = useTheme();
        const isRecording = variant === 'video-recording';
        const innerColor = isRecording
          ? t.colors.destructive
          : t.colors.primary;

    Verify `t.colors.destructive` and `t.colors.primary` exist on the theme object (they do — see `src/theme/tokens.ts`).

    **Step 4 — REPLACE the current 3-nested-shape inner-fill block** (current lines 81-96, the "square inside a circle" geometry the user complained about) with the new uniform 52pt circle + Feather glyph:

        <View
          style={{
            width: 52,
            height: 52,
            borderRadius: 26,
            backgroundColor: innerColor,
            alignItems: 'center',
            justifyContent: 'center',
          }}
        >
          {variant === 'photo' && (
            <Feather name="camera" size={ICON_SIZE} color="#FFFFFF" />
          )}
          {variant === 'video-idle' && (
            <Feather name="video" size={ICON_SIZE} color="#FFFFFF" />
          )}
          {/* video-recording: clean filled red circle, no glyph */}
        </View>

    The `borderRadius: 26` (= width/2) makes the inner-fill a perfect circle for ALL three variants. RESEARCH Pitfall 5 calls out: do NOT exceed `width: 56` for the inner fill — it would clip the 4pt outer ring (72pt outer - 4pt border × 2 = 64pt usable; 52pt + 11pt visual gap is safe).

    **Step 5 — PRESERVE verbatim:**
    - Pressable wrapper (current lines 51-63) — DO NOT modify
    - Outer ring `<Animated.View>` (current lines 64-75) — DO NOT modify
    - Pulse animation `useEffect` (current lines 25-48) — DO NOT modify
    - `a11yLabel` derivation — DO NOT modify (must produce "Take photo" / "Start recording" / "Stop recording")
    - `ShutterProps` export — DO NOT modify

    **Step 6 — Run tests + typecheck:**

    1. `pnpm test --testPathPattern Shutter` — all 4 tests from Task 1 must remain GREEN.
    2. `pnpm test` — full Jest suite must remain GREEN (no other test should regress).
    3. `pnpm typecheck` — must report 0 errors.
    4. The current 99-line file should shrink slightly (~85-90 lines) since the inner-fill block is shorter than the 3-nested-shape stack.

    Commit message: `feat(03.1-03): rewrite Shutter with Feather-icon variants (D-08..D-10)`. After commit, log 30-min work entry on JIRA story ("Shutter visual rewrite — Feather glyphs replace blocky geometry").
  </action>
  <verify>
    <automated>grep -q "Feather" src/components/Shutter.tsx && grep -q "ICON_SIZE" src/components/Shutter.tsx && pnpm test --testPathPattern Shutter && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Feather imported: `grep -q "import { Feather } from '@expo/vector-icons'" src/components/Shutter.tsx`
    - useTheme imported: `grep -q "import { useTheme } from '../theme/useTheme'" src/components/Shutter.tsx`
    - ICON_SIZE constant present: `grep -qE "const ICON_SIZE\s*=\s*30" src/components/Shutter.tsx`
    - Inner-fill is a uniform 52pt circle: `grep -q "width: 52" src/components/Shutter.tsx` AND `grep -q "borderRadius: 26" src/components/Shutter.tsx`
    - NO `borderRadius: 0` (the original "blocky square" property): `! grep -qE "borderRadius:\s*0[^.0-9]" src/components/Shutter.tsx`
    - Photo variant renders Feather camera: `grep -q 'name="camera"' src/components/Shutter.tsx`
    - Video-idle variant renders Feather video: `grep -q 'name="video"' src/components/Shutter.tsx`
    - Icon color is white literal: `grep -q 'color="#FFFFFF"' src/components/Shutter.tsx`
    - innerColor is theme-derived: `grep -qE "innerColor\s*=" src/components/Shutter.tsx` AND `grep -q "t.colors.destructive" src/components/Shutter.tsx` AND `grep -q "t.colors.primary" src/components/Shutter.tsx`
    - Outer ring preserved: `grep -q "borderColor: '#FFFFFF'" src/components/Shutter.tsx` AND `grep -q "borderWidth: 4" src/components/Shutter.tsx`
    - Pulse animation preserved: `grep -q "Animated.loop" src/components/Shutter.tsx` AND `grep -q "pulseOpacity" src/components/Shutter.tsx`
    - Public API preserved: `grep -q "export interface ShutterProps" src/components/Shutter.tsx` AND `grep -q "variant: 'photo' | 'video-idle' | 'video-recording'" src/components/Shutter.tsx`
    - Accessibility labels preserved: `grep -q "Take photo" src/components/Shutter.tsx` AND `grep -q "Start recording" src/components/Shutter.tsx` AND `grep -q "Stop recording" src/components/Shutter.tsx`
    - Behavior-preservation tests still pass: `pnpm test --testPathPattern Shutter` exits 0
    - Full Jest suite green: `pnpm test` exits 0
    - Typecheck clean: `pnpm typecheck` exits 0
    - File line count reduced (was 99 lines, expected 80-95 lines): `wc -l < src/components/Shutter.tsx` returns a value between 75 and 95
    - Caller in capture screen UNCHANGED: `grep -q "Shutter" "app/(app)/capture/[groupId].tsx"` (caller still imports Shutter; no edits needed because public API is unchanged)
  </acceptance_criteria>
  <done>Visual rewrite committed; behavior-preservation tests + full suite remain green; public API unchanged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT — Shutter visual reads cleanly at all 3 variants</name>
  <files>.planning/phases/03.1-p3-polish-realtime-hardening/03.1-VERIFICATION.md</files>
  <action>
    See `<how-to-verify>` block below for the full UAT walkthrough. The executor presents the steps, the user runs the walkthrough on iOS dev client (and Android if available), and the executor captures receipts CK-09..CK-12 in `.planning/phases/03.1-p3-polish-realtime-hardening/03.1-VERIFICATION.md`. Crucially the user must verbally confirm the original "blocky / square inside a circle" complaint (notes/2026-05-06T01-05-10Z.md) is resolved.
  </action>
  <verify>
    <automated>test -f .planning/phases/03.1-p3-polish-realtime-hardening/03.1-VERIFICATION.md && grep -qE "CK-(09|1[0-2])" .planning/phases/03.1-p3-polish-realtime-hardening/03.1-VERIFICATION.md</automated>
  </verify>
  <done>4 UAT receipts (CK-09..CK-12) captured + user confirms the "blocky" complaint is resolved.</done>
  <what-built>
    `src/components/Shutter.tsx` rewritten per D-08..D-10. Feather camera glyph for `photo`, Feather video glyph for `video-idle`, clean filled red circle for `video-recording`. Outer-ring opacity pulse preserved. Public ShutterProps API unchanged.
  </what-built>
  <how-to-verify>
    iOS dev client (and Android if available) — visual judgment required since the user complaint is geometric.

    **Setup:**
    - Build and install the latest dev client with the Shutter rewrite deployed
    - Sign in to a group where the user is a member
    - Have at least one PHOTO group and one VIDEO group available for testing (or a video-capable group where toggling between modes works)

    **Walkthrough — photo variant:**
    1. Navigate to the capture screen for a PHOTO group
    2. Observe the Shutter at the bottom of the screen
    3. PASS criterion: A 72pt circle with a 4pt white outer ring and a 52pt theme-primary (yellow) inner circle. A white Feather `camera` glyph is centered inside the inner circle. The geometry should NOT read as "blocky" or "square inside a circle".
    4. Capture a photo by tapping — confirm onPress still fires (preview/upload flow proceeds as before)

    **Walkthrough — video-idle variant:**
    5. Navigate to the capture screen for a VIDEO group (or toggle to video mode)
    6. Observe the Shutter — should match the photo variant geometry except the inner circle now contains a Feather `video` glyph (filmstrip icon)
    7. PASS criterion: clean circle-inside-circle with a video glyph centered

    **Walkthrough — video-recording variant:**
    8. From video-idle, tap the Shutter to start recording
    9. Observe the Shutter transition to the recording state
    10. PASS criterion: A 72pt outer ring (4pt white border with 1.4s opacity pulse 0.85↔1.0) + a 52pt SOLID FILLED RED CIRCLE inside it. NO inner glyph. NO inner white square. NO 90-degree corners. The geometry should read as "ring + red dot" — clean and unambiguous as the recording-active state.
    11. Confirm the outer ring opacity pulse is visibly animating

    **Walkthrough — recording → stopped:**
    12. Tap the Shutter again to stop recording
    13. Observe the Shutter transition back to the video-idle state with the Feather video glyph

    **Walkthrough — accessibility labels (negative test for screen readers):**
    14. Enable VoiceOver (iOS) or TalkBack (Android)
    15. Focus on the Shutter at each variant — should announce "Take photo" / "Start recording" / "Stop recording" correspondingly
    16. PASS criterion: all 3 labels announced correctly

    **Capture receipts in 03.1-VERIFICATION.md:**
    - Receipt CK-09: photo variant screenshot — confirms Feather camera glyph centered in theme-primary circle
    - Receipt CK-10: video-idle variant screenshot — confirms Feather video glyph centered
    - Receipt CK-11: video-recording variant screenshot — confirms clean filled red circle, no nested square
    - Receipt CK-12: VoiceOver/TalkBack accessibility label confirmations

    **Pass criteria:**
    - User confirms the "blocky / square inside a circle" complaint from 2026-05-06 is RESOLVED
    - All 4 receipts captured
    - Capture flow (photo/video submit) still works end-to-end (no regression to onPress wiring)

    **Fail signals:**
    - Recording variant still shows nested square → the `borderRadius: 0` was not removed from the inner fill. Check `grep "borderRadius: 0" src/components/Shutter.tsx`.
    - Glyph not visible → Feather not bundled OR `color="#FFFFFF"` is missing. Check the import + the Feather usage line.
    - Glyph too small / too large → `ICON_SIZE` outside 28-34 range. Adjust to taste; D-09 says 28-34pt; if 30 reads too small, try 34; if too large, try 28.
    - Glyph poorly chosen for the surface → swap per Claude's discretion (D-09 lists `aperture` / `circle` / `play-circle` as alternates if `camera` / `video` don't render well at the final size). Re-run Task 2 acceptance to update the grep.
  </how-to-verify>
  <resume-signal>Type "approved" with the 4 receipts captured in 03.1-VERIFICATION.md, OR describe issues with screenshots and the user's verbatim feedback.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Update JIRA story status + log final work entry for Plan 03</name>
  <files></files>
  <read_first>
    - CLAUDE.md section "Process — JIRA + commit convention"
    - docs/processes/03-requirements-status-tracking.md (rationale + audit precedent)
    - 03.1-01-SUMMARY.md if Plan 01 already shipped (to inherit the SCRUM ID)
  </read_first>
  <action>
    Using the SCRUM ID captured in Plan 01 Task 1 (or prompt the user for it now if Plan 01 has not yet run):

    1. If THIS plan is the LAST plan in the 03.1 phase to close (verify by checking which other plans are still in-flight), move the SCRUM story status forward to "In Review" or "Done" per closing convention. Otherwise keep it "In Progress".

    2. Add a work-log entry rounded to 15-min increments summarizing total time spent on Plan 03 work (Tasks 1-3). Format:

        Plan 03: Shutter visual rewrite — close 2026-05-06 user complaint (D-08..D-10). 3-variant Feather-icon-based design replaces the "square inside a circle" geometry. Public ShutterProps API unchanged. UAT receipts CK-09..CK-12 captured.

    3. If THIS is the closing plan for the entire 03.1 phase, also append a phase-close summary line:

        Phase 03.1 closes: VERIFICATION items 1+2 + Shutter user complaint resolved. Items 3-5 remain deferred per D-11.

    4. Verify the JIRA story now shows the cumulative work log from all `(03.1-03)`-scoped commits + the phase-close summary if applicable.

    Use the Atlassian MCP tools (`mcp__atlassian__editJiraIssue`, `mcp__atlassian__addCommentToJiraIssue`). If MCP auth fails or the SCRUM ID is "skipped" per Plan 01 Task 1, this task becomes a no-op — record the rationale in `.planning/notes/` so the audit trail is preserved.
  </action>
  <verify>
    <automated>echo "JIRA update is verified by checking the SCRUM story page; this acceptance criterion is observational"</automated>
  </verify>
  <acceptance_criteria>
    - SCRUM story (or "skipped" sentinel from Plan 01 Task 1) acknowledged in resume notes
    - If JIRA was used: SCRUM story work log shows the time spent on Plan 03 (work-log entry visible at `https://comp586.atlassian.net/browse/SCRUM-NN`)
    - If skipped: rationale documented in `.planning/notes/` so the audit trail is preserved (per CLAUDE.md skipping is a "process defect" unless rationale is captured)
    - Phase-close summary appended on JIRA if this is the LAST plan to close in 03.1
  </acceptance_criteria>
  <done>JIRA story updated (or skip rationale documented). Plan 03 closes cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → onPress callback | The Pressable's onPress is invoked on tap; the callback signature is `() => void` with no parameters from the Shutter — the caller controls all behavior |
| Theme tokens → inner-fill color | Theme is a trusted source (no user input touches the theme); inner fill color is a static lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-031-09 | Tampering (regression) | Public ShutterProps API | mitigate | The rewrite is purely VISUAL — public API (`ShutterProps`) is unchanged. Acceptance criterion (Task 2) enforces `grep -q "export interface ShutterProps"` AND the variant union type AND the caller in `app/(app)/capture/[groupId].tsx` is unchanged. The behavior-preservation test from Task 1 (4 cases covering all 3 variants + onPress firing) is the regression gate. |
| T-031-10 | Information Disclosure | Accessibility labels | accept | The 3 a11y labels ("Take photo" / "Start recording" / "Stop recording") are user-facing strings, not security-sensitive. The rewrite preserves them. Acceptance criterion (Task 2 grep) enforces all 3 strings remain in the file. |
| T-031-11 | DoS / Resource Exhaustion | Animated.loop on outer-ring opacity | accept | The 1.4s opacity loop is unchanged from the current implementation. Native driver enabled (`useNativeDriver: true`) — runs on the GPU compositor without crossing the JS bridge per frame. Already shipped in P3; no new attack surface. |
</threat_model>

<verification>
## Plan-Level Verification

Run before marking the plan complete:

    # 1. Test file exists with 4 cases
    test -f tests/components/Shutter.test.tsx
    grep -cE "^\s*it\(" tests/components/Shutter.test.tsx | grep -qE "^[4-9]|^[1-9][0-9]"

    # 2. Shutter rewritten with Feather + uniform 52pt circle
    grep -q "Feather" src/components/Shutter.tsx
    grep -q 'name="camera"' src/components/Shutter.tsx
    grep -q 'name="video"' src/components/Shutter.tsx
    grep -q "borderRadius: 26" src/components/Shutter.tsx
    ! grep -qE "borderRadius:\s*0[^.0-9]" src/components/Shutter.tsx

    # 3. Public API preserved
    grep -q "export interface ShutterProps" src/components/Shutter.tsx
    grep -q "variant: 'photo' | 'video-idle' | 'video-recording'" src/components/Shutter.tsx

    # 4. Outer ring + pulse preserved
    grep -q "borderWidth: 4" src/components/Shutter.tsx
    grep -q "Animated.loop" src/components/Shutter.tsx

    # 5. Caller unchanged (no edits to capture screen needed)
    grep -q "<Shutter" "app/(app)/capture/[groupId].tsx"

    # 6. Tests + suite green
    pnpm test --testPathPattern Shutter
    pnpm test
    pnpm typecheck

    # 7. UAT receipts captured
    grep -q "CK-09\|CK-10\|CK-11\|CK-12" .planning/phases/03.1-p3-polish-realtime-hardening/03.1-VERIFICATION.md

    # 8. JIRA story updated (or skip rationale documented)
</verification>

<success_criteria>
- New Shutter component test file exists with 4 cases — all pass
- `src/components/Shutter.tsx` uses Feather glyphs (`camera` for photo, `video` for video-idle, no glyph for video-recording)
- Inner fill is a uniform 52pt circle (`borderRadius: 26`) for ALL three variants — no `borderRadius: 0` remains
- Public ShutterProps API unchanged; capture screen caller does NOT need to be edited
- Outer-ring + pulse animation preserved
- Full Jest suite remains green; typecheck clean
- UAT receipts CK-09..CK-12 captured in 03.1-VERIFICATION.md (3 variant screenshots + accessibility label confirmations)
- User confirms the original "blocky / square inside a circle" complaint is RESOLVED
- JIRA story updated per CLAUDE.md convention (or skip rationale documented)
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-p3-polish-realtime-hardening/03.1-03-SUMMARY.md` capturing:
- What shipped (Shutter rewrite + 4-case test)
- The 3-variant icon vocabulary (Feather camera / Feather video / no glyph for recording) as the canonical Shutter design
- 4 UAT receipts (CK-09..CK-12) with screenshots and the user's verbatim approval
- Confirmation that the original 2026-05-06 user complaint is resolved
- JIRA SCRUM story ID and final status + total work logged
- If this is the closing plan for Phase 03.1: phase-close confirmation that VERIFICATION items 1+2 are closed AND items 3-5 remain deferred per D-11
</output>
