---
phase: 03
plan: 08
type: execute
wave: 5
depends_on: [03-07]
files_modified:
  - .planning/phases/03-capture-admin-review/03-VERIFICATION.md
  - .planning/phases/03-capture-admin-review/03-UAT.md
autonomous: false
requirements: [SUB-01, SUB-02, SUB-03, SUB-04, SUB-05, SUB-06, ADM-01, ADM-02, ADM-03, ADM-04, PLAT-03]
estimated_tasks: 4
tags: [verification, uat, phase-gate, manual-testing, ios-device, end-to-end]
user_setup:
  - service: physical-ios-device
    why: "Per PITFALLS §10 + RESEARCH §Validation Architecture line 1466 — physical-device + airplane-mode + Slow-3G testing is the only way to validate SUB-03 (resilience to flaky networks) and SUB-02 (10s video capture + memory pressure). Simulator behavior diverges from real device for camera/video/network."
    env_vars: []
    dashboard_config:
      - task: "Have at least one iOS physical device available with the dev client installed (Plan 03-01 Task 4 rebuilt it). Two devices preferable for the cross-device Realtime test (admin device + member device)."
        location: "iOS only per Phase 1+2 precedent — Android UAT remains a tracked deferred item per project convention"
  - service: real-supabase-project
    why: "UAT must run against the live remote schema (Plan 03-02 pushed migration 0006). Local supabase-dev has the same schema after `supabase db push` but UAT validates the deployed flow end-to-end."
    env_vars: []
    dashboard_config:
      - task: "Confirm app's EXPO_PUBLIC_SUPABASE_URL points at the live remote (not localhost or another project)"
        location: ".env file or shell"

must_haves:
  truths:
    - "Full automated suite is GREEN: `pnpm test` (Jest) + `supabase test db` (pgTAP) + `pnpm typecheck` (TS) + `expo-doctor` (toolchain)"
    - "All 11 Phase 3 requirement IDs (SUB-01..06, ADM-01..04, PLAT-03) are validated either via automated test or manual UAT — every ID has at least one PASS receipt in 03-VERIFICATION.md"
    - "11-checkpoint manual UAT walkthrough on iOS dev client per UI-SPEC §Manual UAT items (cross-referenced from RESEARCH §Validation Architecture lines 130-143) — every checkpoint passed OR explicitly documented as deferred"
    - "Cross-device Realtime test PASSED: on member's device, observe StatusPill cross-fade from 'Pending review' → 'Approved' (or 'Today didn't count') within ~2s of admin pressing approve/reject on a separate device"
    - "**PER REVIEWS.md C9 (HARD GATE — non-deferrable):** Airplane-mode upload-resilience test PASSED (PITFALLS §10): toggle airplane-mode ON before tapping Submit → capture screen dismisses → QueueBadge appears on Today GroupCard → toggle airplane-mode OFF → queue flushes within ~5s → StatusPill shows Pending review. SUB-03 is the headline product promise of P3; this checkpoint receipt MUST be PASS for phase close — deferred-with-reason is not acceptable for this specific test (other UAT items remain deferrable)."
    - "Already-submitted-twice protection PASSED: after a successful submit, attempt to submit again same day → typed error `You already submitted today. Streak's safe — see you tomorrow.` shown as inline error"
    - "Camera permission denial flow PASSED: deny camera → permission-denied screen renders with project-specific copy (`Accountibuzz needs camera access...`) → Open Settings deep-link works → grant in OS Settings → return to app → permission re-polled → viewfinder appears"
    - "Discard-take modal PASSED: capture a take → tap × → modal opens with `Discard this take?` title + `Keep recording` dismiss label (NEVER `Cancel`) → Discard returns to Today; Keep recording returns to review state"
    - "Admin swipe queue gestures PASSED on physical device: rubber-band on under-threshold swipe / swipe-fly-off on over-threshold / reject-reason panel slides up on left commit / Approve button (fallback) fires same RPC"
    - "Reject-reason terminal-rejection invariant PASSED: admin rejects → submitter's Today GroupCard cross-fades to `Today didn't count` (rejected pill, tappable for reason modal) → submitter is BLOCKED from re-submitting until next group-local midnight (per D-12 — verified by attempting Submit and seeing `already_submitted_today` typed error)"
    - "PendingReviewRow visibility gate PASSED: as admin → row visible with correct count when pending > 0 / hidden when count = 0; as non-admin → row never visible regardless of count (UI gate + RPC 0-leak combined)"
    - "Stack → Tabs migration PASSED on device: Today/Groups/Profile tabs all navigate; bottom-tab bar shows 56pt + safe-area inset; active tab indicator visible (yellow underline OR documented-deferred per Plan 03-06 SUMMARY); navigation between tabs preserves TanStack cache"
    - "Open items from prior plans (cutoffTime '12:00 AM' literal vs 'midnight'; SwipeCard prop name alignment from Plan 03-07; QueueBadge inline-vs-extracted decision from Plan 03-04) are reviewed and either accepted or filed in `deferred-items.md`"
    - "ADM-04 rewording open item from CONTEXT line 188-189 (`REQUIREMENTS.md` ADM-04 needs rewording per D-12) is COMPLETED — `REQUIREMENTS.md` line 49 updated from 'Rejected submissions notify the submitter so they can resubmit before cutoff' to a wording aligned with terminal-rejection (e.g. 'Rejected submitters are notified that today did not count')"
    - "ROADMAP.md Phase 3 Success Criterion #5 rewording from CONTEXT line 190 also COMPLETED — same rephrasing as ADM-04"
    - "`03-VERIFICATION.md` exists with the per-requirement PASS/FAIL/DEFERRED matrix + automated suite outputs + UAT checkpoint receipts"
    - "`03-UAT.md` exists with the 11-checkpoint walkthrough script (the actual instructions the human runs through)"
    - "Pre-existing Android dev-build warnings (`edgeToEdgeEnabled` + `expo-system-ui`) tracked in `01-foundation/deferred-items.md` from Phase 2 are NOT introduced as P3 regressions"
  artifacts:
    - path: ".planning/phases/03-capture-admin-review/03-VERIFICATION.md"
      provides: "Per-requirement validation matrix + automated suite output + UAT receipts"
      contains: "VERIFICATION"
    - path: ".planning/phases/03-capture-admin-review/03-UAT.md"
      provides: "11-checkpoint manual UAT walkthrough script with pass/fail receipts"
      contains: "UAT"
  key_links:
    - from: "03-VERIFICATION.md"
      to: "Each P3 requirement ID with automated test reference OR UAT checkpoint reference"
      via: "Coverage matrix per VALIDATION.md Source Data lines 50-82"
      pattern: "SUB-0\\|ADM-0\\|PLAT-03"
    - from: "REQUIREMENTS.md ADM-04 wording update"
      to: "CONTEXT.md Open Items line 188-189"
      via: "Phase-close documentation correction per D-12 rescoping"
      pattern: "Rejected submitters"
---

<objective>
Run the full Phase 3 verification gate — automated suite (Jest + pgTAP + typecheck + expo-doctor) + 11-checkpoint manual UAT walkthrough on iOS physical device. Document the results in `03-VERIFICATION.md` (machine-validated) + `03-UAT.md` (human-validated). Update `REQUIREMENTS.md` ADM-04 + `ROADMAP.md` Success Criterion #5 to reflect the terminal-rejection rescoping per CONTEXT Open Items.

Purpose: This is the phase-close gate. Plan 03-07 finished the implementation; this plan proves it works end-to-end and signs the receipt for SUB-01..06, ADM-01..04, PLAT-03. The cross-device Realtime test is the most novel validation (no in-repo precedent for multi-device flows); the airplane-mode queue test is the highest-risk SUB-03 validation per PITFALLS §10.

Output: 2 new docs (VERIFICATION + UAT) + 2 doc updates (REQUIREMENTS + ROADMAP) + go/no-go decision for phase close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-capture-admin-review/03-CONTEXT.md
@.planning/phases/03-capture-admin-review/03-RESEARCH.md
@.planning/phases/03-capture-admin-review/03-VALIDATION.md
@.planning/phases/03-capture-admin-review/03-UI-SPEC.md
@.planning/phases/02-groups-invites/02-VERIFICATION.md
@.planning/phases/01-foundation/deferred-items.md
@CLAUDE.md

<interfaces>
<!-- This plan creates documentation, not code. -->
<!-- Reference patterns from prior phase verifications: -->

```
.planning/phases/02-groups-invites/02-VERIFICATION.md
  - 11-checkpoint UAT walkthrough format
  - Per-requirement PASS/FAIL/PARTIAL/DEFERRED grid
  - Automated suite output capture (paste pnpm test + supabase test db tail)
  - Inline-fix log (any fixes that surfaced during UAT and were applied immediately)
```

Mirror that format for 03-VERIFICATION.md.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run full automated suite + capture output</name>
  <read_first>
    - package.json (test scripts: `pnpm test`, `pnpm test:all`, `pnpm typecheck`, `pnpm types:gen`)
    - .planning/phases/02-groups-invites/02-VERIFICATION.md (FULL — for the structure of the automated-suite output capture section)
  </read_first>
  <action>
    Run, in order, capturing output to a temporary scratch file or just the terminal scroll:

    ```bash
    cd /Users/chris/projects/accountibuzz

    # 1. Jest (RN unit + integration)
    pnpm test 2>&1 | tee /tmp/03-jest.log
    # Expected: PASS — all P1 + P2 + P3 test files green

    # 2. pgTAP (DB)
    supabase test db 2>&1 | tee /tmp/03-pgtap.log
    # Expected: ALL tests pass (P1 + P2 + 4 new P3 files: submit_today, review_submission,
    # get_pending_review_count, submissions_admin_immutable)

    # 3. TypeScript
    pnpm typecheck 2>&1 | tee /tmp/03-typecheck.log
    # Expected: 0 errors

    # 4. Expo Doctor (toolchain)
    npx expo-doctor 2>&1 | tee /tmp/03-doctor.log
    # Expected: green; pre-existing Android prebuild warnings about edgeToEdgeEnabled
    # + expo-system-ui are tracked in 01-foundation/deferred-items.md and are NOT P3 regressions.
    ```

    If ANY of the four commands fails:
    - **Jest failure** → identify which file; fix immediately if trivial (e.g. snapshot drift) OR document in 03-VERIFICATION.md as a blocker that must be addressed before phase close
    - **pgTAP failure** → identify which test/assertion; usually means the deployed schema diverges from what the test expects (re-run `supabase db push` first; then re-run `supabase test db`)
    - **typecheck failure** → fix immediately (TS errors don't auto-resolve)
    - **expo-doctor failure** → if it's the pre-existing Android warnings, document as continued deferred (NOT a P3 regression). If it's NEW (e.g. a P3 dep introduces a doctor warning), document.

    Do NOT proceed to Tasks 2+ until all four green OR all failures explicitly accepted in writing.
  </action>
  <verify>
    <automated>cd /Users/chris/projects/accountibuzz && pnpm test 2>&1 | tail -3 && supabase test db 2>&1 | tail -5 && pnpm typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits 0 (all Jest tests green)
    - `supabase test db` exits 0 (all pgTAP green — P1 + P2 + 4 new P3 files)
    - `pnpm typecheck` exits 0 (no TS errors)
    - `npx expo-doctor` either green OR only the documented pre-existing Android warnings
    - All 4 outputs captured for inclusion in 03-VERIFICATION.md
  </acceptance_criteria>
  <done>Automated gate passes; ready for manual UAT.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write 03-UAT.md (11-checkpoint manual walkthrough script)</name>
  <read_first>
    - .planning/phases/03-capture-admin-review/03-VALIDATION.md §"Manual-Only Verifications" (lines 130-143) — 11 manual UAT items
    - .planning/phases/03-capture-admin-review/03-UI-SPEC.md §State Matrix (multiple) — visual expectations to validate
    - .planning/phases/02-groups-invites/02-VERIFICATION.md (P2 UAT — for the script format/style)
    - .planning/phases/03-capture-admin-review/03-CONTEXT.md §D-12 (rejection terminal — UAT must verify the resubmit block)
  </read_first>
  <action>
    Create `.planning/phases/03-capture-admin-review/03-UAT.md` with an 11-checkpoint walkthrough. Each checkpoint:
    - **Pre-conditions** (state to set up before the test)
    - **Steps** (numbered actions the human takes)
    - **Expected result** (what the human should see)
    - **Pass criteria** (single-sentence yes/no)
    - **Receipt slot** (`PASS / FAIL / DEFERRED — initials + date`)

    The 11 checkpoints (per VALIDATION lines 132-143):

    1. **10s video capture on physical device** (SUB-02) — record a 10s video; observe file size < ~25 MB and app stays responsive throughout.
    2. **Airplane-mode upload-resilience** (SUB-03 + PITFALLS §10) — toggle airplane-mode ON, capture + tap Submit, observe QueueBadge on Today, toggle airplane-mode OFF, observe queue auto-flush + Pending pill appears within 5s.
    3. **Slow-3G upload progress** (SUB-03 + PITFALLS §10) — Network Link Conditioner "Slow 3G" preset on iOS, capture 5MB photo, tap Submit, observe upload completes (may take 10-30s), Pending pill appears.
    4. **JWT refresh during long upload** (PITFALLS §12) — set device clock forward to force JWT expiry mid-upload, capture + tap Submit, observe retry layer recovers (the upload completes; possibly with a brief retry blip in logs).
    5. **Cross-device Realtime cross-fade** (SUB-04 / ADM-04) — on member device: open Today, see Pending pill on the just-submitted GroupCard. On admin device (separate device or browser-based admin): approve. On member device: pill cross-fades to Approved within 2s.
    6. **Camera permission denial + Settings deep-link** (SUB-01 / SUB-02) — deny camera at OS prompt, observe permission-denied screen renders with project-specific copy ("Accountibuzz needs camera access..."), tap Open Settings, grant in OS Settings, return to app, observe viewfinder appears.
    7. **Tinder-stack swipe feel** (ADM-02 / ADM-03) — open admin queue with ≥3 pending submissions, swipe right (approve), swipe left (reject), mid-drag-release (under-threshold rubber-band), observe animations match UI-SPEC §Interaction Contracts (250ms ease-out fly-off, 300ms spring rubber-band).
    8. **Discard-take modal during in-progress recording** (SUB-02) — start recording video, tap × close, observe `Discard this take?` modal appears with `Keep recording` (NEVER `Cancel`) dismiss label, verify no recording leftover in temp dir after Discard.
    9. **Reduced-motion mode disables gesture animations** (UI-SPEC line 1070) — enable Reduce Motion in OS Settings, return to admin queue, observe swipe gestures still work but card-fly-off + cross-fade are instant; fallback Approve / Reject buttons still functional.
    10. **Terminal rejection invariant** (D-12) — as admin: reject a submission with a reason. As that submitter: observe Today GroupCard cross-fades to `Today didn't count` (rejected pill, tappable for the reason modal). Attempt Submit again same day → typed error `You already submitted today...`. Confirm: streak resets at next group-local midnight (or document if pg_cron is not yet wired — that's P5; for P3 just confirm the resubmit block).
    11. **Full E2E loop** — sign up new test user → create photo group → invite a friend (use second device) → friend joins → friend submits photo → admin sees Pending pill on Today + Pending review (1) entry on group detail → admin approves via swipe → friend sees Approved pill within 2s → admin attempts to submit (admin is also member) → admin's own submission flows through review queue → both members have approved status by end.

    For each checkpoint, structure exactly as P2's UAT format. Example for checkpoint #2:

    ```markdown
    ## Checkpoint 2 — Airplane-mode upload resilience (SUB-03)

    ### Pre-conditions
    - iOS dev client installed (Plan 03-01 Task 4)
    - User signed in, member of at least one photo group
    - Today screen open, GroupCard visible for the photo group with status `none`

    ### Steps
    1. Toggle Airplane Mode ON in iOS Control Center
    2. Tap `Submit photo` on the GroupCard
    3. Capture screen opens; tap shutter to capture a photo
    4. Tap `Submit photo` in ReviewPanel
    5. Capture screen dismisses (back to Today)
    6. Observe: GroupCard now shows `Submit photo` CTA still (no Pending pill yet) AND a QueueBadge below the card showing `Upload pending — N MB queued`
    7. Toggle Airplane Mode OFF
    8. Wait up to 5 seconds

    ### Expected result
    - QueueBadge disappears
    - StatusPill cross-fades to `Pending review`
    - Cutoff hint changes to `Submitted just now`

    ### Pass criteria
    Queue auto-flushes within 5 seconds of network reconnect AND status updates to Pending without manual refresh.

    ### Receipt
    `_______________` (PASS / FAIL / DEFERRED — initials + date)
    ```

    Apply this exact structure to all 11 checkpoints. Per Phase 1+2 precedent, Android UAT is deferred — checkpoints are iOS-only unless explicitly noted.

    Save as `.planning/phases/03-capture-admin-review/03-UAT.md`.
  </action>
  <verify>
    <automated>cd /Users/chris/projects/accountibuzz && [ -f .planning/phases/03-capture-admin-review/03-UAT.md ] && grep -c "## Checkpoint" .planning/phases/03-capture-admin-review/03-UAT.md | grep -q "^11$" && grep -q "Airplane" .planning/phases/03-capture-admin-review/03-UAT.md && grep -q "Realtime" .planning/phases/03-capture-admin-review/03-UAT.md && grep -q "Discard this take" .planning/phases/03-capture-admin-review/03-UAT.md && grep -q "Receipt" .planning/phases/03-capture-admin-review/03-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `03-UAT.md` exists
    - Contains exactly 11 `## Checkpoint` sections (one per VALIDATION line 132-143 + the E2E loop)
    - Each checkpoint has Pre-conditions / Steps / Expected result / Pass criteria / Receipt sub-sections
    - Receipt slots are blank (to be filled by the human during the walkthrough)
  </acceptance_criteria>
  <done>UAT script ready; user runs through it on physical device.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run 11-checkpoint UAT walkthrough on iOS physical device</name>
  <files>(no file edits; the user fills receipts into 03-UAT.md during the walkthrough)</files>
  <action>
    Human-only checkpoint — Claude cannot operate an iPhone interactively for the 11-checkpoint UAT walkthrough. Claude's role: ensure the dev client is running (`npx expo run:ios` in a background terminal if needed) and relay the checkpoint instructions in 03-UAT.md. After the user fills each receipt, capture the verdicts into 03-08-SUMMARY.md. Inline fixes welcome — apply 1-line obvious fixes immediately and document.
  </action>
  <verify>
    <automated>pnpm test 2>&amp;1 | tail -3 &amp;&amp; supabase test db 2>&amp;1 | tail -5 &amp;&amp; pnpm typecheck 2>&amp;1 | tail -3</automated>
  </verify>
  <what-built>
    Tasks 1+2 produced: full automated suite green + 11-checkpoint UAT script. The UAT walkthrough requires a HUMAN running through each checkpoint on a physical iOS device with the dev client installed (Plan 03-01 Task 4). Two devices are preferable for Checkpoint 5 (cross-device Realtime); a second supabase-auth user can be created on the same device by signing out / signing in to alternate accounts if a second device isn't available.

    Per project memory (Phase 1+2 precedent): iOS only — Android UAT deferred to a future hardening pass.

    The receipts in `03-UAT.md` get filled in by the user during this checkpoint.
  </what-built>
  <how-to-verify>
    Open `.planning/phases/03-capture-admin-review/03-UAT.md` and run through each of the 11 checkpoints in order on an iOS physical device.

    For each checkpoint:
    1. Set up the pre-conditions
    2. Execute the steps
    3. Compare actual result to expected result
    4. Mark the receipt: `PASS` / `FAIL` / `DEFERRED` + initials + date
    5. If `FAIL`: capture a screenshot or video AND describe what went wrong in the checkpoint's notes section

    **Stopping conditions:**
    - 3 consecutive `FAIL`s → halt UAT, report back to planner via `/gsd-plan-phase --gaps` for gap-closure plan
    - Any `FAIL` on a P3 BLOCKING requirement (cross-device Realtime, airplane-mode resilience, terminal rejection, camera permission flow) → halt UAT, request fix
    - `DEFERRED` is acceptable for: (a) Slow-3G test if no Network Link Conditioner available — defer to Plan 03-08-A retest, (b) JWT refresh test if device clock manipulation isn't accessible — defer

    **Inline fixes welcome:** if a UAT failure surfaces a 1-line obvious fix (e.g. wrong copy string, missing import), apply the fix immediately and document in the checkpoint receipt as `INLINE FIX: <description>` then re-run the checkpoint.

    Time budget: ~60-90 minutes for the full 11-checkpoint walkthrough.
  </how-to-verify>
  <resume-signal>Type "approved — UAT complete: N PASS, M FAIL, K DEFERRED" with brief notes on any FAIL/DEFERRED items.</resume-signal>
  <acceptance_criteria>
    - All 11 checkpoint receipts in `03-UAT.md` are filled in
    - At minimum: SUB-01, SUB-02, SUB-04, SUB-05, ADM-02, ADM-03, ADM-04 checkpoints are PASS (these are the core loop)
    - **PER REVIEWS.md C9 (NON-DEFERRABLE):** the SUB-03 airplane-mode-toggle UAT checkpoint (Checkpoint 2 — toggle airplane ON before submit, dismiss to Today, see QueueBadge, toggle airplane OFF, queue auto-flushes within ~5s, StatusPill cross-fades to Pending review) MUST be PASS. SUB-03 is the headline product promise of Phase 3 ("upload is resilient to flaky networks"); deferred-with-reason is NOT acceptable for this specific receipt. Slow-3G + JWT-refresh receipts CAN remain deferrable due to tooling constraints, but the airplane-mode receipt is a hard gate.
    - PLAT-03 implicitly validated by Checkpoint 11 (E2E loop — the admin queue gating works)
    - Any FAIL items have inline fixes applied OR are documented as gap-closure work for a Plan 03-09 follow-up
    - Any DEFERRED items have a clear reason (e.g. "no Slow-3G tooling available") — except for the SUB-03 airplane-mode receipt (see above)
  </acceptance_criteria>
  <done>11-checkpoint UAT receipts captured; phase ready for the verification matrix + doc updates.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Write 03-VERIFICATION.md + update REQUIREMENTS.md ADM-04 + ROADMAP.md SC#5</name>
  <read_first>
    - .planning/phases/02-groups-invites/02-VERIFICATION.md (FULL — for the per-requirement matrix format + automated-suite capture sections)
    - .planning/phases/03-capture-admin-review/03-UAT.md (just written + filled by Task 3 — for the receipts)
    - /tmp/03-jest.log + /tmp/03-pgtap.log + /tmp/03-typecheck.log + /tmp/03-doctor.log (or terminal scroll from Task 1)
    - .planning/REQUIREMENTS.md (line 49 — current ADM-04 wording to update)
    - .planning/ROADMAP.md (Phase 3 Success Criterion #5 — current wording to update)
    - .planning/phases/03-capture-admin-review/03-CONTEXT.md §"Open Items" (lines 188-190) — the planned wording rationale
  </read_first>
  <action>
    **Create `.planning/phases/03-capture-admin-review/03-VERIFICATION.md`**:

    Mirror the P2 VERIFICATION format. Required sections:

    1. **Header**: phase, date, gate status (PASS / FAIL / PARTIAL)
    2. **Per-requirement validation matrix** — one row per Phase 3 requirement ID:
       | Req | Description | Test Type | Test Source | Receipt | Status |
       |-----|-------------|-----------|-------------|---------|--------|
       | SUB-01 | Member can capture and submit a photo... | Jest + pgTAP + UAT-1 | submitMedia.test.ts + submit_today.sql + 03-UAT.md ck#11 | PASS | ✅ |
       | SUB-02 | Member can capture and submit a short video... | Jest + pgTAP + UAT-1 | submitMedia.test.ts + submit_today.sql + 03-UAT.md ck#1 | PASS | ✅ |
       | SUB-03 | Upload is resilient to flaky networks | Jest + UAT-2/3/4 | uploadQueueManager.test.ts + 03-UAT.md ck#2,3,4 | **PER REVIEWS.md C9: airplane-mode UAT (ck#2) MUST be PASS — non-deferrable.** Other SUB-03 sub-tests can be deferred. | PASS | ✅ |
       | SUB-04 | Member can see status pending/approved/rejected | Jest + UAT-5 | useTodaySubmissionRealtime.test.ts + 03-UAT.md ck#5 | PASS | ✅ |
       | SUB-05 | Member is blocked from submitting twice | pgTAP + UAT-10 | submit_today.sql + 03-UAT.md ck#10 | PASS | ✅ |
       | SUB-06 | Caption ≤ 140 chars | Jest + pgTAP | schemas.test.ts + submit_today.sql | PASS | ✅ |
       | ADM-01 | Admin sees queue of pending | Jest + pgTAP + UAT-7 | useReviewQueue.test.ts + get_pending_review_count.sql + 03-UAT.md ck#7 | PASS | ✅ |
       | ADM-02 | Admin can approve | Jest + pgTAP + UAT-7 | useReviewSubmission.test.ts + review_submission.sql + 03-UAT.md ck#7 | PASS | ✅ |
       | ADM-03 | Admin can reject with reason | Jest + pgTAP + UAT-7,10 | useReviewSubmission.test.ts + review_submission.sql + 03-UAT.md ck#7,10 | PASS | ✅ |
       | ADM-04 (rescoped) | Rejected submitters notified that today did not count | Jest + UAT-10 | useTodaySubmissionRealtime.test.ts (review event) + 03-UAT.md ck#10 | PASS | ✅ |
       | PLAT-03 | Group admin can only review for their groups | pgTAP (cross-group) + UAT-11 | review_submission.sql cross-group test + 03-UAT.md ck#11 | PASS | ✅ |

       Adjust statuses based on actual UAT receipts (Task 3 outcome). Use `❌` for FAIL and `⚠️` for DEFERRED.

    3. **Automated suite output capture**:
       - Jest summary (last 5 lines of `pnpm test` output)
       - pgTAP summary (`supabase test db` output)
       - Typecheck summary (`pnpm typecheck` exit code + first 10 lines if errors)
       - expo-doctor summary

    4. **Inline-fix log** — any 1-line fixes applied during UAT walkthrough:
       | Symptom | Root cause | File | Fix | Receipt |
       |---------|-----------|------|-----|---------|
       | (e.g. "Caption counter color stayed muted at 140 chars") | (Conditional check used >= instead of >) | `src/components/ReviewPanel.tsx` | Changed `>=` to `>` on line N | UAT ck#11 PASS after fix |

    5. **Threat-model validation** — confirm each P3 plan's `<threat_model>` STRIDE entries are mitigated:
       - PLAT-03 three-layer defense (RLS + RPC + 0003 trigger) — validated by `submissions_admin_immutable.sql` + `review_submission.sql` cross-group test
       - Threat 7 (cross-group review) — validated by Plan 03-02 pgTAP
       - Threat 8 (Realtime payload leak) — validated by Plan 03-05 useTodaySubmissionRealtime.test (subscribe with user_id filter)
       - Pitfall 9 (concurrent admin race) — validated by review_submission.sql 'not_pending' test
       - Pitfall 11 (Realtime channel leak) — validated by useTodaySubmissionRealtime.test cleanup-on-unmount
       - Pitfall 4 (queue corruption) — validated by uploadQueueManager.test corruption recovery
       - Pitfall 1 (server-derived local_date) — validated by submit_today.sql server-derived assertion

    6. **Phase close decision**: PASS / PARTIAL / FAIL with brief rationale.

    **Update `REQUIREMENTS.md` ADM-04** (line 49):
    - **BEFORE**: `**ADM-04**: Rejected submissions notify the submitter so they can resubmit before cutoff`
    - **AFTER**: `**ADM-04**: Rejected submitters are notified that today did not count (rejection is terminal — no resubmit per D-12)`

    **Update `ROADMAP.md` Phase 3 Success Criterion #5**:
    - **BEFORE**: `Rejected submitters are notified so they can resubmit before cutoff`
    - **AFTER**: `Rejected submitters are notified that today did not count` (or similar — match the spirit of the ADM-04 rewording)

    **Update `STATE.md`** (per Phase 1 + 2 precedent):
    - `progress.completed_phases`: 2 → 3
    - `progress.completed_plans`: 13 → 21 (add 8 P3 plans)
    - `Roadmap At-a-Glance` table: Phase 3 status → "Complete"
    - `Performance Metrics`: P3 requirements shipped — count from the matrix
    - `Last session` / `Next session`: update for Phase 4 readiness
    - `Stopped at`: "Phase 3 complete; awaiting /gsd-discuss-phase 4" or similar

    Commit all 4 doc updates plus 03-VERIFICATION.md + 03-UAT.md (Task 2 + 3 + 4 outputs) in a single Plan-level commit.
  </action>
  <verify>
    <automated>cd /Users/chris/projects/accountibuzz && [ -f .planning/phases/03-capture-admin-review/03-VERIFICATION.md ] && grep -q "SUB-01" .planning/phases/03-capture-admin-review/03-VERIFICATION.md && grep -q "PLAT-03" .planning/phases/03-capture-admin-review/03-VERIFICATION.md && grep -q "Inline-fix log\|Inline Fix" .planning/phases/03-capture-admin-review/03-VERIFICATION.md && grep -q "rescoped\|terminal\|did not count" .planning/REQUIREMENTS.md && ! grep -q "they can resubmit before cutoff" .planning/REQUIREMENTS.md && ! grep -q "they can resubmit before cutoff" .planning/ROADMAP.md && grep -q "did not count\|Rejected submitters are notified that today" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `03-VERIFICATION.md` exists with the per-requirement matrix (all 11 P3 IDs) + automated suite output + UAT receipts cross-referenced + threat-model validation + phase-close decision
    - `REQUIREMENTS.md` line 49 (ADM-04) updated to terminal-rejection wording (no longer mentions "resubmit before cutoff")
    - `ROADMAP.md` Phase 3 Success Criterion #5 updated to match the rephrasing
    - `STATE.md` updated: completed_phases: 3, completed_plans: 21, Phase 3 Complete, next session pointer updated
    - All 4 doc updates committed
  </acceptance_criteria>
  <done>Phase 3 verification complete; CONTEXT Open Items closed; ready to advance to Phase 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated suite ↔ deployed schema | `pnpm test` and `supabase test db` both run against artifacts the human can inspect — no hidden state. |
| UAT walkthrough | Physical-device behavior is the ground truth for SUB-03 (network resilience), SUB-02 (video file size), SUB-04 (Realtime cross-fade timing). Simulator behavior diverges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-01 | Tampering | UAT receipt fabrication | accept | Receipts are filled by the user (the developer) — no external incentive to falsify. The cross-device Realtime test requires actual cross-device observation; can't be faked from terminal. |
| T-03-08-02 | Information Disclosure | Test logs may capture session tokens | mitigate | The test setup uses test-only Supabase users created in the pgTAP fixture (not real production users). The Jest mocks supply mock session shapes. No production secrets in logs. |
| T-03-08-03 | Denial of Service | UAT walkthrough takes longer than expected (>90 min) | accept | Acceptable; phase close is not time-critical. If UAT surfaces blockers, proceed to gap-closure (Plan 03-09 via `/gsd-plan-phase --gaps`). |
| T-03-08-04 | Tampering | REQUIREMENTS.md / ROADMAP.md updates lose history | mitigate | Git tracks the changes. The CONTEXT Open Items section explicitly tracked the planned rewording so the audit trail is complete (CONTEXT line 188-190 → REQUIREMENTS line 49 + ROADMAP SC#5). |
</threat_model>

<verification>
After all 4 tasks:

```bash
cd /Users/chris/projects/accountibuzz

# All 4 docs present + updated
[ -f .planning/phases/03-capture-admin-review/03-VERIFICATION.md ]
[ -f .planning/phases/03-capture-admin-review/03-UAT.md ]
grep -q "did not count" .planning/REQUIREMENTS.md
grep -q "did not count" .planning/ROADMAP.md

# STATE.md shows Phase 3 complete
grep -q "Phase 3.*Complete\|completed_phases: 3" .planning/STATE.md

# Full automated suite still green
pnpm test
supabase test db
pnpm typecheck
```
</verification>

<success_criteria>
- Full automated suite GREEN: `pnpm test` + `supabase test db` + `pnpm typecheck` + `expo-doctor` (or pre-existing-deferred warnings only)
- 11/11 UAT checkpoints have receipts (PASS / FAIL / DEFERRED)
- All 11 Phase 3 requirement IDs (SUB-01..06, ADM-01..04, PLAT-03) have at least one PASS receipt OR explicit deferred-with-reason — **PER REVIEWS.md C9 EXCEPTION:** SUB-03's airplane-mode-toggle UAT (Checkpoint 2) is NON-DEFERRABLE. SUB-03 is the headline product promise of P3 ("upload is resilient to flaky networks"), so the airplane-mode-then-reconnect manual UAT MUST receive a PASS receipt before phase close. Slow-3G + JWT-refresh receipts CAN remain deferrable (tooling-dependent), but airplane-mode-PASS is a hard gate.
- `03-VERIFICATION.md` exists with the validation matrix + automated outputs + threat-model receipts
- `03-UAT.md` exists with all 11 checkpoint walkthroughs + filled receipts
- `REQUIREMENTS.md` ADM-04 updated to terminal-rejection wording
- `ROADMAP.md` Phase 3 Success Criterion #5 updated
- `STATE.md` shows Phase 3 complete + next-session pointer updated
- All doc updates committed
</success_criteria>

<output>
After completion, create `.planning/phases/03-capture-admin-review/03-08-SUMMARY.md` per the template at `$HOME/.claude/get-shit-done/templates/summary.md`.

Key items to record:
- The PASS/FAIL/DEFERRED breakdown of the 11 UAT checkpoints (e.g. "11 PASS, 0 FAIL, 0 DEFERRED" or "9 PASS, 0 FAIL, 2 DEFERRED — Slow-3G + JWT refresh deferred for lack of tooling")
- Any inline fixes applied during UAT (file + 1-line summary per fix)
- Whether ANY P3 plan's threat-model entries were not validated by the test/UAT pass — if any, what's the gap and how to address
- The new STATE.md status string (e.g. "Phase 3 complete; awaiting /gsd-discuss-phase 4")
- Any items moved to `01-foundation/deferred-items.md` (or a new P3 deferred-items.md if substantial — likely orphan media cleanup tracking, the `12:00 AM` cutoff label question, etc.)
- Cross-reference: which Plan 03-XX SUMMARY documents recorded notes that this plan acted on (e.g. Plan 03-06 SUMMARY noted the cutoffTime literal — confirm what was decided)
- Phase 4 readiness flag: confirm submissions table is populated with realistic test data (a few approved + rejected rows) so Phase 4's leaderboard + feed have content to render
</output>
