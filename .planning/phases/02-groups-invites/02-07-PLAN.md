---
phase: 02
plan: 07
type: execute
wave: 5
depends_on: [02-04, 02-05, 02-06]
files_modified: []
autonomous: false
requirements: [GRP-01, GRP-02, GRP-03, GRP-04, GRP-05, INV-01, INV-02, INV-03]
estimated_tasks: 2
tags: [verification, manual-uat, phase-gate]
user_setup: []

must_haves:
  truths:
    - "`pnpm test:all` green: Jest (all unit + integration) + pgTAP (all 11 suites — P1 3 + P2 8)"
    - "`pnpm typecheck` green"
    - "`npx expo-doctor` green"
    - "RLS CI check (`.github/workflows/rls-check.yml`) still passes — no new public-schema tables added in P2, all P1 tables still RLS-enabled"
    - "Manual iOS dev-build walkthrough exercises every Phase 2 success criterion"
  artifacts: []
  key_links: []
---

<objective>
Gate Phase 2 closure. Before declaring the phase complete, every automated check must be green AND the manual iOS walkthrough must exercise every binding success criterion from ROADMAP.md §Phase 2. This is the final checkpoint before `/gsd-verify-work`.

Purpose: Phase 1 set a precedent of iOS-only UAT (Android environment not set up per STATE.md). Phase 2 inherits that stance. The automated suite catches logic regressions; the manual walkthrough catches visual/flow regressions that RN Testing Library can't observe.

Output: Two sequential tasks — a fully automated green-light gate, then a human checkpoint that walks the full P2 user journey.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-groups-invites/02-CONTEXT.md
@.planning/phases/02-groups-invites/02-UI-SPEC.md
@.planning/phases/02-groups-invites/02-VALIDATION.md
@.planning/phases/02-groups-invites/02-01-SUMMARY.md
@.planning/phases/02-groups-invites/02-02-SUMMARY.md
@.planning/phases/02-groups-invites/02-03-SUMMARY.md
@.planning/phases/02-groups-invites/02-04-SUMMARY.md
@.planning/phases/02-groups-invites/02-05-SUMMARY.md
@.planning/phases/02-groups-invites/02-06-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run the full automated suite and document results</name>
  <files>(no file edits; runs pnpm typecheck / pnpm test / supabase test db / npx expo-doctor)</files>
  <read_first>
    - .planning/phases/02-groups-invites/02-VALIDATION.md §"Sampling Rate" (confirms `pnpm test:all` + typecheck + expo-doctor gate)
    - package.json (confirm `test:all` and `typecheck` scripts; confirm `types:gen` already ran in plan 02)
    - .github/workflows/rls-check.yml (confirm the RLS probe still targets all public-schema tables)
  </read_first>
  <action>
    Run each command IN ORDER. If any exits non-zero, STOP and surface the failure — do not continue to the next command, and do not auto-fix without re-verification.

    ```bash
    pnpm typecheck
    pnpm test
    supabase test db
    npx expo-doctor
    ```

    Then run the combined script if `test:all` is defined:
    ```bash
    pnpm test:all
    ```

    Also verify the RLS CI check would pass locally by running whatever local equivalent `.github/workflows/rls-check.yml` uses — typically a SQL probe:
    ```bash
    # Read .github/workflows/rls-check.yml and reproduce its probe command locally against the reset local DB.
    # Example (planner illustrative; executor verify against actual workflow):
    # psql "$(supabase status | grep 'DB URL' | awk '{print $NF}')" -c \
    #   "select schemaname, tablename from pg_tables where schemaname='public' and rowsecurity=false"
    ```
    Expected: zero rows returned (every public-schema table has RLS enabled).

    Finally, compile a results table for the SUMMARY.md:

    | Check | Command | Result | Notes |
    |-------|---------|--------|-------|
    | Typecheck | `pnpm typecheck` | ✅/❌ | ... |
    | Jest | `pnpm test` | ✅/❌ + count | ... |
    | pgTAP | `supabase test db` | ✅/❌ + count (expect 11 files) | ... |
    | expo-doctor | `npx expo-doctor` | ✅/❌ | ... |
    | RLS probe | local SQL | ✅/❌ | zero rows |

    If every row is ✅, proceed to Task 2. If any ❌, fix the failing check before proceeding — a phase gate with red checks is not a gate.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test && supabase test db && npx expo-doctor 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` exits 0
    - `pnpm test` exits 0 (total Jest test count includes P1 tests + all tests added in plans 01/03/04/05/06 — expected ≥ 25 new P2 cases, full repo ≥ 40+)
    - `supabase test db` exits 0 (expect 11 pgTAP files: P1 profiles_rls, profiles_trigger, rls_helpers + P2 create_group, redeem_invite, get_invite_preview, leave_group, transfer_admin, delete_group, regenerate_invite, invites_policies)
    - `npx expo-doctor` exits 0 (no warnings requiring action)
    - Local RLS probe returns zero rows against public schema (no RLS-off tables)
  </acceptance_criteria>
  <done>Every automated check green; results captured for SUMMARY.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual iOS dev-build walkthrough — full Phase 2 user journey</name>
  <files>(no file edits; manual iOS dev-build verification)</files>
  <action>
    Human-only checkpoint — Claude cannot operate an iPhone / iOS simulator interactively for UI verification. Claude's role: ensure the dev build starts (`npx expo run:ios` in a separate terminal if not already running) and relay the checkpoint instructions below to the user. After the user reports results for each of the 11 checkpoints (A–K), capture the verdicts into `02-07-SUMMARY.md`.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm test &amp;&amp; supabase test db</automated>
  </verify>
  <done>User replies with "approved" (optionally with per-checkpoint notes) for every checkpoint A–K. Any failed checkpoint must be captured and escalated via a gap-closure plan before the phase closes.</done>
  <what-built>
    The entire Phase 2 surface: group creation, groups-list home, group detail (both admin and member views), invite share, invite code copy, join-by-code, deep-link redemption with auth detour, admin-leave branching modal (transfer + delete paths), leave-group flow, regenerate-invite flow.
  </what-built>
  <how-to-verify>
    Follow the walkthrough in ORDER. Use the iOS dev build (`npx expo run:ios`). Android is explicitly deferred per Phase 1 precedent (STATE.md — no Android env set up).

    **Pre-flight:**
    1. `npx expo run:ios` builds + launches the dev build on the iOS simulator.
    2. If prompted to re-prebuild because plans 01 added `expo-clipboard` + `expo-haptics`, accept; these are plugin-free libs but `npx expo install` may have amended `ios/Podfile.lock`.
    3. Sign in with your P1 test account (or sign up a fresh one).

    **Checkpoint A: Empty state (GRP-03)**
    - Signed-in home should show `'Your groups'` title + `'No groups yet'` empty state + `'Create a group'` primary CTA + `'Join with a code'` accent link.
    - PASS CRITERIA: empty-state copy matches UI-SPEC §Copywriting verbatim.

    **Checkpoint B: Create group (GRP-01, GRP-02)**
    - Tap `Create a group` → `/groups/new`.
    - Verify 4 fields: Group name, goal (with char counter), submission type Photo/Video segmented control, timezone (defaulted to device tz).
    - Tap the timezone field → Iana picker opens (sheet on iOS). Type `Tokyo` → list filters. Select `Asia/Tokyo`. Picker dismisses.
    - Fill name `Morning runners`, goal `Post a photo of your run before 9am.`, segmented = `Photo`, tz = Asia/Tokyo.
    - Tap `Create group` → routes to `/groups/<new_id>`.
    - PASS CRITERIA: detail screen loads with the group's name as display header + post-create banner `'Group created — share the code to invite friends.'`.

    **Checkpoint C: Admin invite panel (INV-01)**
    - Verify the admin invite panel shows: `'Invite code'` caption + chunked 8-char code (format `ABCD-EF12`) + `Share code` primary button + `Regenerate code` accent link.
    - Tap `Copy` in the InviteCodeChip → feel haptic + see text swap `Copied ✓` for 2s → revert.
    - Paste into Notes app → verify the raw 8-char code (not dashed) was copied.
    - Tap `Share code` → OS share sheet opens with message containing the group name, the dashed code, `accountibuzz://invite/{raw_code}`, and `<store link placeholder>` literal.
    - PASS CRITERIA: share-sheet message matches UI-SPEC §Native share-sheet message (lines 292-297).

    **Checkpoint D: Deep-link redemption (INV-02) — authed path**
    - Dismiss the share sheet. Open Notes app. Paste the `accountibuzz://invite/{raw_code}` URL. Tap it.
    - PASS CRITERIA: App opens on `/invite/{code}` → renders `'Ready to join?'` → shows `You're about to join {group_name}.` → tapping `Join group` surfaces `already_member` error copy (because you ARE already the admin).

    **Checkpoint E: Regenerate invite (INV-01)**
    - Back in the detail screen, tap `Regenerate code` → Modal opens: `'Make a new code?'` + cancelLabel `'Keep current code'`.
    - Tap `Regenerate` (primary).
    - PASS CRITERIA: Modal closes → banner appears: `'New code ready — share it with anyone who hasn't joined yet.'` → chip shows a DIFFERENT 8-char code.

    **Checkpoint F: Second-user join flow (GRP-03, INV-02, INV-03)**
    - Sign out. Sign up a second test account (different email).
    - Empty-state home → tap `Join with a code`.
    - Enter the code (can use `abcd-ef12` lowercase → should normalize to uppercase + dashed display).
    - Tap `Join group` → routes to the group's detail screen.
    - PASS CRITERIA: You now see the group as a MEMBER (no admin invite panel, `Leave group` destructive text at the bottom, no `Transfer admin`/`Delete group` actions).

    **Checkpoint G: Members list (GRP-04)**
    - Verify members section shows BOTH users with their avatars + display names + `ADMIN` badge on the original admin's row.
    - Pull-to-refresh → members list reloads.

    **Checkpoint H: Member leave (GRP-05)**
    - Tap `Leave group` → Modal opens: `'Leave {group_name}?'` + cancelLabel `'Stay in group'`.
    - Tap `Stay in group` → modal dismisses.
    - Tap `Leave group` again → tap `Leave group` (destructive filled) → routes to home → group no longer in list.

    **Checkpoint I: Admin-leave branching (GRP-05 + D-10)**
    - Sign back in as the first (admin) account.
    - Open the group → tap kebab menu → tap `Leave group`.
    - PASS CRITERIA: Admin-leave branching modal opens with title `'Admins can't just leave'` + primary `Transfer admin instead` + secondary `Delete the group` + cancelLabel `'Never mind'`.
    - Tap `Never mind` → modal dismisses.
    - Tap kebab → `Leave group` again → tap `Transfer admin instead` → picker modal opens with the remaining member selectable.
    - Tap `Keep my admin role` → modal dismisses (no transfer).
    - Tap kebab → `Delete group` → `'Delete this group?'` modal with cancelLabel `'Keep the group'`.
    - Tap `Delete group` (destructive filled) → routes to home → group deleted.

    **Checkpoint J: Deep-link redemption (INV-02) — unauth path**
    - Sign out. Re-create a group from the second account (now empty-state again after the first was deleted). Share the code (pick it up from the share sheet or copy).
    - Sign out. Tap the deep link from Notes.
    - PASS CRITERIA: `'{admin} invited you'` preview renders. Tap `Sign in to join` → routes to login → enter credentials → post-login you automatically land on the invite screen again (replay) → tap `Join group` → joined.

    **Checkpoint K: 10-member cap (INV-03)**
    - OUT-OF-BAND testing if needed: cap test can be done with SQL directly (seed 10 members, attempt an 11th via `redeem_invite`) — covered by pgTAP in plan 02. A real 11-device test is impractical for solo UAT. Accept pgTAP coverage as sufficient here per plan 02 Task 2.

    **Sign-off:** Type `approved` (with a short note per checkpoint) OR describe any issue that failed. If any checkpoint fails, escalate to planner/executor with the specific failure.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally with notes per checkpoint), or describe failures in format "Checkpoint X: <issue>".</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new boundaries introduced by this plan — it only runs verification against existing surfaces.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-UAT-GAP | N/A (operational) | iOS-only UAT with Android deferred | accept | Per STATE.md and P1 precedent. Pre-rollout hardening (P6) is the natural revisit point. Each Phase 2 checkpoint covers a specific binding success criterion so regressions are catchable even without Android coverage. |
| T-02-CAP-RACE (UAT) | Tampering | 11-device live cap test impractical | accept | Covered by pgTAP structural+behavioral test in plan 02 (FOR UPDATE lock assertion + 10-member seed + 11th-attempt rejection). Live UAT is not load-testing; the unit suite is the authoritative proof. |
</threat_model>

<verification>
- `pnpm typecheck` + `pnpm test` + `supabase test db` + `npx expo-doctor` all exit 0
- Manual UAT checkpoint receives `approved` (with optional per-checkpoint notes) OR specific failure descriptions surface and the phase does not close
</verification>

<success_criteria>
- [ ] `pnpm typecheck` green
- [ ] `pnpm test` green (full repo)
- [ ] `supabase test db` green with 11 pgTAP files
- [ ] `npx expo-doctor` green
- [ ] RLS local probe returns zero RLS-off tables
- [ ] All 11 manual checkpoints (A–K) approved by user
- [ ] Every ROADMAP §Phase 2 success criterion exercised at least once
</success_criteria>

<output>
After completion, create `.planning/phases/02-groups-invites/02-07-SUMMARY.md` with:
- Automated results table (Task 1)
- UAT approval + any per-checkpoint notes (Task 2)
- Test counts: Jest total, pgTAP total, any skipped tests with reason
- Final Phase 2 status: ready for `/gsd-verify-work` OR blocked on X/Y/Z
</output>
