- 2026-04-22 (01-03): Stale worktree `.claude/worktrees/agent-a00b0e89/` contains copy of tests that fail jest (AsyncStorage v3 mock issue). Out of scope for 01-03. Cleanup: remove the stale worktree dir OR add it to `testPathIgnorePatterns` in `jest.config.js`. Not a regression from this plan — failures pre-date it. **Status 2026-04-22:** Mitigated via `testPathIgnorePatterns` in `jest.config.js` (commit c88082c adds `/.claude/`). Stale worktree dirs `agent-a00b0e89` and `agent-a87a6cce` are still on disk — delete manually when convenient.

- 2026-04-22 (01-06): **Android UAT deferred.** PLAT-01 verification on Android is incomplete. Same RN codebase, no iOS-specific hacks — Android-only issues are unlikely for Phase 1, but the walkthrough (steps 1–9 of 01-06-PLAN Task 2) must still be run on a real Android device or emulator. Blocker: no Android workstation/env set up yet. Track as a remediation item for the Android env setup pass; not a real phase-01 gap but MUST be closed before any store-ready build.

- 2026-04-22 (01-06): **Gmail strips custom-scheme URLs.** Learning from the deep-link → OTP pivot (commit adfe89e). Gmail's HTML renderer rewrites/strips anchor hrefs that point at custom schemes like `accountibuzz://...`, and link-prefetch crawlers also consume single-use Supabase tokens. Any future flow that relies on emailed tappable links back into the app must either (a) use a universal/app link on an owned HTTPS domain, or (b) copy the OTP code pattern. This constrains universal-link domain ownership decisions for Phase 2 invite links.

- 2026-04-22 (01-06): **Supabase recovery OTP token length is variable.** Observed 8-digit tokens from a real project despite the common `{{ .Token }}` template emitting 6 digits. `otpSchema` in `src/features/auth/schemas.ts` was loosened from 6 to 4–10 digits (commit 5ccb9c6). If upstream Supabase standardizes or the dashboard template is locked to 6 digits, tighten back to `^\d{6}$`.

- 2026-04-22 (01-06): **Post-MVP skills audit.** `.planning/notes/2026-04-22-revisit-expo-rn-skills.md` captures a pending pass to re-audit the auth + profile implementation against the expo:* and supabase:* skills installed mid-plan. Not blocking, but should happen before Phase 3 uploads (TUS / resumable) land.
